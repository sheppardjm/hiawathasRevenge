---
phase: 33-pipeline-route-data
plan: 03
type: execute
wave: 3
depends_on: ["33-02"]
files_modified:
  - scripts/generate-routes-manifest.js
  - scripts/copy-gpx.js
  - src/content.config.ts
  - src/components/RouteMap.astro
  - src/components/ElevationProfile.astro
autonomous: true

must_haves:
  truths:
    - "public/data/routes.json exists with metadata for all 3 routes (id, name, color, sectorIds, totalMiles, elevationGainFeet)"
    - "All 3 GPX files are copied to public/ for download"
    - "content.config.ts loads from public/data/100mi/ subdirectory paths"
    - "RouteMap.astro fetches route-data, annotations, sector-elevations, and surface-points from /data/100mi/ paths"
    - "ElevationProfile.astro fetches route-data and annotations from /data/100mi/ paths"
    - "Running `npm run build` completes without errors and the site renders identically to v1.4"
  artifacts:
    - path: "scripts/generate-routes-manifest.js"
      provides: "Reads per-route route-data.json and route-config.js to produce routes.json manifest"
    - path: "public/data/routes.json"
      provides: "Route manifest with defaultRoute, routes array containing metadata"
    - path: "public/Munising_Hiawatha_s_Revenge.gpx"
      provides: "100mi GPX download"
    - path: "public/Hiawatha_s_Revenge_100k.gpx"
      provides: "100k GPX download"
    - path: "public/Hiawatha_s_Revenge_50K_.gpx"
      provides: "50k GPX download"
  key_links:
    - from: "src/content.config.ts"
      to: "public/data/100mi/route-data.json"
      via: "file() loader path"
      pattern: "public/data/100mi/route-data\\.json"
    - from: "src/components/RouteMap.astro"
      to: "/data/100mi/"
      via: "fetch() calls at runtime"
      pattern: "fetch.*data/100mi/"
    - from: "src/components/ElevationProfile.astro"
      to: "/data/100mi/"
      via: "fetch() calls at runtime"
      pattern: "fetch.*data/100mi/"
    - from: "scripts/generate-routes-manifest.js"
      to: "scripts/route-config.js"
      via: "import ROUTES, DEFAULT_ROUTE_ID"
      pattern: "import.*from.*route-config"
---

<objective>
Create the routes manifest generator, update copy-gpx for all 3 files, update all frontend data consumers to use 100mi subdirectory paths, and validate the full pipeline end-to-end with `npm run build`.

Purpose: Complete the Phase 33 pipeline so the build produces all required artifacts and the existing site continues to render correctly from the new data paths.

Output: routes.json manifest, 3 GPX files in public/, updated content/component paths, passing build.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-pipeline-route-data/33-RESEARCH.md
@.planning/phases/33-pipeline-route-data/33-01-SUMMARY.md
@.planning/phases/33-pipeline-route-data/33-02-SUMMARY.md
@scripts/copy-gpx.js
@scripts/route-config.js
@src/content.config.ts
@src/components/RouteMap.astro
@src/components/ElevationProfile.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create generate-routes-manifest.js and update copy-gpx.js</name>
  <files>scripts/generate-routes-manifest.js, scripts/copy-gpx.js</files>
  <action>
**Create `scripts/generate-routes-manifest.js`:**

1. Import `ROUTES`, `DEFAULT_ROUTE_ID` from `./route-config.js`
2. Import `readFileSync`, `writeFileSync` from `fs` and path utilities
3. For each route in ROUTES:
   - Read `public/data/${route.id}/route-data.json`
   - Extract `meta.totalMiles` and `meta.elevationGainFeet`
4. Build the manifest object:
   ```json
   {
     "defaultRoute": "100mi",
     "routes": [
       {
         "id": "100mi",
         "name": "100 Mile",
         "shortName": "100mi",
         "gpxFile": "Munising_Hiawatha_s_Revenge.gpx",
         "color": "#c8973e",
         "totalMiles": <from route-data.json>,
         "elevationGainFeet": <from route-data.json>,
         "sectorIds": ["sector-520", ...]
       },
       ...
     ]
   }
   ```
5. Write to `public/data/routes.json`
6. Log summary: number of routes, each route's name/miles/elevation

**Update `scripts/copy-gpx.js`:**

1. Import `ROUTES` from `./route-config.js`
2. Replace the single-file copy with a loop over `ROUTES`:
   ```
   for (const route of ROUTES) {
     const src = join(projectRoot, route.gpxFile);
     const dest = join(projectRoot, 'public', route.gpxFile);
     if (!existsSync(src)) {
       console.warn(`[copy-gpx] Source not found, skipping: ${route.gpxFile}`);
       continue;
     }
     copyFileSync(src, dest);
     console.log(`[copy-gpx] Copied ${route.gpxFile} to public/`);
   }
   ```
3. Remove the hardcoded single-file path variables.
  </action>
  <verify>
```
node scripts/generate-routes-manifest.js
node scripts/copy-gpx.js
```
Verify:
- `public/data/routes.json` exists with 3 route entries, each having totalMiles and elevationGainFeet
- `public/Munising_Hiawatha_s_Revenge.gpx` exists
- `public/Hiawatha_s_Revenge_100k.gpx` exists
- `public/Hiawatha_s_Revenge_50K_.gpx` exists
  </verify>
  <done>routes.json manifest is generated from per-route data. All 3 GPX files are copied to public/ for download.</done>
</task>

<task type="auto">
  <name>Task 2: Update content.config.ts and component fetch paths, then validate build</name>
  <files>src/content.config.ts, src/components/RouteMap.astro, src/components/ElevationProfile.astro</files>
  <action>
**Update `src/content.config.ts`:**

Change the 3 file() loader paths from flat to 100mi subdirectory:
- `file('public/data/route-data.json', ...)` -> `file('public/data/100mi/route-data.json', ...)`
- `file('public/data/annotations.json')` -> `file('public/data/100mi/annotations.json')`
- `file('public/data/sector-elevations.json')` -> `file('public/data/100mi/sector-elevations.json')`

Do NOT change `photos.json` or any other shared data paths. Only the 3 route-specific collections change.

**Update `src/components/RouteMap.astro`:**

Change the 4 route-specific fetch paths (around lines 311-315):
- `fetch('/data/route-data.json')` -> `fetch('/data/100mi/route-data.json')`
- `fetch('/data/annotations.json')` -> `fetch('/data/100mi/annotations.json')`
- `fetch('/data/sector-elevations.json')` -> `fetch('/data/100mi/sector-elevations.json')`
- `fetch('/data/surface-points.json')` -> `fetch('/data/100mi/surface-points.json')`

Do NOT change `fetch('/data/sector-details.json')` or `fetch('/data/photos.json')` -- these remain shared.

**Update `src/components/ElevationProfile.astro`:**

Change the 2 route-specific fetch paths (around lines 56, 66):
- `fetch('/data/route-data.json')` -> `fetch('/data/100mi/route-data.json')`
- `fetch('/data/annotations.json')` -> `fetch('/data/100mi/annotations.json')`

**Full pipeline validation:**

Run `npm run build` (which triggers the pipeline via prebuild, then Astro build). This is the critical end-to-end test. The build must:
1. Run all pipeline steps (3 routes x 4 route-specific steps + shared steps)
2. Produce all per-route JSON files
3. Produce routes.json manifest
4. Copy all 3 GPX files
5. Astro build succeeds (content collections resolve, components render)

If the build fails, debug and fix. Common failure points:
- content.config.ts path typo
- Missing subdirectory (parse-gpx didn't create it)
- generate-sector-details.js not finding 100mi/annotations.json
- Astro content collection schema mismatch
  </action>
  <verify>
```
npm run build
```
Must complete without errors. Then verify:
- `ls dist/data/100mi/` contains route-data.json, annotations.json, sector-elevations.json, surface-points.json
- `ls dist/data/100k/` contains the same 4 files
- `ls dist/data/50k/` contains the same 4 files
- `ls dist/data/routes.json` exists
- `ls dist/*.gpx` shows all 3 GPX files
- `ls dist/data/sector-details.json` exists (shared, unchanged)
  </verify>
  <done>All frontend data consumers point to 100mi subdirectory. `npm run build` completes successfully. The site renders identically to v1.4 from the new paths. All Phase 33 success criteria are met.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `public/data/routes.json` exists with 3 routes, each having id, name, color, totalMiles, elevationGainFeet, sectorIds
3. `public/data/100mi/` has 4 route-specific JSON files
4. `public/data/100k/` has 4 route-specific JSON files
5. `public/data/50k/` has 4 route-specific JSON files
6. `public/*.gpx` has 3 GPX files for download
7. Content collections in Astro resolve without schema errors
8. The built site loads the same data as v1.4 (100mi route renders correctly)
</verification>

<success_criteria>
Full pipeline runs end-to-end via `npm run build`. All 5 phase success criteria are met: 3 route subdirectories with 4 JSON files each, coordinate-based sector snapping, routes.json manifest, 3 GPX files in public/, and the 100mi site renders identically from new paths.
</success_criteria>

<output>
After completion, create `.planning/phases/33-pipeline-route-data/33-03-SUMMARY.md`
</output>
