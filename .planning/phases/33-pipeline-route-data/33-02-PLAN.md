---
phase: 33-pipeline-route-data
plan: 02
type: execute
wave: 2
depends_on: ["33-01"]
files_modified:
  - scripts/resolve-annotations.js
  - scripts/generate-surface-points.js
  - scripts/compute-sector-elevations.js
  - scripts/generate-sector-details.js
autonomous: true

must_haves:
  truths:
    - "resolve-annotations.js uses coordinate-based snapping (haversine to nearest route point), not mile-based snapping"
    - "Each route's annotations.json contains only the sectors in that route's sectorIds config"
    - "100k and 50k surface-points.json use proximity fallback from 100mi RidewithGPS data with 100m threshold"
    - "100mi surface-points.json still uses exact coordinate key lookup (no regression)"
    - "compute-sector-elevations.js reads from and writes to per-route subdirectories"
    - "generate-sector-details.js reads annotations from public/data/100mi/annotations.json"
  artifacts:
    - path: "public/data/100mi/annotations.json"
      provides: "7 sectors + 2 restock points snapped by coordinate"
    - path: "public/data/100k/annotations.json"
      provides: "4 sectors + 1 restock point snapped by coordinate"
    - path: "public/data/50k/annotations.json"
      provides: "4 sectors + 0 restock points snapped by coordinate"
    - path: "public/data/100mi/surface-points.json"
      provides: "Surface type per simplified point via exact key lookup"
    - path: "public/data/100k/surface-points.json"
      provides: "Surface type per simplified point via proximity fallback"
    - path: "public/data/50k/surface-points.json"
      provides: "Surface type per simplified point via proximity fallback"
    - path: "public/data/100mi/sector-elevations.json"
      provides: "Per-sector elevation profiles for 100mi"
    - path: "public/data/100k/sector-elevations.json"
      provides: "Per-sector elevation profiles for 100k"
    - path: "public/data/50k/sector-elevations.json"
      provides: "Per-sector elevation profiles for 50k"
  key_links:
    - from: "scripts/resolve-annotations.js"
      to: "scripts/route-config.js"
      via: "import ROUTES, SECTOR_DEFS, RESTOCK_DEFS"
      pattern: "import.*SECTOR_DEFS.*from.*route-config"
    - from: "scripts/generate-surface-points.js"
      to: "scripts/route-config.js"
      via: "import ROUTES to determine rwgpsJson availability"
      pattern: "import.*ROUTES.*from.*route-config"
    - from: "scripts/generate-sector-details.js"
      to: "public/data/100mi/annotations.json"
      via: "readFileSync to load canonical sector annotations"
      pattern: "100mi.*annotations\\.json"
---

<objective>
Convert resolve-annotations.js to coordinate-based snapping with per-route sector membership, add proximity-based surface fallback for routes without RidewithGPS data, and update all downstream scripts to read/write per-route subdirectories.

Purpose: Correct sector annotation positioning (coordinate-based vs mile-based) and enable surface coloring for 100k/50k routes that lack direct RidewithGPS JSON data.

Output: Per-route annotations.json, surface-points.json, and sector-elevations.json in all 3 route subdirectories. generate-sector-details.js reads from the updated 100mi path.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/33-pipeline-route-data/33-RESEARCH.md
@.planning/phases/33-pipeline-route-data/33-01-SUMMARY.md
@scripts/resolve-annotations.js
@scripts/generate-surface-points.js
@scripts/compute-sector-elevations.js
@scripts/generate-sector-details.js
@scripts/route-config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor resolve-annotations.js for coordinate-based snapping</name>
  <files>scripts/resolve-annotations.js</files>
  <action>
Rewrite `scripts/resolve-annotations.js` to:

1. Import `ROUTES`, `SECTOR_DEFS`, `RESTOCK_DEFS` from `./route-config.js`
2. Read `routeId` from `process.argv[2]`. Throw if missing.
3. Look up `routeConfig = ROUTES.find(r => r.id === routeId)`. Throw if not found.
4. Load route points from `public/data/${routeId}/route-data.json` (per-route subdirectory from plan 33-01).
5. Remove the hardcoded `GRAVEL_SECTORS` and `RESTOCK_POINTS` arrays. Use `SECTOR_DEFS` and `RESTOCK_DEFS` from route-config instead.
6. Filter sectors to only those in `routeConfig.sectorIds`: `const routeSectors = SECTOR_DEFS.filter(s => routeConfig.sectorIds.includes(s.id))`
7. Filter restock points to only those in `routeConfig.restockIds`: `const routeRestocks = RESTOCK_DEFS.filter(r => routeConfig.restockIds.includes(r.id))`
8. Replace the `snapByMileage` function with a `snapByCoordinate` function using haversine distance:
   ```
   function haversineMeters(lat1, lon1, lat2, lon2) {
     const R = 6371000;
     const toRad = d => d * Math.PI / 180;
     const dLat = toRad(lat2 - lat1);
     const dLon = toRad(lon2 - lon1);
     const a = Math.sin(dLat/2)**2 +
       Math.cos(toRad(lat1)) * Math.cos(toRad(lat2)) * Math.sin(dLon/2)**2;
     return R * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1-a));
   }

   function snapByCoordinate(targetLat, targetLon, points) {
     let bestIdx = 0, bestDist = Infinity;
     for (let i = 0; i < points.length; i++) {
       const dist = haversineMeters(targetLat, targetLon, points[i].lat, points[i].lon);
       if (dist < bestDist) { bestDist = dist; bestIdx = i; }
     }
     const pt = points[bestIdx];
     return { lat: pt.lat, lon: pt.lon, ele: pt.ele, miles: pt.miles, snapIdx: bestIdx, snapDist: bestDist };
   }
   ```
9. Snap sector start/end using `snapByCoordinate(sector.startLat, sector.startLon, routePoints)` and `snapByCoordinate(sector.endLat, sector.endLon, routePoints)`. Build the output object with the same shape as before (id, type, name, startMile, endMile, lengthMiles, startLat, startLon, endLat, endLon, startIdx, endIdx, difficulty, stars). Compute lengthMiles as `endSnap.miles - startSnap.miles`. Keep the startIdx < endIdx guard.
10. Snap restock points using `snapByCoordinate(restock.lat, restock.lon, routePoints)`. Output shape: `{ id, type: 'restock', name, mile: snap.miles, lat: snap.lat, lon: snap.lon, ele: snap.ele, snapIdx: snap.snapIdx }`.
11. Write output to `public/data/${routeId}/annotations.json`.
12. Log snap distances for each sector to aid debugging: `[resolve-annotations:${routeId}] sector-520 start snapped at 17m, end at 4m`
  </action>
  <verify>
Run for all 3 routes:
```
node scripts/resolve-annotations.js 100mi
node scripts/resolve-annotations.js 100k
node scripts/resolve-annotations.js 50k
```
Verify:
- 100mi/annotations.json has 7 sectors + 2 restocks = 9 entries
- 100k/annotations.json has 4 sectors + 1 restock = 5 entries
- 50k/annotations.json has 4 sectors + 0 restocks = 4 entries
- All sectors have startIdx < endIdx
- Log output shows snap distances (all should be under 200m for valid sectors)
  </verify>
  <done>resolve-annotations.js uses coordinate-based snapping, filters sectors/restocks by route membership from config, writes to per-route subdirectories. All 3 routes produce correct annotations.</done>
</task>

<task type="auto">
  <name>Task 2: Add proximity fallback to generate-surface-points.js and update remaining scripts</name>
  <files>scripts/generate-surface-points.js, scripts/compute-sector-elevations.js, scripts/generate-sector-details.js</files>
  <action>
**generate-surface-points.js:**

1. Import `ROUTES` from `./route-config.js`
2. Read `routeId` from `process.argv[2]`. Throw if missing.
3. Look up `routeConfig`.
4. Load route points from `public/data/${routeId}/route-data.json`.
5. Keep the existing `S_TO_SURFACE` mapping.
6. Branch on `routeConfig.rwgpsJson`:
   - If truthy (100mi): use existing exact coordinate key lookup (`origLookup` Map). Load from `resolve(ROOT, routeConfig.rwgpsJson)`.
   - If null (100k/50k): use proximity-based fallback:
     - Load 100mi's RidewithGPS JSON: `resolve(ROOT, ROUTES.find(r => r.id === '100mi').rwgpsJson)`
     - For each simplified point, find the nearest original track point by haversine distance
     - If distance < 100m, use that point's S value. Otherwise, surface = 'unknown'.
     - Add the same `haversineMeters` function from resolve-annotations (or extract to a shared utility -- inline is fine for now).
7. Write output to `public/data/${routeId}/surface-points.json`.
8. Adjust the unmatched threshold: for proximity mode, do NOT exit(1) on unmatched count > 5 since divergent segments legitimately produce 'unknown'. Instead, log a summary: `[generate-surface-points:${routeId}] ${matched} matched, ${unknown} unknown (proximity mode, threshold 100m)`.

**compute-sector-elevations.js:**

1. Read `routeId` from `process.argv[2]`. Throw if missing.
2. Change input paths:
   - `routeDataPath = resolve(ROOT, 'public', 'data', routeId, 'route-data.json')`
   - `annotationsPath = resolve(ROOT, 'public', 'data', routeId, 'annotations.json')`
3. Change output path: `resolve(ROOT, 'public', 'data', routeId, 'sector-elevations.json')`
4. Update log messages to include routeId.
5. No changes to the elevation computation logic itself.

**generate-sector-details.js:**

1. Update the `annotationsPath` from `resolve(ROOT, 'public', 'data', 'annotations.json')` to `resolve(ROOT, 'public', 'data', '100mi', 'annotations.json')`. This is because sector details are editorial/route-agnostic and the 100mi annotations contain all 7 sectors.
2. No other changes. This script is a shared step, not route-specific, so it does NOT accept routeId from argv.
  </action>
  <verify>
Run the full route-specific pipeline for each route (after 33-01's parse-gpx has already run):
```
node scripts/resolve-annotations.js 100mi
node scripts/generate-surface-points.js 100mi
node scripts/compute-sector-elevations.js 100mi

node scripts/resolve-annotations.js 100k
node scripts/generate-surface-points.js 100k
node scripts/compute-sector-elevations.js 100k

node scripts/resolve-annotations.js 50k
node scripts/generate-surface-points.js 50k
node scripts/compute-sector-elevations.js 50k
```
Then run the shared step:
```
node scripts/generate-sector-details.js
```
Verify:
- Each route subdirectory has annotations.json, surface-points.json, sector-elevations.json
- 100mi surface-points.json has 0 unmatched (exact key lookup preserved)
- 100k/50k surface-points.json have some 'unknown' entries (expected for divergent segments) but most shared segments show paved/gravel/dirt
- sector-details.json at public/data/sector-details.json still has 7 entries
- No errors or crashes
  </verify>
  <done>generate-surface-points.js has proximity fallback for routes without RidewithGPS JSON. compute-sector-elevations.js reads/writes per-route paths. generate-sector-details.js reads from 100mi/annotations.json. All 3 routes produce complete per-route data.</done>
</task>

</tasks>

<verification>
1. `public/data/100mi/` contains annotations.json (9 entries), surface-points.json (~456 entries, 0 unknown), sector-elevations.json (7 sectors)
2. `public/data/100k/` contains annotations.json (5 entries), surface-points.json (~300-450 entries), sector-elevations.json (4 sectors)
3. `public/data/50k/` contains annotations.json (4 entries), surface-points.json (~150-250 entries), sector-elevations.json (4 sectors)
4. `public/data/sector-details.json` still has 7 entries (shared, route-agnostic)
5. No script crashes when run individually with any valid routeId
6. Sector start/end coordinates in annotations.json align with actual gravel road positions (verify by checking snap distances in log output -- all under 200m)
</verification>

<success_criteria>
All 4 route-specific data files exist for each route. Coordinate-based snapping replaces mile-based. Surface proximity fallback works for 100k/50k. generate-sector-details.js reads from updated 100mi path without error.
</success_criteria>

<output>
After completion, create `.planning/phases/33-pipeline-route-data/33-02-SUMMARY.md`
</output>
