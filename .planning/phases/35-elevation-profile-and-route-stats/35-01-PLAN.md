---
phase: 35-elevation-profile-and-route-stats
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteMap.astro
  - src/components/ElevationProfile.astro
  - src/components/RouteStats.astro
autonomous: true

must_haves:
  truths:
    - "Switching routes before scrolling to the elevation chart, then scrolling down, shows the correct route's elevation profile (not hardcoded 100mi)"
    - "Route stats (miles, elevation, sectors) update to match the selected route when user switches"
    - "A third stat card shows sector count alongside miles and elevation"
  artifacts:
    - path: "src/components/RouteMap.astro"
      provides: "window.__activeRouteId set on every renderRoute() call"
      contains: "window.__activeRouteId"
    - path: "src/components/ElevationProfile.astro"
      provides: "Post-init route sync reading window.__activeRouteId"
      contains: "__activeRouteId"
    - path: "src/components/RouteStats.astro"
      provides: "Dynamic stat updates via route:change listener, sector count card, fixed HTML structure"
      contains: "route:change"
  key_links:
    - from: "src/components/RouteMap.astro"
      to: "window.__activeRouteId"
      via: "assignment in renderRoute() before route:change dispatch"
      pattern: "window\\.__activeRouteId\\s*="
    - from: "src/components/ElevationProfile.astro"
      to: "window.__activeRouteId"
      via: "read after chart construction in initChart()"
      pattern: "window\\.__activeRouteId"
    - from: "src/components/RouteStats.astro"
      to: "/data/routes.json"
      via: "cached fetch in route:change listener"
      pattern: "fetch.*routes\\.json"
---

<objective>
Fix the ElevationProfile lazy-init race condition and make RouteStats dynamically update on route switch.

Purpose: Ensures the elevation chart initializes with the correct route when a user switches routes before scrolling to the chart section, and that the stats section reflects the active route's distance, elevation gain, and sector count.

Output: Three modified Astro components — RouteMap (sets active route ID globally), ElevationProfile (reads it post-init), RouteStats (dynamic stat updates with sector count card).
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-elevation-profile-and-route-stats/35-RESEARCH.md

@src/components/RouteMap.astro
@src/components/ElevationProfile.astro
@src/components/RouteStats.astro
@src/pages/index.astro
@public/data/routes.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ElevationProfile lazy-init race condition</name>
  <files>src/components/RouteMap.astro, src/components/ElevationProfile.astro</files>
  <action>
    **RouteMap.astro** — In the `renderRoute()` function, add `window.__activeRouteId = routeId;` BEFORE the `route:change` CustomEvent dispatch (currently at line ~724). Place it right before the `window.dispatchEvent(new CustomEvent('route:change', ...))` call. This ensures the global is set before any listeners fire.

    **ElevationProfile.astro** — At the end of `initChart()`, after the `chart = new Chart(canvas, {...})` constructor (line ~131) and after the mousemove/mouseleave event listeners (line ~232), add a post-init sync check:

    ```javascript
    // Sync to active route if user switched before chart initialized
    const pendingRoute = window.__activeRouteId;
    if (pendingRoute && pendingRoute !== '100mi') {
      updateChart(pendingRoute);
    }
    ```

    Place this code at the very end of `initChart()`, just before the closing `}` of the function (before the `// end initChart` comment on line ~233). This handles the case where:
    1. User switches to 50k on the map (fires route:change)
    2. ElevationProfile's route:change listener gets `if (!chart) return` — silently drops the event
    3. User scrolls down, IntersectionObserver fires initChart()
    4. Chart constructs with 100mi data (hardcoded fetch on line 105)
    5. Post-init sync detects `window.__activeRouteId === '50k'` and calls `updateChart('50k')`

    Do NOT change the hardcoded `100mi` initial fetch in initChart(). The 100mi data is the correct default; the post-init sync handles the edge case.
  </action>
  <verify>
    Run `npm run build` to confirm no syntax errors. Then manually verify the logic:
    1. `grep -n '__activeRouteId' src/components/RouteMap.astro` should show the assignment in renderRoute()
    2. `grep -n '__activeRouteId' src/components/ElevationProfile.astro` should show the post-init check
    3. The `window.__activeRouteId` assignment must appear BEFORE the `route:change` dispatch in RouteMap.astro
  </verify>
  <done>
    RouteMap.astro sets `window.__activeRouteId` on every renderRoute() call. ElevationProfile.astro reads it after chart construction and calls updateChart() if the active route differs from the 100mi default.
  </done>
</task>

<task type="auto">
  <name>Task 2: Make RouteStats dynamic with sector count and route:change listener</name>
  <files>src/components/RouteStats.astro</files>
  <action>
    Modify `src/components/RouteStats.astro` to:

    **1. Fix the spurious `</div>` bug (line 17):**
    The current HTML has:
    ```html
    <div class="stats-grid">
      <div class="stat-card">...</div>
      <div class="stat-card">...</div>
      </div>   <!-- SPURIOUS — remove this line -->
    </div>
    ```
    Remove the extra `</div>` on line 17 so there is only one closing `</div>` for `.stats-grid`.

    **2. Add `id` attributes to stat value spans:**
    - Miles stat value: add `id="stat-miles"` to the `<span class="stat-value">` containing `{Math.round(totalMiles)}`
    - Elevation stat value: add `id="stat-elevation"` to the `<span class="stat-value">` containing `{elevationGainFeet.toLocaleString()}`

    **3. Add a third stat card for sector count:**
    After the elevation stat card, add:
    ```html
    <div class="stat-card">
      <span class="stat-value" id="stat-sectors">7</span>
      <span class="stat-label">Sectors</span>
    </div>
    ```
    The initial value of 7 matches the 100mi route's sector count (100mi has 7 sectors in routes.json). The grid already uses `repeat(3, 1fr)` at sm breakpoint, so 3 cards will fill naturally.

    **4. Add a `<script>` block for runtime route:change updates:**
    Place this after the closing `</style>` tag:
    ```html
    <script>
      let _routesCache = null;
      async function getRoutesData() {
        if (!_routesCache) _routesCache = fetch('/data/routes.json').then(r => r.json());
        return _routesCache;
      }

      window.addEventListener('route:change', async (e) => {
        const data = await getRoutesData();
        const route = data.routes.find(r => r.id === e.detail.routeId);
        if (!route) return;
        const milesEl = document.getElementById('stat-miles');
        const elevEl = document.getElementById('stat-elevation');
        const secEl = document.getElementById('stat-sectors');
        if (milesEl) milesEl.textContent = Math.round(route.totalMiles);
        if (elevEl) elevEl.textContent = route.elevationGainFeet.toLocaleString();
        if (secEl) secEl.textContent = route.sectorIds.length;
      });
    </script>
    ```

    **Important:** The `_routesCache` pattern (module-scope cached promise) is the established codebase pattern from ElevationProfile.astro. The fetch only fires once even if `route:change` fires many times. `routes.json` is ~400 bytes and already cached by the browser after RouteMap fetches it.

    Do NOT change the Astro frontmatter (`getEntry` import and usage). It still provides the correct 100mi initial values at build time. The script only updates values at runtime.
  </action>
  <verify>
    1. Run `npm run build` — must succeed with no errors
    2. Verify HTML structure: `grep -c 'stat-card' src/components/RouteStats.astro` should return 3
    3. Verify IDs exist: `grep 'id="stat-' src/components/RouteStats.astro` should show stat-miles, stat-elevation, stat-sectors
    4. Verify script block: `grep 'route:change' src/components/RouteStats.astro` should show the listener
    5. Count `</div>` tags in the stats-grid section — should be exactly 4 (3 stat-cards + 1 stats-grid)
  </verify>
  <done>
    RouteStats.astro renders 3 stat cards (miles, elevation, sectors) with `id` attributes. A `<script>` block listens for `route:change` events and updates all 3 stat values from the cached routes.json manifest. The spurious extra `</div>` is removed.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -n '__activeRouteId' src/components/RouteMap.astro src/components/ElevationProfile.astro` shows both the setter and the reader
3. `grep -c 'stat-card' src/components/RouteStats.astro` returns 3
4. `grep 'route:change' src/components/RouteStats.astro` shows the listener registration
5. The HTML in RouteStats.astro has correct nesting (no spurious `</div>`)
</verification>

<success_criteria>
- ELEV-01, ELEV-02, ELEV-03 confirmed working (Phase 34 implemented; lazy-init race now fixed)
- STAT-01 satisfied: Route stats update on route:change with correct miles, elevation, and sector count
- RouteStats shows 3 stat cards (miles, elevation gain, sectors)
- No build errors introduced
</success_criteria>

<output>
After completion, create `.planning/phases/35-elevation-profile-and-route-stats/35-01-SUMMARY.md`
</output>
