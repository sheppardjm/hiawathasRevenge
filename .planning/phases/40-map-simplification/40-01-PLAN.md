---
phase: 40-map-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/components/RouteMap.astro]
autonomous: true

must_haves:
  truths:
    - "Route track displays exactly two colors — forest900 for road, amber500 for gravel sectors"
    - "No surface-type multi-color polyline or color key appears on the map"
    - "Two-color scheme applies consistently across all three routes (100mi, 100k, 50k)"
    - "Ghost polylines for inactive routes remain visible at 0.2 opacity"
  artifacts:
    - path: "src/components/RouteMap.astro"
      provides: "Simplified two-color route rendering"
      contains: "L.polyline(latlngs"
  key_links:
    - from: "renderRoute()"
      to: "L.polyline single-color base"
      via: "activeRouteGroup.addLayer"
      pattern: "activeRouteGroup\\.addLayer\\(L\\.polyline\\(latlngs"
    - from: "sector overlay step 5"
      to: "amber500 visible polyline"
      via: "SECTOR_COLOR.line"
      pattern: "color: SECTOR_COLOR\\.line"
---

<objective>
Replace the four-color surface-type polyline with a single-color road base, producing a clean two-color map (forest900 road + amber500 gravel sectors).

Purpose: Visitors see a readable map with gravel sectors visually distinct from other road — no confusing multi-color surface legend.
Output: Modified RouteMap.astro with simplified polyline rendering and one fewer network fetch per route switch.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/components/RouteMap.astro
@.planning/phases/40-map-simplification/40-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove surface-color infrastructure</name>
  <files>src/components/RouteMap.astro</files>
  <action>
    In `src/components/RouteMap.astro`, make these three deletions:

    1. **Delete `SURFACE_COLORS` constant** (lines 312-318):
       Remove the entire `SURFACE_COLORS` object. Do NOT remove `lake400`, `amber400`, `amber500`, or `forest900` variable declarations — they are used elsewhere (restock markers, bike icon, sector overlays).

    2. **Delete `drawSurfacePolyline()` function** (lines 552-578):
       Remove the entire function including the comment header on line 552 and the function body through line 578. This removes the run-flush algorithm entirely.

    3. **Remove `surface-points.json` fetch from `renderRoute()`** (lines 583-588):
       Change the Promise.all from 4 fetches to 3 fetches:
       - Remove the `fetch(\`/data/${routeId}/surface-points.json\`)` line
       - Update destructuring from `[routeData, annotations, sectorElevations, surfacePoints]` to `[routeData, annotations, sectorElevations]`

    4. **Replace `drawSurfacePolyline` call with single polyline** (line 608-609):
       Replace:
       ```javascript
       // 4. Draw surface-colored polyline (run-flush algorithm)
       drawSurfacePolyline(latlngs, surfacePoints, activeRouteGroup);
       ```
       With:
       ```javascript
       // 4. Draw route base polyline (road color — sector overlays in step 5 supply gravel color)
       activeRouteGroup.addLayer(L.polyline(latlngs, {
         color: forest900,
         weight: 4,
         opacity: 0.9,
         smoothFactor: 1,
         interactive: false,
       }));
       ```

    **Important ordering notes:**
    - The base polyline MUST be added in step 4, BEFORE sector overlays in step 5 — this ensures sector overlays (amber500) render on top of the road base (forest900).
    - Do NOT change sector overlay logic in step 5 — `SECTOR_COLOR.line` (amber500) already provides the gravel-sector color.
    - Do NOT change ghost polyline logic — `route.color` at opacity 0.2 is correct per success criterion 4.
  </action>
  <verify>
    1. Run `grep -n "SURFACE_COLORS" src/components/RouteMap.astro` — should return zero matches
    2. Run `grep -n "drawSurfacePolyline" src/components/RouteMap.astro` — should return zero matches
    3. Run `grep -n "surface-points.json" src/components/RouteMap.astro` — should return zero matches
    4. Run `grep -n "forest900" src/components/RouteMap.astro` — should find the polyline color usage
    5. Run `grep -n "SECTOR_COLOR.line" src/components/RouteMap.astro` — should still exist (unchanged sector overlays)
    6. Build the project: `/Users/Sheppardjm/.volta/bin/node node_modules/.bin/astro build` — should succeed with no errors
  </verify>
  <done>
    RouteMap.astro renders the route as a single forest900 polyline base with amber500 sector overlays on top. No surface-points.json fetch occurs. No SURFACE_COLORS constant exists. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Visual verification across all routes</name>
  <files>src/components/RouteMap.astro</files>
  <action>
    Start the dev server and verify the two-color rendering works correctly for all three routes:

    1. Start dev server: `/Users/Sheppardjm/.volta/bin/node node_modules/.bin/astro dev`
    2. Open http://localhost:4321 in a browser
    3. Verify 100mi route:
       - Route base track is dark green (forest900)
       - Gravel sectors display in amber (amber500) on top
       - Sector click/hover still works (ghost polys intact)
    4. Switch to 100k route via route selector:
       - Same two-color rendering
       - Ghost polyline for 100mi appears at low opacity
    5. Switch to 50k route:
       - Same two-color rendering
       - Ghost polylines for 100mi and 100k visible

    6. Check browser console — no errors about undefined surfacePoints or failed fetches
    7. Check network tab — no requests to surface-points.json on route switch

    If any visual issues found (sector overlays hidden behind base, z-order problems), the fix is to ensure the base polyline `addLayer` call stays BEFORE the sector overlay loop in `renderRoute()`.
  </action>
  <verify>
    1. Dev server starts without errors
    2. Browser console shows no JavaScript errors on any route
    3. Network tab shows no surface-points.json requests
    4. All three routes display forest900 base + amber500 sectors
  </verify>
  <done>
    All three routes (100mi, 100k, 50k) display a clean two-color map with forest900 road base and amber500 gravel sectors. Ghost polylines remain visible. No console errors. No unnecessary network fetches.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "SURFACE_COLORS\|drawSurfacePolyline\|surface-points.json" src/components/RouteMap.astro` returns 0
2. `grep "SECTOR_COLOR.line" src/components/RouteMap.astro` returns matches (sector overlays unchanged)
3. `grep "forest900" src/components/RouteMap.astro` shows base polyline usage
4. Astro build succeeds with no errors
5. Dev server renders all three routes with exactly two track colors
</verification>

<success_criteria>
- Route track displays exactly two colors: forest900 (road) and amber500 (gravel sectors)
- No surface-type legend or multi-color polyline appears on the map
- Two-color scheme works consistently across 100mi, 100k, and 50k routes
- Ghost polylines remain visible and visually subordinate
- No surface-points.json network requests on route switch
- Build succeeds, no console errors
</success_criteria>

<output>
After completion, create `.planning/phases/40-map-simplification/40-01-SUMMARY.md`
</output>
