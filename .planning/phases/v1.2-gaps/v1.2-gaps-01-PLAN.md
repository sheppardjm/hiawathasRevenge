---
phase: v1.2-gaps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FloralDivider.astro
  - src/components/AnimatedDivider.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "FloralDivider displays bold, filled geometric shapes in a repeating pattern band -- not hairline strokes"
    - "AnimatedDivider three variants use geometric motifs that draw-on via stroke-dashoffset animation"
    - "All divider patterns are bilaterally symmetric and read as intentional graphic design"
    - "Reduced motion shows static fully-visible divider with no animation"
  artifacts:
    - path: "src/components/FloralDivider.astro"
      provides: "Static geometric pattern divider"
      contains: "polygon\\|rect\\|path.*fill"
    - path: "src/components/AnimatedDivider.astro"
      provides: "Three animated geometric divider variants"
      contains: "pathLength|polygon"
  key_links:
    - from: "src/components/AnimatedDivider.astro"
      to: "IntersectionObserver"
      via: "is-visible class toggle"
      pattern: "querySelectorAll.*animated-divider"
---

<objective>
Replace the hairline floral vine dividers with bold geometric patterns inspired by Great Lakes Indigenous textile/beadwork traditions and National Park graphic design.

Purpose: UAT Tests 1, 5, 6 all failed because the thin organic strokes look "amateur and tacky." The user specifically requested Ojibwe/Iroquois geometric textile patterns with bold filled shapes, chevrons, arrows, and bilateral symmetry. The animation infrastructure (IntersectionObserver, stroke-dashoffset, prefers-reduced-motion) in AnimatedDivider is sound and must be preserved -- only the SVG content changes.

Output: Two rewritten components with high-quality geometric SVG dividers that match the bold, confident aesthetic of the inspiration images at images/inspiration/ (National Park badges, arrowhead motifs, bold geometric forms, earth-tone fills).
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/FloralDivider.astro
@src/components/AnimatedDivider.astro
@src/pages/index.astro
@src/styles/global.css

Inspiration images directory: images/inspiration/
Key reference images showing target aesthetic:
- Arrowhead/topo patterns: original-21cf144750c04b7d07af135578e70983.webp (arrowhead shields with topo-line fills)
- Bold geometric marks: original-0ac1226989a12b196feeb9b3f9f6b47e.webp (geometric arrow/feather in solid fill)
- AZ geometric icons: original-f6e89013406c62872ce58faf0385b7d5.webp (sunburst radiating lines, waves, geometric shapes in 2 colors)
- National Park badges: original-767cdd8de8acd99a9e4d17166b5b3007.webp (Yosemite badge with bold strokes, earth tones)
- Line art style: original-116171370441ce4fcd033d6070c3fdf2.webp (clean bold outline illustrations)
- Tribal geometric: original-0224278e4cf61770e3df248f5cd1f4bb.webp (geometric borders, chevrons, crosses)

Color tokens available (from global.css):
- gold-500 (#d4a017), gold-400 (#e6b422) -- primary divider color
- amber-500 (#c8973e) -- warm accent
- turquoise-500 (#14b8a6), turquoise-400 (#2dd4bf) -- cool accent
- scarlet-400 (#f87171) -- warm pop (decorative safe)
- forest-700 (#3d6b3d) -- medium green for subtle elements
- cream-100 (#f5f0e8) -- light accent on dark bg

CRITICAL CONSTRAINTS:
- AnimatedDivider: KEEP the <style> block's .vine-path class, stroke-dasharray/dashoffset animation, .is-visible toggle, @keyframes, and @media (prefers-reduced-motion) block. KEEP the <script> block's IntersectionObserver entirely unchanged. Only replace SVG element content.
- AnimatedDivider: KEEP the Props interface and variant prop ('floral' | 'minimal' | 'berry').
- FloralDivider: Complete rewrite of SVG. Keep the outer div structure, aria-hidden, role="presentation".
- Both: Maintain max-width container (max-w-4xl on Floral, max-width: 56rem on Animated).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign FloralDivider with geometric Great Lakes pattern band</name>
  <files>src/components/FloralDivider.astro</files>
  <action>
  Replace the entire SVG content in FloralDivider.astro with a bold geometric pattern band. The outer div wrapper stays the same (class, aria-hidden, role).

  DESIGN DIRECTION -- inspired by the reference images, create a horizontal band pattern using these principles:
  1. BILATERAL SYMMETRY: Mirror left-right from center point. The pattern should read the same forwards and backwards.
  2. BOLD FILLED SHAPES: Use polygon, rect, and path elements with SOLID fills at FULL opacity. No hairline strokes. Minimum stroke-width of 2.5px where strokes are used.
  3. GEOMETRIC VOCABULARY: Chevrons (V-shapes), arrows/arrowheads, nested diamonds, horizontal lines, and small filled circles as punctuation. These are all present in Great Lakes beadwork and the inspiration images.
  4. 2-3 COLOR PALETTE per divider: gold-500 as primary, with amber-500 or turquoise-500 as secondary. Do NOT use more than 3 colors. The inspiration images show restrained palettes.
  5. REPEATING UNITS: Create a single geometric unit (e.g., arrowhead flanked by chevron lines) and repeat it 3-5 times across the width with consistent spacing.
  6. HEIGHT: Increase viewBox height to 80px (from 60px) to give the pattern room to breathe. Set height="80" on the SVG.

  SPECIFIC PATTERN for FloralDivider (static, no animation):
  - ViewBox: "0 0 800 80"
  - Center element: A diamond/arrowhead shape (filled polygon) in gold-500, approximately 40px tall
  - Flanking: Nested chevron lines (pointing outward from center) in amber-500, stroke-width 3px
  - Between chevrons: Small filled circles (r=4) in gold-400 as rhythm markers
  - Outer edges: Horizontal parallel lines (3 lines, 2px apart) in forest-700 running to the SVG edges, acting as "rails" that ground the pattern
  - All fills at opacity 1.0. No semi-transparent elements.

  QUALITY CHECK: Before writing, mentally render the SVG. Every element should be visible at arm's length on a phone screen. If an element would disappear at small sizes, make it bigger or remove it.
  </action>
  <verify>
  Run `npx astro build` (using /Users/Sheppardjm/.volta/bin/npx) and confirm zero errors. Open the built HTML and search for the FloralDivider SVG -- confirm it contains polygon/rect elements with solid fills, no elements with opacity below 0.8.
  </verify>
  <done>
  FloralDivider renders a bold geometric pattern band with filled shapes, bilateral symmetry, 2-3 color palette from project tokens, no hairline strokes, and full opacity fills. The pattern reads as intentional graphic design, not wispy decoration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Redesign AnimatedDivider three variants with geometric patterns preserving animation infrastructure</name>
  <files>src/components/AnimatedDivider.astro</files>
  <action>
  Replace all SVG content within each variant's conditional block while preserving:
  - The Props interface and variant prop
  - The entire `<style>` block (vine-path animation, is-visible toggle, reduced-motion media query)
  - The entire `<script>` block (IntersectionObserver)
  - The outer div.animated-divider wrapper

  REMOVE the blossom-cycle and berry-cycle @keyframes and classes from the style block -- these animated the old floral elements which are being removed. Keep cycle-blossom/cycle-berry definitions for backward compat but they can be removed if nothing references them. Actually -- fully remove them. The new patterns will use the stroke-dashoffset draw-on animation exclusively.

  DESIGN for each variant -- all three should share the geometric vocabulary but differ in density/complexity:

  **Variant 'floral' (richest, used between major sections):**
  - ViewBox: "0 0 800 80", height="80"
  - A horizontal band with a center arrowhead motif flanked by repeating chevron-arrow sequences
  - Main animating path: A continuous zigzag/chevron polyline running the full 800px width. This gets class="vine-path" and pathLength="1" for the draw-on animation. Use stroke-width="3", stroke in gold-500.
  - Static decorative elements (NO vine-path class): Filled arrowhead polygons at x=200, x=400, x=600. Small filled diamond shapes between them. All in amber-500 and turquoise-500 fills.
  - Horizontal rail lines at y=10 and y=70 (2px, forest-700) grounding the band -- these are static.

  **Variant 'minimal' (lightest, minor section breaks):**
  - ViewBox: "0 0 800 40", height="40"
  - A single chevron line running center 400px of the width. class="vine-path", pathLength="1", stroke-width="3", stroke in gold-500.
  - Two small filled diamonds flanking center (static, no vine-path). Fill: amber-500.
  - Horizontal hairline rules at y=5 and y=35 in forest-700, full width, static.

  **Variant 'berry' (medium complexity):**
  - ViewBox: "0 0 800 60", height="60"
  - Animating path: A stepped/terraced zigzag line (like a topographic contour stepped pattern) running full width. class="vine-path", pathLength="1", stroke-width="2.5", stroke in turquoise-500.
  - Static elements: Filled arrow/chevron shapes at x=200, x=400, x=600 in gold-500. Small filled circles (r=3) at peaks of the zigzag in scarlet-400.
  - Horizontal rails at y=5 and y=55, forest-700, static.

  For ALL variants:
  - Every element that should animate on scroll gets class="vine-path" and pathLength="1"
  - Every static decorative element gets NO vine-path class
  - All fills at full opacity (1.0)
  - Min stroke-width 2.5px for any visible stroke
  - Bilateral symmetry: mirror the pattern around x=400

  STYLE BLOCK CHANGES:
  - Remove the @keyframes cycle-blossom and cycle-berry blocks
  - Remove .blossom-cycle and .berry-cycle rules
  - Keep everything else in the style block exactly as-is
  </action>
  <verify>
  Run `npx astro build` and confirm zero errors. Verify in built output that:
  1. Each variant SVG has at least one element with class="vine-path" and pathLength="1"
  2. The IntersectionObserver script block is unchanged
  3. The prefers-reduced-motion media query still sets stroke-dashoffset: 0 and transition: none
  4. No elements reference blossom-cycle or berry-cycle classes
  </verify>
  <done>
  AnimatedDivider renders three geometric pattern variants that draw-on via stroke-dashoffset when scrolled into view. All three use bold filled shapes and geometric motifs. The draw-on animation, IntersectionObserver trigger, and reduced-motion fallback work exactly as before. No floral or organic vine elements remain.
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` passes with zero errors
2. Open dev server (`npx astro dev`) and scroll the page -- AnimatedDividers draw-on when entering viewport
3. All divider SVGs contain polygon/rect/path elements with solid fills at full opacity
4. No SVG elements with stroke-width below 2px (except intentional rail lines)
5. Bilateral symmetry: elements at x=200 have mirrors at x=600, elements at x=150 have mirrors at x=650
6. macOS Reduce Motion enabled: all dividers render static, no animation
</verification>

<success_criteria>
- Both divider components rewritten with bold geometric patterns
- AnimatedDivider draw-on animation preserved and functional
- No hairline strokes (min 2.5px for visible elements)
- No semi-transparent fills (min opacity 0.8 for visible elements)
- Reduced motion preference respected
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-gaps/v1.2-gaps-01-SUMMARY.md`
</output>
