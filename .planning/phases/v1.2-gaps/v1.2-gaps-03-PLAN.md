---
phase: v1.2-gaps
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteExplainer.astro
  - src/components/ElevationSparkline.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Each segment card has a large photo header that dominates the card layout"
    - "Segment cards have visible backgrounds, shadows, and padding creating a card container feel"
    - "Elevation sparklines have a visible background rect, horizontal grid lines, and are at least 50px tall"
    - "Sparklines render at full opacity, not faded out"
    - "Segment 520 (0 photos) renders gracefully with a fallback treatment"
  artifacts:
    - path: "src/components/RouteExplainer.astro"
      provides: "Magazine-style segment cards with photo-dominant layout"
      contains: "segment-card-container"
    - path: "src/components/ElevationSparkline.astro"
      provides: "Enhanced sparkline with background and grid lines"
      contains: "rect"
  key_links:
    - from: "src/components/RouteExplainer.astro"
      to: "src/components/ElevationSparkline.astro"
      via: "component import and render"
      pattern: "ElevationSparkline"
    - from: "src/components/RouteExplainer.astro"
      to: "photos.json"
      via: "import and filter"
      pattern: "photosData"
---

<objective>
Redesign segment cards from thin 2-column text-photo strips into photo-dominant magazine cards, and enhance elevation sparklines from invisible 30px lines into clear mini-charts with backgrounds and grid lines.

Purpose: UAT Test 10 failed because the cards look flat with tiny thumbnail strips (min(280px, 35%) photo column). Test 8 failed because sparklines are 30px tall at 60% opacity with no background. The user wants "a magazine layout" where "photo dominates" with "a big background image and content card image."

Output: RouteExplainer with photo-hero segment cards and ElevationSparkline with background rect and grid lines.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@src/components/RouteExplainer.astro
@src/components/ElevationSparkline.astro
@src/styles/global.css

Photo data: 51 photos across 6/7 segments. Segment 520 has 0 photos. Photos are 1536x2048 portrait WebPs available as thumbs at /thumbs/*.webp. Each photo has: thumb, mile, lat, lon.

Current segment photo allocation (slice(0, 2)):
- 520: 0 photos (needs fallback)
- NF2266: 8 available
- Bass Lake Rd: 9 available
- NF2217-2218: 12 available
- ND2225: 9 available
- Doe Lake: 6 available
- Rapid River Truck Trail: 7 available

Color tokens:
- Card bg: forest-800 (#2d4a2d) for card container
- Card border: forest-700 (#3d6b3d)
- Sparkline bg: forest-800 with some transparency
- Grid lines: forest-700

Scroll reveal animation: segment cards use nth-child stagger delays and opacity/transform transition. The .route-explainer-section.is-visible .segment-card rule reveals them. This animation system MUST be preserved.

Existing data model: SEGMENTS array with name, startMi, endMi, distFromStart, length, difficulty, description, stravaId. SECTOR_IDS maps segment names to content collection IDs for sparklines. DIFFICULTY_COLORS maps difficulty number to Tailwind text color class. All of this data stays unchanged.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign segment cards with photo-hero layout and card containers</name>
  <files>src/components/RouteExplainer.astro</files>
  <action>
  Redesign the segment card layout from a side-by-side photo-text grid into a photo-dominant card layout. Keep all existing JavaScript/data logic (SEGMENTS, DIFFICULTY_COLORS, SECTOR_IDS, segmentsWithPhotos mapping, imports). Keep the outer section element with data-reveal. Keep the scroll animation CSS (opacity, transform, transition-delay nth-child rules, is-visible rules, reduced-motion rules).

  CARD STRUCTURE CHANGE:
  Replace the current 2-column grid layout with a stacked card layout:

  ```html
  <article class="segment-card-container">
    {seg.photos.length > 0 ? (
      <div class="segment-hero">
        <img src={seg.photos[0].thumb} alt="..." loading="lazy" decoding="async" />
        <div class="segment-hero-overlay">
          <div class="flex items-center gap-2">
            <ShieldMotif ... />
            <h3 ...>{seg.name}</h3>
          </div>
        </div>
      </div>
    ) : (
      <div class="segment-hero segment-hero-fallback">
        <div class="segment-hero-overlay">
          <div class="flex items-center gap-2">
            <ShieldMotif ... />
            <h3 ...>{seg.name}</h3>
          </div>
        </div>
      </div>
    )}
    <div class="segment-body">
      <!-- metadata, strava link, description, sparkline -->
    </div>
  </article>
  ```

  PHOTO HERO SECTION:
  ```css
  .segment-hero {
    position: relative;
    width: 100%;
    height: 220px;
    overflow: hidden;
    border-radius: 0.5rem 0.5rem 0 0;
  }
  .segment-hero img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }
  .segment-hero-overlay {
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    padding: 1rem 1.25rem;
    background: linear-gradient(transparent, rgba(13, 26, 13, 0.85));
  }
  .segment-hero-fallback {
    background: linear-gradient(135deg, var(--color-forest-800), var(--color-forest-700));
    display: flex;
    align-items: flex-end;
  }
  ```

  The heading (h3 with shield motif) moves INTO the hero overlay, appearing over the photo with a gradient scrim for legibility. For segment 520 (no photo), the fallback shows a forest gradient background with the heading at the bottom.

  CARD CONTAINER:
  ```css
  .segment-card-container {
    background: var(--color-forest-800);
    border: 1px solid var(--color-forest-700);
    border-radius: 0.5rem;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.3);
    margin-bottom: 2rem;
    overflow: hidden;
  }
  ```

  CARD BODY:
  ```css
  .segment-body {
    padding: 1.25rem;
  }
  ```

  The segment body contains: metadata line (distFromStart, length, star rating), strava link, description paragraph, and sparkline -- arranged vertically. This is similar to current .segment-content but now inside a visible card.

  STAGGER ANIMATION:
  Update the stagger CSS selectors from .segment-card to .segment-card-container:
  ```css
  .segment-card-container {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.5s cubic-bezier(0.4, 0, 0.2, 1),
                transform 0.5s cubic-bezier(0.4, 0, 0.2, 1);
  }
  .segment-card-container:nth-child(1) { transition-delay: 0ms; }
  /* ... etc through :nth-child(7) at 600ms */
  .route-explainer-section.is-visible .segment-card-container {
    opacity: 1;
    transform: translateY(0);
  }
  @media (prefers-reduced-motion: reduce) {
    .segment-card-container {
      opacity: 1;
      transform: none;
      transition: none;
      transition-delay: 0ms;
    }
  }
  ```

  SPARKLINE CALL: Remove the `class="opacity-60"` from the ElevationSparkline usage. Change to just:
  ```
  <ElevationSparkline sectorId={SECTOR_IDS[seg.name]} class="mt-3" />
  ```

  REMOVE the old .segment-card, .segment-photo, .segment-content, .no-photo CSS rules and the @media (min-width: 768px) grid rules that controlled the old 2-column layout. These are fully replaced by the new card layout.

  KEEP the star-rating CSS, strava-link CSS, and the route-explainer-section background-image/border styles.

  RESPONSIVE: The new card layout is mobile-first and works at all widths -- photo hero on top, content below. No breakpoint needed for the card itself. The photo hero height could shrink slightly on mobile:
  ```css
  @media (max-width: 640px) {
    .segment-hero { height: 180px; }
  }
  ```
  </action>
  <verify>
  Run `npx astro build` -- zero errors. In dev server:
  1. Each segment card has a photo hero image spanning full card width with heading overlaid
  2. Segment 520 shows gradient fallback (no broken image)
  3. Cards have visible forest-800 background, border, shadow
  4. Scroll animation: cards stagger-reveal when section enters viewport
  5. Reduce Motion: cards visible immediately, no animation
  </verify>
  <done>
  Segment cards display as photo-dominant magazine-style cards with hero images, gradient overlays, visible card containers with backgrounds and shadows, and graceful fallback for photoless segments. Stagger animation preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhance ElevationSparkline with background, grid lines, and increased prominence</name>
  <files>src/components/ElevationSparkline.astro</files>
  <action>
  Enhance the sparkline SVG to be more visually prominent.

  CHANGES:

  1. INCREASE ViewBox height and rendered height:
     - ViewBox: "0 0 100 50" (was "0 0 100 30")
     - Keep height prop responsive but set minimum: height="50" (was "30")
     - Update the H constant in the computation: `const W = 100, H = 50;` (was H = 30)

  2. ADD background rect as first child of SVG:
     ```html
     <rect x="0" y="0" width="100" height="50" rx="3" fill="var(--color-forest-900)" opacity="0.6" />
     ```
     This creates a subtle dark background that separates the sparkline from the card.

  3. ADD horizontal grid lines at 25%, 50%, 75% heights:
     ```html
     <line x1="0" y1="12.5" x2="100" y2="12.5" stroke="var(--color-forest-700)" stroke-width="0.5" opacity="0.4" />
     <line x1="0" y1="25" x2="100" y2="25" stroke="var(--color-forest-700)" stroke-width="0.5" opacity="0.4" />
     <line x1="0" y1="37.5" x2="100" y2="37.5" stroke="var(--color-forest-700)" stroke-width="0.5" opacity="0.4" />
     ```
     These go AFTER the background rect but BEFORE the polyline, so the elevation line draws over the grid.

  4. INCREASE stroke-width on the polyline from 1.5 to 2.5:
     ```html
     <polyline ... stroke-width="2.5" ... />
     ```

  5. ADD a subtle fill gradient under the elevation line to create an area chart effect:
     Add a defs block with a linear gradient:
     ```html
     <defs>
       <linearGradient id={`sparkfill-${sectorId}`} x1="0" y1="0" x2="0" y2="1">
         <stop offset="0%" stop-color={strokeColor} stop-opacity="0.3" />
         <stop offset="100%" stop-color={strokeColor} stop-opacity="0.05" />
       </linearGradient>
     </defs>
     ```
     Then add a filled polygon using the same points but closing to the bottom:
     ```html
     <polygon
       points={`0,50 ${polylinePoints} 100,50`}
       fill={`url(#sparkfill-${sectorId})`}
     />
     ```
     This goes AFTER grid lines but BEFORE the polyline stroke.

  6. ADD small circle markers at the start and end points of the polyline:
     Parse the first and last point from polylinePoints to get coordinates, then:
     ```html
     <circle cx={startX} cy={startY} r="2" fill={strokeColor} />
     <circle cx={endX} cy={endY} r="2" fill={strokeColor} />
     ```

  7. Keep: aria-hidden, role="presentation", focusable="false", the class prop passthrough, the DIFFICULTY_COLORS mapping, the content collection lookup.

  COMPUTATION UPDATE:
  The point computation must use H = 50. Add a small padding so the polyline doesn't touch the very top and bottom of the SVG:
  ```js
  const padding = 4;
  const y = eleRange > 0
    ? padding + (H - 2 * padding) - ((p.ele - eleMin) / eleRange * (H - 2 * padding))
    : H / 2;
  ```

  Extract first/last points for the circle markers:
  ```js
  const pointPairs = polylinePoints.split(' ').map(p => p.split(',').map(Number));
  const firstPt = pointPairs[0];
  const lastPt = pointPairs[pointPairs.length - 1];
  ```
  </action>
  <verify>
  Run `npx astro build` -- zero errors. In dev server, inspect segment cards:
  1. Sparklines are visibly taller (~50px) with a dark background rect
  2. Three horizontal grid lines visible behind the elevation curve
  3. Elevation curve has thicker stroke (2.5px) and a subtle gradient fill underneath
  4. Start and end points marked with small circles
  5. Difficulty color coding still works (green for easy, gold for moderate, red for hard)
  6. Sparkline is NOT faded (no opacity-60 -- that was removed in Task 1's RouteExplainer change)
  </verify>
  <done>
  Elevation sparklines are visually prominent mini-charts at 50px height with dark background, horizontal grid lines, gradient area fill, thicker stroke, and endpoint markers. They stand out clearly within segment cards at full opacity.
  </done>
</task>

</tasks>

<verification>
1. `npx astro build` passes cleanly
2. All 7 segment cards render with photo-hero layout
3. Segment 520 renders with gradient fallback (no broken image or layout collapse)
4. Cards have visible backgrounds (forest-800), borders, and shadows
5. Sparklines in 6 segments have background rect, grid lines, area fill, thick stroke
6. Sparklines render at full opacity (no opacity-60 class)
7. Scroll stagger animation still works
8. Reduced motion: all cards and sparklines visible immediately
</verification>

<success_criteria>
- Segment cards have photo-hero layout with gradient overlay heading
- Card containers have visible bg, border, shadow
- Segment 520 fallback renders cleanly
- Sparklines 50px tall with background, grid lines, area fill
- No opacity-60 on sparklines
- Stagger animation preserved
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-gaps/v1.2-gaps-03-SUMMARY.md`
</output>
