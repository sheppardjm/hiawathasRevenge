---
phase: 00-full-site
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteStats.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Route stats surface breakdown shows correct surface types for the route (no singletrack)"
    - "Surface type labels match actual terrain: FS roads, scenic roads, rugged truck trail"
  artifacts:
    - path: "src/components/RouteStats.astro"
      provides: "Correct surface type labels and count"
      contains: "FS Roads"
  key_links:
    - from: "src/components/RouteStats.astro"
      to: "public/data/annotations.json"
      via: "difficulty field maps to surface labels"
      pattern: "difficulty.*moderate|easy|hard"
---

<objective>
Replace incorrect "singletrack" surface type labels with actual route surface descriptions.

Purpose: The route has zero singletrack. The existing difficulty categories (easy/moderate/hard) from annotations.json map to gravel road sectors with distinct characters, not singletrack. Labels must reflect actual terrain.
Output: RouteStats showing correct surface types: Forest Service Roads, Scenic County Roads, Rugged Truck Trail, plus the unlabeled road segments.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-full-site/00-UAT.md
@src/components/RouteStats.astro
@public/data/annotations.json

Sector data from annotations.json (7 gravel sectors):
- difficulty "moderate" sectors: 520, NF2266, NF2217, ND2225 — these are numbered National Forest roads (ORV trails / regular gravel)
- difficulty "easy" sectors: Bass Lake Rd, Doe Lake — these are scenic county/forest roads (pristine FS roads)
- difficulty "hard" sector: Rapid River Truck Trail — rugged two-track with sand sections

The ~72.8 mi of unlabeled road (roadMiles variable) is pavement and regular Forest Service roads.

User-provided surface types: "ORV trails, pristine Forest Service roads, rugged two-track, regular gravel, pavement, sand"

Mapping decision (from user guidance):
- "easy" (Bass Lake Rd, Doe Lake) -> "Scenic Roads" — pristine paved/graded forest roads
- "moderate" (520, NF2266, NF2217, ND2225) -> "FS Gravel Roads" — numbered National Forest roads, typical gravel
- "hard" (Rapid River Truck Trail) -> "Rugged Two-Track" — rough, sandy truck trail
- unlabeled road miles -> "Pavement & Forest Roads" — the remaining ~72.8 mi of mixed surface

Also update "4 Surface Types" to match the actual count of surface categories displayed (still 4 categories).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename surface type labels from singletrack to actual surfaces</name>
  <files>src/components/RouteStats.astro</files>
  <action>
In the surface-breakdown section (lines 36-57), replace the four surface type labels:

1. Line 39: "Forest Roads" -> "Pavement & Forest Roads" (these are the ~72.8 mi of unlabeled road)
2. Line 44: "Moderate Singletrack" -> "FS Gravel Roads" (NF-numbered gravel roads)
3. Line 49: "Easy Singletrack" -> "Scenic Roads" (Bass Lake Rd, Doe Lake — pristine graded roads)
4. Line 54: "Hard Singletrack" -> "Rugged Two-Track" (Rapid River Truck Trail)

Keep all colors unchanged — they already match the map sector overlay colors:
- forest-600 for pavement/forest roads (unlabeled)
- amber-300 for moderate (FS gravel)
- amber-500 for easy (scenic roads)
- rust-500 for hard (rugged two-track)

Keep the mileage calculations unchanged — the difficulty categories and math are correct; only the display labels are wrong.

Keep "4 Surface Types" in the stat card (line 33) — still 4 categories.
  </action>
  <verify>
1. Read the modified file and confirm:
   - No occurrence of the word "Singletrack" anywhere in the file
   - Labels read: "Pavement & Forest Roads", "FS Gravel Roads", "Scenic Roads", "Rugged Two-Track"
   - All color variables unchanged
   - Mileage calculation code unchanged
2. Run `PATH="/usr/local/opt/node/bin:$PATH" npx astro build` to confirm no build errors
  </verify>
  <done>Route stats surface breakdown shows "Pavement & Forest Roads", "FS Gravel Roads", "Scenic Roads", and "Rugged Two-Track" — no mention of singletrack.</done>
</task>

</tasks>

<verification>
1. `PATH="/usr/local/opt/node/bin:$PATH" npx astro build` completes without errors
2. No instance of "Singletrack" or "singletrack" in RouteStats.astro
3. Four surface labels: "Pavement & Forest Roads", "FS Gravel Roads", "Scenic Roads", "Rugged Two-Track"
4. Colors and mileage calculations unchanged
</verification>

<success_criteria>
- Route stats surface breakdown accurately describes the actual terrain types on the Hiawatha's Revenge route
- No reference to singletrack anywhere in the component
- Mileage per surface type is unchanged (same math, different labels)
</success_criteria>

<output>
After completion, create `.planning/phases/00-full-site/00-03-SUMMARY.md`
</output>
