---
phase: 00-full-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/PhotoGallery.astro
  - src/components/RouteMap.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Photo gallery thumbnails load successfully (no 404s)"
    - "Restock markers are visually distinct from photo markers on the map"
  artifacts:
    - path: "src/components/PhotoGallery.astro"
      provides: "Correct thumbnail src path"
      contains: "photo.thumb"
    - path: "src/components/RouteMap.astro"
      provides: "Water drop SVG icon for restock markers"
      contains: "water drop"
  key_links:
    - from: "src/components/PhotoGallery.astro"
      to: "public/data/photos.json"
      via: "photo.thumb field used directly as src"
      pattern: "src=.*photo\\.thumb"
---

<objective>
Fix photo gallery 404s and restyle restock markers with a water drop icon.

Purpose: Unblock photo gallery (blocker) and differentiate restock markers from photo markers (minor UX).
Output: Working photo thumbnails and visually distinct restock markers on the map.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-full-site/00-UAT.md

Key facts:
- photos.json `thumb` field already contains the full path: `/thumbs/filename.webp`
- PhotoGallery.astro line 43 currently does `src={`/thumbs/${photo.thumb}`}` which doubles the prefix to `/thumbs//thumbs/filename.webp`
- Restock markers (#c8973e) and photo markers (#d4a84e) are nearly identical 14px amber circles
- User wants a water drop icon for restock markers
- Restock marker code is in RouteMap.astro lines ~170-178 (the restockIcon L.divIcon)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix photo thumbnail double-prefix 404</name>
  <files>src/components/PhotoGallery.astro</files>
  <action>
On line 43, change:
```
src={`/thumbs/${photo.thumb}`}
```
to:
```
src={photo.thumb}
```

The `photo.thumb` field in photos.json already contains the full path (e.g., `/thumbs/3PCGOOVOwBqssLxnyUx4ca6DmyvpSlk49x6iLrCzLK0-1536x2048.webp`). Prepending `/thumbs/` again creates a broken double-prefix URL.

Do NOT modify photos.json — the data format is correct; only the component has the bug.
  </action>
  <verify>
Run `PATH="/usr/local/opt/node/bin:$PATH" npx astro build` and confirm no build errors. Then verify by reading the built HTML or the component source that the img src uses `photo.thumb` directly without prepending `/thumbs/`.
  </verify>
  <done>Photo thumbnail URLs resolve correctly (e.g., `/thumbs/filename.webp` not `/thumbs//thumbs/filename.webp`). No 404s in browser dev tools Network tab.</done>
</task>

<task type="auto">
  <name>Task 2: Replace restock marker with water drop SVG icon</name>
  <files>src/components/RouteMap.astro</files>
  <action>
In the restock marker section (~lines 170-178), replace the plain amber circle divIcon with a water drop SVG icon. Replace the current `restockIcon` definition:

```javascript
const restockIcon = L.divIcon({
  className: 'restock-marker',
  html: `<svg xmlns="http://www.w3.org/2000/svg" width="20" height="26" viewBox="0 0 20 26">
    <path d="M10 0C10 0 0 12 0 17c0 5.523 4.477 10 10 10s10-4.477 10-10C20 12 10 0 10 0z" fill="#4a90d9" stroke="#1a2e1a" stroke-width="1.5"/>
    <circle cx="10" cy="17" r="3" fill="rgba(255,255,255,0.5)"/>
  </svg>`,
  iconSize: [20, 26],
  iconAnchor: [10, 26],
  popupAnchor: [0, -26]
});
```

Key design decisions:
- Water drop shape (teardrop/droplet path) in a water blue (#4a90d9) — clearly distinct from amber photo/restock circles
- Dark forest green stroke (#1a2e1a) matches existing marker borders
- 20x26px size — slightly larger than the 14px photo circles for visibility
- iconAnchor at bottom-center of the drop (the pointy end points at the location)
- popupAnchor adjusted for new height
- Small white highlight circle inside for depth
- Keep className 'restock-marker' for existing CSS transparency rules
  </action>
  <verify>
Run `PATH="/usr/local/opt/node/bin:$PATH" npx astro build` and confirm no build errors. Grep for the water drop SVG path in the built output to confirm it compiled.
  </verify>
  <done>Restock markers render as blue water drop icons on the map — visually distinct from amber circle photo markers. Clicking still opens the name/mileage popup.</done>
</task>

</tasks>

<verification>
1. `PATH="/usr/local/opt/node/bin:$PATH" npx astro build` completes without errors
2. Photo gallery img src attributes use `photo.thumb` directly (no `/thumbs/` prefix prepended)
3. Restock marker SVG contains a water drop path shape, not a plain circle
4. Photo markers remain unchanged (amber circles)
</verification>

<success_criteria>
- All photo thumbnails load (no 404s) when viewing the gallery
- Restock markers are immediately distinguishable from photo markers on the map
- Both marker types retain their click functionality (popup for restock, lightbox for photos)
</success_criteria>

<output>
After completion, create `.planning/phases/00-full-site/00-01-SUMMARY.md`
</output>
