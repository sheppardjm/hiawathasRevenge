---
phase: 18-color-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/FloralDivider.astro
autonomous: true

must_haves:
  truths:
    - "Turquoise, scarlet, and sun-yellow token families are defined in @theme static and produce CSS custom properties visible in browser DevTools"
    - "At least one token from each new family (turquoise, scarlet, sun-yellow) renders visibly as an SVG fill or stroke in FloralDivider on the page"
    - "All 7 orphaned v1.1 tokens (lake-500, lake-600, lake-700, berry-500, berry-700, moss-600, gold-600) appear in at least one visible SVG element in FloralDivider"
    - "Every new color token has a documented WCAG AA contrast ratio against forest-900 and forest-950 in CSS comments, with classification as text-safe, large-text-only, or decorative-only"
    - "scarlet-600 is documented as large-text/decorative ONLY (3.00:1 on forest-900, 3.71:1 on forest-950)"
    - "Build passes cleanly with zero errors"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Three new color token families (turquoise 5 shades, scarlet 4 shades, sun-yellow 4 shades) with WCAG contrast documentation"
      contains: "--color-turquoise-300"
    - path: "src/components/FloralDivider.astro"
      provides: "Visible rendering of all new tokens and all orphaned v1.1 tokens via SVG fills/strokes"
      contains: "color-lake-500"
  key_links:
    - from: "src/styles/global.css"
      to: "src/components/FloralDivider.astro"
      via: "CSS custom property references in SVG fill/stroke attributes"
      pattern: "var\\(--color-(turquoise|scarlet|sun|lake|berry|moss|gold)"
---

<objective>
Add three new color token families (turquoise, scarlet, sun-yellow) to the Tailwind 4 @theme static block with full WCAG contrast documentation, and activate all 7 orphaned v1.1 tokens by assigning each a visible SVG fill or stroke in FloralDivider.astro.

Purpose: Establishes the expanded color vocabulary for v1.2 Cultural Maximalism and ensures no dead tokens remain in the design system
Output: 13 new CSS custom properties with contrast ratios documented, all orphaned tokens visibly rendered in FloralDivider
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-color-foundation/18-RESEARCH.md
@src/styles/global.css
@src/components/FloralDivider.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add turquoise, scarlet, and sun-yellow token families to @theme static with WCAG documentation</name>
  <files>src/styles/global.css</files>
  <action>
  In `src/styles/global.css`, inside the `@theme static { ... }` block, insert a new section AFTER the v1.1 Ojibwe-inspired color families block (after the `--color-moss-500` line, before the Typography Scale comment). Add the following:

  ```css
  /* ============================================================
     v1.2: Bold expansion palette — turquoise, scarlet, sun-yellow
     WCAG AA contrast ratios documented per shade vs forest-900 (#1a2e1a) / forest-950 (#0d1a0d)
     Classification: text-safe (>=4.5:1), large-text-only (3.0-4.5:1), decorative-only (<3.0:1)
     ============================================================ */

  /* Turquoise family — Ojibwe sky/water tradition
     -300: 9.79:1 / 12.13:1 text-safe    -400: 7.78:1 / 9.64:1 text-safe
     -500: 5.82:1 / 7.21:1 text-safe     -600: 3.87:1 / 4.79:1 large-text-only
     -700: 2.65:1 / 3.28:1 decorative-only */
  --color-turquoise-300: #5eead4;
  --color-turquoise-400: #2dd4bf;
  --color-turquoise-500: #14b8a6;
  --color-turquoise-600: #0d9488;
  --color-turquoise-700: #0f766e;

  /* Scarlet family — bold red accent
     -400: 5.24:1 / 6.48:1 text-safe     -500: 3.85:1 / 4.77:1 large-text-only
     -600: 3.00:1 / 3.71:1 large-text/decorative ONLY (fails AA normal text on both backgrounds)
     -700: 2.24:1 / 2.77:1 decorative-only */
  --color-scarlet-400: #f87171;
  --color-scarlet-500: #ef4444;
  --color-scarlet-600: #dc2626;
  --color-scarlet-700: #b91c1c;

  /* Sun-yellow family — all four shades pass AA normal text on forest-900/950
     -300: 10.99:1 / 13.61:1 text-safe   -400: 9.46:1 / 11.71:1 text-safe
     -500: 7.55:1 / 9.35:1 text-safe     -600: 4.93:1 / 6.11:1 text-safe */
  --color-sun-300: #fde047;
  --color-sun-400: #facc15;
  --color-sun-500: #eab308;
  --color-sun-600: #ca8a04;
  ```

  **Do NOT modify any existing token definitions.** Only add the new block.
  </action>
  <verify>
  - `grep "turquoise-300" src/styles/global.css` returns a match
  - `grep "scarlet-600" src/styles/global.css` returns a match with "large-text/decorative ONLY" in the comment above
  - `grep "sun-300" src/styles/global.css` returns a match
  - Count: 13 new `--color-` lines added (5 turquoise + 4 scarlet + 4 sun)
  - Each family has a WCAG contrast ratio comment block with ratios vs BOTH forest-900 and forest-950
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - 13 new color tokens defined in @theme static (turquoise-300..700, scarlet-400..700, sun-300..600)
  - Each token family has inline WCAG contrast ratio documentation against both forest-900 and forest-950
  - scarlet-600 explicitly documented as "large-text/decorative ONLY (fails AA normal text on both backgrounds)"
  - Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Activate orphaned v1.1 tokens and render new tokens in FloralDivider.astro</name>
  <files>src/components/FloralDivider.astro</files>
  <action>
  Modify `src/components/FloralDivider.astro` to visually use all 7 orphaned v1.1 tokens AND at least one token from each of the 3 new families. The component is purely decorative (aria-hidden), so color additions are safe and natural.

  **Orphaned token activations (7 tokens to add):**

  1. **berry-500**: Change the first blossom center dot (x=270) from `berry-600` to `berry-500`. Replace:
     ```
     <circle cx="270" cy="44" r="2" fill="var(--color-berry-600)" opacity="0.75" />
     ```
     With:
     ```
     <circle cx="270" cy="44" r="2" fill="var(--color-berry-500)" opacity="0.75" />
     ```
     (The second blossom at x=530 keeps `berry-600` so both are used.)

  2. **berry-700**: Add a parallel vine shadow path below the main vine, at reduced opacity:
     ```xml
     <!-- Vine shadow — berry-700 (orphan activation) -->
     <path
       d="M0 32 Q100 12 200 32 Q300 52 400 32 Q500 12 600 32 Q700 52 800 32"
       stroke="var(--color-berry-700)"
       stroke-width="1"
       fill="none"
       opacity="0.3"
     />
     ```
     Insert this immediately after the main vine `<path>` element (after the closing `/>` on the gold-500 vine path).

  3. **lake-500**: Add a stroke to the first leaf pair at x=150. Replace:
     ```
     <ellipse cx="150" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(-25, 150, 18)" opacity="0.55" />
     ```
     With:
     ```
     <ellipse cx="150" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" stroke="var(--color-lake-500)" stroke-width="0.5" transform="rotate(-25, 150, 18)" opacity="0.55" />
     ```

  4. **lake-600**: Add a stroke to the opposing leaf at x=150. Replace:
     ```
     <ellipse cx="150" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(25, 150, 18)" opacity="0.55" />
     ```
     With:
     ```
     <ellipse cx="150" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" stroke="var(--color-lake-600)" stroke-width="0.5" transform="rotate(25, 150, 18)" opacity="0.55" />
     ```

  5. **lake-700**: Add a background circle behind each blossom center. Insert before the first blossom center dot (before the `<circle cx="270"` line):
     ```xml
     <circle cx="270" cy="44" r="3.5" fill="var(--color-lake-700)" opacity="0.4" />
     ```
     And before the second blossom center dot (before the `<circle cx="530"` line):
     ```xml
     <circle cx="530" cy="44" r="3.5" fill="var(--color-lake-700)" opacity="0.4" />
     ```

  6. **moss-600**: Change the center leaf pair (x=400) fill to `moss-600` to distinguish it from the outer leaf pairs. Replace both leaves at x=400:
     ```
     <ellipse cx="400" cy="30" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(-25, 400, 30)" opacity="0.55" />
     <ellipse cx="400" cy="30" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(25, 400, 30)" opacity="0.55" />
     ```
     With:
     ```
     <ellipse cx="400" cy="30" rx="10" ry="4" fill="var(--color-moss-600)" transform="rotate(-25, 400, 30)" opacity="0.55" />
     <ellipse cx="400" cy="30" rx="10" ry="4" fill="var(--color-moss-600)" transform="rotate(25, 400, 30)" opacity="0.55" />
     ```

  7. **gold-600**: Add stroke to the double-curve accent paths. Replace:
     ```
     <path
       d="M385 22 Q400 16 415 22"
       stroke="var(--color-gold-500)"
       stroke-width="1"
       fill="none"
       opacity="0.5"
     />
     ```
     With:
     ```
     <path
       d="M385 22 Q400 16 415 22"
       stroke="var(--color-gold-600)"
       stroke-width="1"
       fill="none"
       opacity="0.5"
     />
     ```
     (Change only the upper double-curve to gold-600; the lower one keeps gold-500 so both are used.)

  **New token family rendering (3 tokens to add):**

  8. **turquoise-500**: Add a stroke to the leaf pair at x=650. Replace:
     ```
     <ellipse cx="650" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(-25, 650, 18)" opacity="0.55" />
     ```
     With:
     ```
     <ellipse cx="650" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" stroke="var(--color-turquoise-500)" stroke-width="0.5" transform="rotate(-25, 650, 18)" opacity="0.55" />
     ```

  9. **scarlet-400**: Add a small accent dot at the vine midpoint. Insert after the vine shadow path:
     ```xml
     <!-- New palette accent dots -->
     <circle cx="400" cy="30" r="1.5" fill="var(--color-scarlet-400)" opacity="0.5" />
     ```

  10. **sun-500**: Add a stroke to the opposing leaf at x=650. Replace:
      ```
      <ellipse cx="650" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" transform="rotate(25, 650, 18)" opacity="0.55" />
      ```
      With:
      ```
      <ellipse cx="650" cy="18" rx="10" ry="4" fill="var(--color-moss-500)" stroke="var(--color-sun-500)" stroke-width="0.5" transform="rotate(25, 650, 18)" opacity="0.55" />
      ```

  **Important:** Keep all existing opacity values and transforms. The additions are subtle — strokes at 0.5px width, background circles at low opacity, and one fill swap. The FloralDivider should look richer, not cluttered.
  </action>
  <verify>
  - `grep "color-berry-500" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-berry-700" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-lake-500" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-lake-600" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-lake-700" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-moss-600" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-gold-600" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-turquoise-500" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-scarlet-400" src/components/FloralDivider.astro` returns at least 1 match
  - `grep "color-sun-500" src/components/FloralDivider.astro` returns at least 1 match
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - All 7 orphaned v1.1 tokens appear in visible SVG elements: berry-500 (blossom center), berry-700 (vine shadow), lake-500 (leaf stroke), lake-600 (leaf stroke), lake-700 (blossom background circle), moss-600 (center leaf fill), gold-600 (double-curve stroke)
  - All 3 new token families rendered: turquoise-500 (leaf stroke), scarlet-400 (accent dot), sun-500 (leaf stroke)
  - FloralDivider retains decorative character — additions are subtle strokes, low-opacity fills, and small accents
  - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `grep -c "color-turquoise" src/styles/global.css` returns 5
2. `grep -c "color-scarlet" src/styles/global.css` returns 4
3. `grep -c "color-sun" src/styles/global.css` returns 4
4. `grep "scarlet-600" src/styles/global.css` has "large-text/decorative ONLY" in surrounding comment
5. All 7 orphaned tokens confirmed in FloralDivider: `grep -E "color-(lake-500|lake-600|lake-700|berry-500|berry-700|moss-600|gold-600)" src/components/FloralDivider.astro | wc -l` returns >= 7
6. All 3 new family tokens in FloralDivider: `grep -E "color-(turquoise|scarlet|sun)" src/components/FloralDivider.astro | wc -l` returns >= 3
7. `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
</verification>

<success_criteria>
- 13 new color tokens defined in @theme static (5 turquoise + 4 scarlet + 4 sun-yellow)
- WCAG AA contrast ratios documented per shade against both forest-900 and forest-950
- scarlet-600 explicitly classified as large-text/decorative ONLY
- All 7 orphaned v1.1 tokens render visibly in FloralDivider SVG elements
- At least 1 token from each new family renders visibly in FloralDivider
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/18-color-foundation/18-01-SUMMARY.md`
</output>
