---
phase: 09-photo-markers-and-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/admin.astro
  - src/pages/api/save-manifest.ts
autonomous: true

must_haves:
  truths:
    - "Opening /admin in Astro dev server shows a UI listing all 54 source photos with a mileage input for each"
    - "Each photo row shows the thumbnail image and filename for visual identification"
    - "Saving the manifest via the admin UI writes public/data/photos-manifest.json with filename and mile for each photo"
    - "The admin page redirects to / in production builds (not exposed to visitors)"
  artifacts:
    - path: "src/pages/admin.astro"
      provides: "Dev-only photo manifest editor page"
      min_lines: 60
    - path: "src/pages/api/save-manifest.ts"
      provides: "POST endpoint that writes photos-manifest.json"
      min_lines: 15
  key_links:
    - from: "src/pages/admin.astro"
      to: "src/pages/api/save-manifest.ts"
      via: "fetch POST on save button click"
      pattern: "fetch.*api/save-manifest"
    - from: "src/pages/api/save-manifest.ts"
      to: "public/data/photos-manifest.json"
      via: "writeFileSync"
      pattern: "writeFileSync.*photos-manifest"
    - from: "src/pages/admin.astro"
      to: "public/thumbs/"
      via: "thumbnail img src for visual identification"
      pattern: "src.*thumbs"
---

<objective>
Create the dev-only admin page (`admin.astro`) and its POST endpoint (`api/save-manifest.ts`) for assigning mileage values to each photo. The admin UI lists all source photos with their thumbnails and mileage inputs, and the save button writes `photos-manifest.json` that the existing pipeline can consume.

Purpose: Without this admin UI, the developer has no way to assign mileage to the 54 source photos. The manifest is the bridge between raw images and geotagged photo markers on the map.
Output: `src/pages/admin.astro` and `src/pages/api/save-manifest.ts` — a working dev-only photo editor.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-photo-markers-and-admin/09-RESEARCH.md
@src/pages/index.astro
@scripts/match-photos.js
@package.json
@astro.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the POST endpoint for saving the manifest</name>
  <files>src/pages/api/save-manifest.ts</files>
  <action>
    Create `src/pages/api/save-manifest.ts` as an Astro API endpoint.

    1. Set `export const prerender = false;` — this endpoint must run server-side in dev, not be prerendered.

    2. Export a `POST` handler typed as `APIRoute` from `'astro'`:

    ```typescript
    import type { APIRoute } from 'astro';
    import { writeFileSync } from 'fs';
    import { resolve } from 'path';

    export const prerender = false;

    export const POST: APIRoute = async ({ request }) => {
      if (import.meta.env.PROD) {
        return new Response(JSON.stringify({ error: 'Not available in production' }), {
          status: 403,
          headers: { 'Content-Type': 'application/json' }
        });
      }

      const manifest = await request.json();
      const outputPath = resolve(process.cwd(), 'public/data/photos-manifest.json');
      writeFileSync(outputPath, JSON.stringify(manifest, null, 2), 'utf8');

      return new Response(JSON.stringify({ ok: true }), {
        status: 200,
        headers: { 'Content-Type': 'application/json' }
      });
    };
    ```

    Key details:
    - `process.cwd()` is safe here because `npm run dev` always runs from project root.
    - The manifest format is `[{ filename: string, mile: number }, ...]` — matches what `match-photos.js` expects.
    - PROD guard returns 403 — endpoint never writes files in production.
    - `writeFileSync` with pretty-print (2-space indent) so the manifest is human-readable.

    Create the directory `src/pages/api/` if it does not already exist.
  </action>
  <verify>
    Run: `ls -la /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/api/save-manifest.ts`
    Expect: File exists with 15+ lines.

    Run: `grep 'prerender = false' src/pages/api/save-manifest.ts`
    Expect: Match found.

    Run: `grep 'writeFileSync' src/pages/api/save-manifest.ts`
    Expect: Match found.
  </verify>
  <done>POST endpoint at /api/save-manifest accepts JSON body and writes public/data/photos-manifest.json. Guarded against production use.</done>
</task>

<task type="auto">
  <name>Task 2: Create the admin.astro page with photo manifest editor UI</name>
  <files>src/pages/admin.astro</files>
  <action>
    Create `src/pages/admin.astro` — a dev-only page for editing photo mileage assignments.

    **Frontmatter section:**

    1. Set `export const prerender = false;` — page runs server-side in dev.

    2. Add PROD redirect guard:
       ```typescript
       if (import.meta.env.PROD) {
         return Astro.redirect('/');
       }
       ```

    3. Import Node.js modules to read existing manifest and list photos:
       ```typescript
       import { readFileSync, existsSync, readdirSync } from 'fs';
       import { resolve, basename, extname } from 'path';
       ```

    4. Load existing manifest (if any) for pre-populating mileage values:
       ```typescript
       const manifestPath = resolve(process.cwd(), 'public/data/photos-manifest.json');
       const manifest = existsSync(manifestPath)
         ? JSON.parse(readFileSync(manifestPath, 'utf8'))
         : [];
       ```

    5. List all source JPGs from the `images/` directory:
       ```typescript
       const imagesDir = resolve(process.cwd(), 'images');
       const photoFiles = readdirSync(imagesDir)
         .filter(f => extname(f).toLowerCase() === '.jpg')
         .sort();
       ```

    6. Build a lookup map from filename to existing mileage:
       ```typescript
       const mileMap = new Map(manifest.map((m: any) => [m.filename, m.mile]));
       ```

    7. Derive thumbnail filename (same logic as generate-thumbnails.js):
       ```typescript
       function thumbName(filename: string): string {
         return basename(filename, extname(filename)).replace(/ /g, '_') + '.webp';
       }
       ```

    **Template section:**

    8. Use a minimal standalone HTML page (NOT BaseLayout — admin is internal dev tooling):

       ```html
       <html lang="en">
       <head>
         <meta charset="utf-8" />
         <meta name="viewport" content="width=device-width, initial-scale=1" />
         <title>Photo Manifest Editor</title>
         <style>
           /* Inline styles — admin page is standalone, does not use Tailwind */
           body { font-family: system-ui, sans-serif; background: #1a2e1a; color: #f5f0e8; padding: 2rem; max-width: 900px; margin: 0 auto; }
           h1 { color: #c8973e; margin-bottom: 1rem; }
           .info { color: #e8e0d0; font-size: 0.875rem; margin-bottom: 2rem; }
           .photo-row { display: flex; align-items: center; gap: 1rem; padding: 0.75rem 0; border-bottom: 1px solid #3d6b3d; }
           .photo-row img { width: 80px; height: 80px; object-fit: cover; border-radius: 4px; }
           .photo-row .filename { flex: 1; font-size: 0.75rem; word-break: break-all; color: #e8e0d0; }
           .photo-row input[type="number"] { width: 80px; padding: 0.25rem 0.5rem; background: #2d4a2d; border: 1px solid #3d6b3d; color: #f5f0e8; border-radius: 4px; font-size: 0.875rem; }
           .photo-row input[type="number"]:focus { outline: 2px solid #c8973e; border-color: #c8973e; }
           .actions { position: sticky; bottom: 0; padding: 1rem 0; background: #1a2e1a; border-top: 2px solid #c8973e; margin-top: 1rem; display: flex; gap: 1rem; align-items: center; }
           button { padding: 0.5rem 1.5rem; background: #c8973e; color: #0d1a0d; border: none; border-radius: 4px; font-weight: 600; cursor: pointer; font-size: 1rem; }
           button:hover { background: #d4a84e; }
           .status { font-size: 0.875rem; color: #e8e0d0; }
           .status.success { color: #8a9a5b; }
           .status.error { color: #a0522d; }
         </style>
       </head>
       <body>
         <h1>Photo Manifest Editor</h1>
         <p class="info">
           Assign mile markers to each photo. Save the manifest, then restart
           <code>npm run dev</code> to regenerate photos.json via the pipeline.
         </p>

         <div id="photo-list">
           {photoFiles.map((file) => (
             <div class="photo-row" data-filename={file}>
               <img src={`/thumbs/${thumbName(file)}`} alt="" />
               <span class="filename">{file}</span>
               <input
                 type="number"
                 step="0.1"
                 min="0"
                 max="105"
                 placeholder="Mile"
                 value={mileMap.has(file) ? mileMap.get(file) : ''}
                 class="mile-input"
               />
             </div>
           ))}
         </div>

         <div class="actions">
           <button id="save-btn">Save Manifest</button>
           <span id="status" class="status"></span>
         </div>
       </body>
       </html>
       ```

    **Script section:**

    9. Add a `<script>` block at the end of the page (after closing `</html>` or inside `<body>`) that handles the save button:

       ```javascript
       const saveBtn = document.getElementById('save-btn');
       const statusEl = document.getElementById('status');

       saveBtn.addEventListener('click', async () => {
         const rows = document.querySelectorAll('.photo-row');
         const manifest = [];

         for (const row of rows) {
           const filename = row.dataset.filename;
           const input = row.querySelector('.mile-input');
           const mile = parseFloat(input.value);

           // Only include photos that have a mileage assigned
           if (!isNaN(mile)) {
             manifest.push({ filename, mile });
           }
         }

         statusEl.textContent = 'Saving...';
         statusEl.className = 'status';

         try {
           const res = await fetch('/api/save-manifest', {
             method: 'POST',
             headers: { 'Content-Type': 'application/json' },
             body: JSON.stringify(manifest)
           });

           if (res.ok) {
             statusEl.textContent = `Saved ${manifest.length} photos. Restart npm run dev to regenerate.`;
             statusEl.className = 'status success';
           } else {
             const data = await res.json();
             statusEl.textContent = `Error: ${data.error || res.statusText}`;
             statusEl.className = 'status error';
           }
         } catch (err) {
           statusEl.textContent = `Network error: ${err.message}`;
           statusEl.className = 'status error';
         }
       });
       ```

    **Critical details:**
    - `prerender = false` on BOTH admin.astro and save-manifest.ts — both must work server-side in dev.
    - Standalone HTML (no BaseLayout) — admin is internal dev tooling, not part of the visitor-facing site.
    - Inline CSS using the project's color tokens (forest greens, amber, cream) for visual consistency.
    - Only photos with a numeric mileage value are included in the saved manifest — empty inputs are skipped.
    - The manifest format `[{ filename, mile }]` matches what `match-photos.js` reads.
    - Thumbnail paths use the same derivation as `generate-thumbnails.js` (space-to-underscore + `.webp`).
    - `step="0.1"` on the number input allows decimal mile values (e.g., 42.5).
    - `max="105"` is slightly above the 100-mile route to allow for GPS variance.

    **Anti-patterns to avoid:**
    - Do NOT use BaseLayout or Tailwind — admin page must be self-contained.
    - Do NOT add an SSR adapter to astro.config.ts — `prerender = false` works in dev without an adapter.
    - Do NOT store data in localStorage — write directly to the filesystem via the POST endpoint.
  </action>
  <verify>
    Run: `PATH="/usr/local/opt/node/bin:$PATH" npx astro build 2>&1 | tail -10`
    Expect: Build completes. The admin page either produces a redirect or is excluded — no errors about `prerender = false` in a static build.

    Check file exists and has content:
    - `wc -l src/pages/admin.astro` shows 60+ lines
    - `grep 'prerender = false' src/pages/admin.astro` returns match
    - `grep 'import.meta.env.PROD' src/pages/admin.astro` returns match
    - `grep 'save-manifest' src/pages/admin.astro` returns match
    - `grep 'photo-row' src/pages/admin.astro` returns match
  </verify>
  <done>
    Opening /admin in Astro dev server shows all 54 source photos with thumbnails, filenames, and mileage number inputs. Clicking Save writes photos-manifest.json to public/data/ via the POST endpoint. The page redirects to / in production.
  </done>
</task>

</tasks>

<verification>
1. `ls src/pages/api/save-manifest.ts` confirms endpoint exists
2. `ls src/pages/admin.astro` confirms admin page exists
3. `grep 'prerender = false' src/pages/admin.astro src/pages/api/save-manifest.ts` shows both files have server-side rendering
4. `grep 'import.meta.env.PROD' src/pages/admin.astro src/pages/api/save-manifest.ts` shows both have production guards
5. `grep 'photos-manifest.json' src/pages/api/save-manifest.ts` confirms correct output path
6. `grep 'thumbName\|thumbs' src/pages/admin.astro` confirms thumbnail display
7. `PATH="/usr/local/opt/node/bin:$PATH" npx astro build 2>&1 | tail -10` completes without errors
</verification>

<success_criteria>
- admin.astro renders in Astro dev server at /admin with photo list and mileage inputs
- admin.astro redirects to / in production (import.meta.env.PROD guard)
- save-manifest.ts POST endpoint writes public/data/photos-manifest.json
- save-manifest.ts returns 403 in production
- Thumbnails display next to each photo filename in the admin UI
- Only photos with a mileage value are included in the saved manifest
- Manifest format matches match-photos.js expectations: [{ filename, mile }]
- astro build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/09-photo-markers-and-admin/09-01-SUMMARY.md`
</output>
