---
phase: 30-image-optimization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HiawathaExplainer.astro
autonomous: true

must_haves:
  truths:
    - "HiawathaExplainer parallax background images use CSS image-set() with WebP preference and JPEG fallback"
    - "Gallery grid serves .webp thumbnail files (not .jpg sources) — already satisfied, verified unchanged"
    - "All three parallax sections (.poem-section, .forest-section, .ride-section) render their background images without visual regression"
  artifacts:
    - path: "src/components/HiawathaExplainer.astro"
      provides: "CSS image-set() rules for all three parallax ::before backgrounds"
      contains: "image-set("
  key_links:
    - from: "src/components/HiawathaExplainer.astro"
      to: "public/images/Eo6Lpv5a2onA-EMkS4BGrkQOHMQb4rwDbey7kfJDAZc-1536x2048.webp"
      via: "CSS image-set() references the WebP file with JPEG fallback"
      pattern: "image-set"
    - from: "src/components/HiawathaExplainer.astro"
      to: "public/images/K9zNeD_N2ikOKXNlKHc1dGUY7N6W3cGWVevoXlB49aI-1536x2048.webp"
      via: "CSS image-set() references the WebP file with JPEG fallback"
      pattern: "image-set"
    - from: "src/components/HiawathaExplainer.astro"
      to: "public/images/Gw-ZiugqoNyWNNMHZ-n65VcO7XjnipWnDWQz77mE2kQ-1536x2048.webp"
      via: "CSS image-set() references the WebP file with JPEG fallback"
      pattern: "image-set"
---

<objective>
Update the three parallax background-image CSS rules in HiawathaExplainer.astro to use CSS `image-set()` with WebP preference and JPEG fallback, and verify that IMG-03 (gallery WebP thumbnails) remains satisfied. The WebP files are generated by Plan 30-01's `generate-webp.js` script.

Purpose: Parallax backgrounds are large images that benefit from WebP's ~25-35% size reduction. CSS `image-set()` with `type()` notation is Baseline-available since September 2023 and provides native format selection without JavaScript.
Output: Updated `src/components/HiawathaExplainer.astro` with `image-set()` CSS rules for all three parallax sections.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-image-optimization/30-RESEARCH.md
@src/components/HiawathaExplainer.astro
@src/components/PhotoGallery.astro
@public/data/photos.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update parallax CSS to image-set() and verify gallery WebP</name>
  <files>src/components/HiawathaExplainer.astro</files>
  <action>
    Two parts:

    1. **Update HiawathaExplainer.astro CSS** — Replace the three `background-image: url(...)` rules for parallax sections with `image-set()` providing WebP preference and JPEG fallback. These are in the `<style>` block.

       Replace the three rules (around lines 237-247):

       Change `.poem-section::before` from:
       ```css
       .poem-section::before {
         background-image: url('/images/Eo6Lpv5a2onA-EMkS4BGrkQOHMQb4rwDbey7kfJDAZc-1536x2048.jpg');
       }
       ```
       To:
       ```css
       .poem-section::before {
         background-image: image-set(
           url('/images/Eo6Lpv5a2onA-EMkS4BGrkQOHMQb4rwDbey7kfJDAZc-1536x2048.webp') type("image/webp"),
           url('/images/Eo6Lpv5a2onA-EMkS4BGrkQOHMQb4rwDbey7kfJDAZc-1536x2048.jpg') type("image/jpeg")
         );
       }
       ```

       Change `.forest-section::before` from:
       ```css
       .forest-section::before {
         background-image: url('/images/K9zNeD_N2ikOKXNlKHc1dGUY7N6W3cGWVevoXlB49aI-1536x2048.jpg');
       }
       ```
       To:
       ```css
       .forest-section::before {
         background-image: image-set(
           url('/images/K9zNeD_N2ikOKXNlKHc1dGUY7N6W3cGWVevoXlB49aI-1536x2048.webp') type("image/webp"),
           url('/images/K9zNeD_N2ikOKXNlKHc1dGUY7N6W3cGWVevoXlB49aI-1536x2048.jpg') type("image/jpeg")
         );
       }
       ```

       Change `.ride-section::before` from:
       ```css
       .ride-section::before {
         background-image: url('/images/Gw-ZiugqoNyWNNMHZ-n65VcO7XjnipWnDWQz77mE2kQ-1536x2048.jpg');
       }
       ```
       To:
       ```css
       .ride-section::before {
         background-image: image-set(
           url('/images/Gw-ZiugqoNyWNNMHZ-n65VcO7XjnipWnDWQz77mE2kQ-1536x2048.webp') type("image/webp"),
           url('/images/Gw-ZiugqoNyWNNMHZ-n65VcO7XjnipWnDWQz77mE2kQ-1536x2048.jpg') type("image/jpeg")
         );
       }
       ```

       CRITICAL NOTES:
       - Use `image-set()` WITHOUT the `-webkit-` prefix — it's Baseline widely available since September 2023.
       - Use `type("image/webp")` and `type("image/jpeg")` notation — this is the standard syntax for format-based selection.
       - The WebP entry MUST come first (browser picks the first format it supports).
       - Do NOT change any other CSS properties on these selectors (`background-size`, `background-position`, `background-attachment` etc. are on the shared `.subsection-bg::before` rule and remain untouched).
       - The WebP files referenced here are generated by Plan 30-01's `generate-webp.js`. If Plan 30-02 executes in parallel with 30-01, the WebP files may not exist yet — that's fine for the CSS change itself, the files will exist after 30-01 runs. The build will succeed either way since CSS background images are runtime references, not build-time imports.

    2. **Verify IMG-03 (gallery WebP)** — Confirm that `public/data/photos.json` already uses `/thumbs/*.webp` paths for thumbnails. Do NOT modify any gallery files. Just verify:
       - `grep '.webp' public/data/photos.json` should return multiple matches.
       - `ls public/thumbs/*.webp | head -5` should show existing WebP thumbnail files.
       This is a read-only verification confirming IMG-03 was satisfied before this phase began.
  </action>
  <verify>
    Run `npm run build` — build completes with zero errors.
    Check built HTML/CSS for image-set: `grep -c 'image-set' dist/index.html` — should be at least 3 (one per parallax section).
    Check all three sections use WebP+JPEG: `grep 'image-set' dist/index.html` should show all three parallax image URLs with both .webp and .jpg variants.
    Verify no `-webkit-` prefix: `grep -c 'webkit-image-set' dist/index.html` — should be 0.
    Verify gallery WebP: `grep -c '.webp' public/data/photos.json` — should be >= 1 (confirming IMG-03 pre-satisfied).
    Verify WebP thumbnails exist: `ls public/thumbs/*.webp | wc -l` — should be > 0.
  </verify>
  <done>All three parallax background-image rules in HiawathaExplainer.astro use `image-set()` with WebP preference and JPEG fallback. Gallery thumbnails confirmed to already serve .webp files (IMG-03 pre-satisfied). Build passes with zero errors.</done>
</task>

</tasks>

<verification>
After the task:

1. `npm run build` completes with zero errors
2. `grep 'image-set' dist/index.html` matches at least 3 times (one per parallax section)
3. Each `image-set()` contains both a `.webp` URL with `type("image/webp")` and a `.jpg` URL with `type("image/jpeg")`
4. No `-webkit-image-set` appears in the output
5. `grep '.webp' public/data/photos.json` confirms gallery thumbnails already use WebP (IMG-03)
6. `ls public/thumbs/*.webp` confirms WebP thumbnail files exist
</verification>

<success_criteria>
- `.poem-section::before` uses `image-set()` with Eo6Lpv5a... WebP and JPEG
- `.forest-section::before` uses `image-set()` with K9zNeD_N... WebP and JPEG
- `.ride-section::before` uses `image-set()` with Gw-ZiugqO... WebP and JPEG
- No `-webkit-` prefix on `image-set()`
- Gallery thumbnails confirmed using .webp paths in photos.json (IMG-03 pre-satisfied)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-image-optimization/30-02-SUMMARY.md`
</output>
