---
phase: 38-ui-config-quick-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteMap.astro
  - src/components/RouteExplainer.astro
  - astro.config.ts
autonomous: false

must_haves:
  truths:
    - "Sector pill labels on the map display full segment names without truncation at zoom level 12+"
    - "The 520 segment card in the RouteExplainer shows a hero photo (the mile 5.51 gravel road photo)"
    - "astro.config.ts has site set to https://hiawathasrevenge.com with no TODO comment"
    - "npm run build succeeds with correct canonical URL meta tags"
  artifacts:
    - path: "src/components/RouteMap.astro"
      provides: "Enlarged sector pill labels"
      contains: "font-size: 13px"
    - path: "src/components/RouteExplainer.astro"
      provides: "520 segment captures mile 5.51 photo via adjusted boundary"
      contains: "endMi: 5.6"
    - path: "astro.config.ts"
      provides: "Production site URL without TODO"
      contains: "site: 'https://hiawathasrevenge.com'"
  key_links:
    - from: "src/components/RouteExplainer.astro SEGMENTS[0].endMi"
      to: "photos.json mile 5.51 photo"
      via: "p.mile >= seg.startMi && p.mile < seg.endMi filter"
      pattern: "endMi: 5.6"
    - from: "src/components/RouteExplainer.astro SEGMENTS[1].startMi"
      to: "SEGMENTS[0].endMi"
      via: "non-overlapping boundary constraint"
      pattern: "startMi: 5.6"
---

<objective>
Apply three independent UI and config fixes: enlarge sector pill labels on the map, capture a hero photo for the 520 segment by adjusting its mile boundary, and remove the TODO comment from the production site URL.

Purpose: Closes LABEL-01, PHOTO-01, and DEPLOY-01 requirements — the three deferred quick fixes that clear the board before the segment description rewrite.
Output: Larger readable map labels, 520 segment with hero photo, clean astro.config.ts with production URL.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-ui-config-quick-fixes/38-RESEARCH.md
@src/components/RouteMap.astro
@src/components/RouteExplainer.astro
@astro.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enlarge map labels, adjust 520 photo boundary, remove site URL TODO</name>
  <files>
    src/components/RouteMap.astro
    src/components/RouteExplainer.astro
    astro.config.ts
  </files>
  <action>
**Edit 1: Sector pill labels in `src/components/RouteMap.astro`**

In the `renderRoute()` function, find the `divIcon` html template (around line 678-692). Make these inline style changes:

- Change `padding: 3px 8px;` to `padding: 5px 10px;`
- Change `font-size: 11px;` to `font-size: 13px;`
- Change the star span `font-size: 9px;` to `font-size: 10px;`

Leave all other inline styles unchanged (`background`, `color`, `border`, `border-radius`, `font-family`, `font-weight`, `white-space`, `transform`, `box-shadow`, `line-height`, `text-align`, `letter-spacing`).

Do NOT change `iconSize: [0, 0]` or `iconAnchor: [0, 0]` — the CSS transform handles centering.

**Edit 2: 520 segment photo boundary in `src/components/RouteExplainer.astro`**

In the SEGMENTS array, the 520 entry (first element) currently has `endMi: 5.0`. The nearest photo in photos.json is at mile 5.51, which falls just outside this boundary. Fix by widening the 520 segment to capture it:

- Change 520's `endMi: 5.0` to `endMi: 5.6`
- Change NF2266's `startMi: 5.0` to `startMi: 5.6` (the adjacent segment, second element)

Both edits are required to keep boundaries non-overlapping. NF2266 currently has 8+ photos starting at mile 5.51; after this change it loses only the mile 5.51 photo (its boundary starts at 5.6), while 520 gains it. NF2266 retains plenty of photo coverage.

The photo filter logic (`p.mile >= seg.startMi && p.mile < seg.endMi`) will now include the 5.51 photo for 520 (since 5.51 < 5.6).

**Edit 3: Remove TODO comment from `astro.config.ts`**

On line 5, change:
```
  site: 'https://hiawathasrevenge.com', // TODO: update to actual deployed URL
```
to:
```
  site: 'https://hiawathasrevenge.com',
```

Remove only the comment. Do not change the URL value.

**Build verification:**

Run `npm run build` to confirm:
- No syntax errors in any modified file
- Build succeeds fully
  </action>
  <verify>
1. In RouteMap.astro, confirm the divIcon html contains `font-size: 13px`, `padding: 5px 10px`, and `font-size: 10px` for stars
2. In RouteExplainer.astro, confirm 520 has `endMi: 5.6` and NF2266 has `startMi: 5.6`
3. In astro.config.ts, confirm `site: 'https://hiawathasrevenge.com',` with no `// TODO` comment
4. `npm run build` succeeds with no errors
5. `grep -c "TODO" astro.config.ts` returns 0
  </verify>
  <done>
Map label pills use 13px font with 5px 10px padding. The 520 segment boundary extends to mile 5.6 capturing the 5.51 photo. NF2266 starts at mile 5.6 (no overlap). The astro.config.ts TODO comment is removed. Build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Three independent fixes: enlarged map sector labels, 520 segment now has a hero photo from the boundary adjustment, and astro.config.ts has a clean production URL.
  </what-built>
  <how-to-verify>
Run `npm run dev` and test in browser:

**Map labels (LABEL-01):**
1. Scroll to the map section
2. Zoom to level 12 or higher so sector labels appear
3. Verify all 7 sector pill labels display full names without text clipping — especially longer names like "NF2217-2218" and "Bass Lake Rd"
4. Verify labels still look proportional and the pill shape is not broken

**520 segment photo (PHOTO-01):**
5. Scroll to the "Route, Segment by Segment" section
6. The first segment card (520) should now display a hero photo — a gravel road through pine forest
7. Verify NF2266 (the second segment card) still has its own hero photo(s)

**Site URL (DEPLOY-01):**
8. View page source or inspect `<link rel="canonical">` — should contain `https://hiawathasrevenge.com`
9. Inspect `<meta property="og:url">` — should also contain the correct URL
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- `grep "font-size: 13px" src/components/RouteMap.astro` finds the enlarged label
- `grep "endMi: 5.6" src/components/RouteExplainer.astro` finds the adjusted 520 boundary
- `grep "startMi: 5.6" src/components/RouteExplainer.astro` finds the adjusted NF2266 boundary
- `grep -c "TODO" astro.config.ts` returns 0
- View source confirms canonical URL is https://hiawathasrevenge.com
</verification>

<success_criteria>
1. Sector pill labels render at 13px font with 5px 10px padding — full names visible without clipping at zoom 12+
2. The 520 segment card displays a hero photo (the mile 5.51 gravel road image captured by the boundary shift)
3. NF2266 segment card still has hero photos (mile 5.6+ coverage)
4. astro.config.ts has `site: 'https://hiawathasrevenge.com'` with no TODO comment
5. Build produces correct canonical URL and OG URL meta tags
6. No regressions — all other segments unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/38-ui-config-quick-fixes/38-01-SUMMARY.md`
</output>
