---
phase: 10-content-narrative-and-visual-identity
plan: 03
type: execute
wave: 2
depends_on: ["10-01", "10-02"]
files_modified:
  - src/pages/index.astro
autonomous: true

must_haves:
  truths:
    - "The page opens with introductory paragraphs covering Hiawatha the historical/literary figure, the Longfellow poem's connection to the Upper Peninsula, and the origin of the National Forest name"
    - "A route stats block displays distance, total elevation gain, and surface type breakdown pulled from build-time data"
    - "A donate CTA button linking to mbtn.org/donate is prominent and visible without scrolling on desktop"
    - "A GPX download link is present and downloads the source GPX file when clicked"
    - "The site name h1 renders as a national park badge design (existing SVG badge preserved)"
    - "Topographic line patterns appear as section dividers between major content areas"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Complete page layout with narrative, stats, donate CTA, GPX link, topo dividers"
      min_lines: 100
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/RouteStats.astro"
      via: "Astro component import and render"
      pattern: "import RouteStats"
    - from: "src/pages/index.astro"
      to: "src/components/DonateCallout.astro"
      via: "Astro component import and render"
      pattern: "import DonateCallout"
    - from: "src/pages/index.astro"
      to: "public/Munising_Hiawatha_s_Revenge.gpx"
      via: "anchor href with download attribute"
      pattern: "download.*gpx"
---

<objective>
Assemble the complete index.astro page by wiring in the RouteStats and DonateCallout components, writing the Hiawatha narrative content, adding the GPX download link, applying topographic dividers, and refining the badge h1.

Purpose: This is the final assembly that transforms the page from a technical demo into a rich, narrative-driven showcase that fulfills all Phase 10 success criteria.
Output: Fully updated `src/pages/index.astro` with all content, components, and visual identity elements.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-content-narrative-and-visual-identity/10-RESEARCH.md
@.planning/phases/10-content-narrative-and-visual-identity/10-01-SUMMARY.md
@.planning/phases/10-content-narrative-and-visual-identity/10-02-SUMMARY.md
@src/pages/index.astro
@src/components/RouteStats.astro
@src/components/DonateCallout.astro
@src/styles/global.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire components, narrative, GPX link, and topo dividers into index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
    Update `src/pages/index.astro` to include all Phase 10 content. This is a significant rewrite of the page body while preserving the existing badge SVG, component imports, and scoped styles.

    **1. Update frontmatter imports:**

    Add imports for the two new components:

    ```astro
    ---
    import BaseLayout from '../layouts/BaseLayout.astro';
    import RouteMap from '../components/RouteMap.astro';
    import ElevationProfile from '../components/ElevationProfile.astro';
    import PhotoGallery from '../components/PhotoGallery.astro';
    import RouteStats from '../components/RouteStats.astro';
    import DonateCallout from '../components/DonateCallout.astro';
    ---
    ```

    **2. Rewrite the page body.**

    The new page layout (top to bottom) is:

    1. **Badge hero section** (PRESERVE EXACTLY as-is — do NOT modify the SVG, badge-content, badge styles)
    2. **Donate CTA section** (NEW — DonateCallout component, placed between hero and narrative for above-fold visibility on desktop)
    3. **Topographic divider** (NEW — `<div class="topo-divider"></div>`)
    4. **Narrative section** (REPLACE existing "The Route" placeholder text with Hiawatha narrative)
    5. **Route Stats section** (NEW — RouteStats component)
    6. **GPX download link** (NEW — inline in the stats section or as its own small section)
    7. **Topographic divider** (NEW — between content and interactive sections)
    8. **Explore the Route section** (PRESERVE — RouteMap component)
    9. **Elevation Profile section** (PRESERVE — ElevationProfile component)
    10. **Photos section** (PRESERVE — PhotoGallery component)
    11. **Support the Trail section** (REPLACE placeholder with DonateCallout component + GPX link)
    12. **Footer section** (PRESERVE)

    **Detailed section content:**

    **Section 2 — Donate CTA (above fold):**
    ```html
    <section class="py-[--spacing-block] flex justify-center">
      <DonateCallout />
    </section>
    ```

    **Section 3 — Topographic divider:**
    ```html
    <div class="topo-divider"></div>
    ```

    **Section 4 — Narrative (replaces "The Route" section):**

    Replace the existing "The Route" `<section>` (lines 50-58) with:

    ```html
    <section class="py-[--spacing-block]">
      <h2 class="text-3xl mb-[--spacing-element]">The Route</h2>
      <div class="max-w-prose space-y-4 text-cream-100 leading-relaxed">
        <p>
          In 1855, Henry Wadsworth Longfellow published <em>The Song of Hiawatha</em>,
          an epic poem that drew on Ojibwe oral traditions collected by ethnographer
          Henry Rowe Schoolcraft. Set along the shores of Lake Superior in Michigan's
          Upper Peninsula, the poem wove together legends of the trickster-hero
          Manabozho under the borrowed name of Hiawatha — a figure from the
          Haudenosaunee (Iroquois) Confederacy far to the east.
        </p>
        <p>
          Longfellow's poem reshaped how Americans imagined this landscape. When the
          federal government designated nearly 900,000 acres of Upper Peninsula forest
          as a National Forest in 1931, they named it after the poem's protagonist:
          the Hiawatha National Forest. Today the forest stretches across the peninsula
          from Lake Superior to Lake Michigan, sheltering over 100 miles of Great Lakes
          shoreline, pristine inland lakes, and dense hardwood wilderness.
        </p>
        <p>
          Hiawatha's Revenge is a 100-mile cycling route that threads through the
          heart of this forest. Starting and ending in Munising, the route follows
          quiet forest roads, rugged two-track, and remote singletrack past
          landscapes that have changed little since Schoolcraft documented the Ojibwe
          stories that would one day name this land. It is a ride that tests your
          legs and rewards your soul.
        </p>
        <p>
          The route is organized by the
          <a href="https://mbtn.org" class="underline hover:text-amber-300">Munising Bay Trail Network</a>
          (MBTN), a 501(c)(3) nonprofit that builds and maintains singletrack trails
          across the Hiawatha National Forest. Every mile you ride supports their
          work preserving access to this extraordinary landscape.
        </p>
      </div>
    </section>
    ```

    **Section 5 — Route Stats:**
    ```html
    <section class="py-[--spacing-block]">
      <h2 class="text-3xl mb-[--spacing-element]">Route Stats</h2>
      <RouteStats />
    </section>
    ```

    **Section 6 — GPX Download (small, inline after stats):**
    ```html
    <section class="py-[--spacing-block] flex flex-col items-center gap-2">
      <a
        href="/Munising_Hiawatha_s_Revenge.gpx"
        download="HiawathasRevenge.gpx"
        class="gpx-download"
      >
        Download GPX File
      </a>
      <span class="text-xs text-cream-200">Load this route onto your GPS device</span>
    </section>
    ```

    **Section 7 — Second topographic divider:**
    ```html
    <div class="topo-divider"></div>
    ```

    **Sections 8-10 — Interactive sections (PRESERVE EXACTLY):**
    Keep the existing RouteMap, ElevationProfile, and Photos sections unchanged.

    **Section 11 — Support the Trail (REPLACE the existing placeholder):**

    Replace the current "Support the Trail" section (which has a non-functional placeholder) with:

    ```html
    <section class="py-[--spacing-block]">
      <h2 class="text-3xl mb-[--spacing-element]">Support the Trail</h2>
      <DonateCallout />
    </section>
    ```

    **Section 12 — Footer (PRESERVE EXACTLY).**

    **3. Add scoped style for GPX download link:**

    Add to the existing `<style>` block (after the badge styles, before the closing `</style>`):

    ```css
    .gpx-download {
      display: inline-block;
      border: 2px solid var(--color-forest-700);
      padding: 0.5rem 1.5rem;
      color: var(--color-amber-400);
      font-family: var(--font-display);
      font-weight: 600;
      font-size: var(--font-size-sm);
      text-transform: uppercase;
      letter-spacing: 0.12em;
      text-decoration: none;
      transition: border-color 0.2s, color 0.2s;
    }

    .gpx-download:hover {
      border-color: var(--color-amber-500);
      color: var(--color-amber-300);
    }
    ```

    **4. Badge h1 refinement (MINIMAL):**

    The existing badge SVG, badge-content, and badge styles are already correct and should NOT be rebuilt. The only refinement: add a thin decorative rule between the title and miles label. Inside the `.badge-content` div, add an `<hr>` element:

    Change the badge-content from:
    ```html
    <div class="badge-content">
      <h1 class="badge-title">Hiawatha's Revenge</h1>
      <span class="badge-miles">100 Miles</span>
    </div>
    ```

    To:
    ```html
    <div class="badge-content">
      <h1 class="badge-title">Hiawatha's Revenge</h1>
      <hr class="badge-rule" />
      <span class="badge-miles">100 Miles</span>
    </div>
    ```

    And add to the scoped styles:
    ```css
    .badge-rule {
      width: 60%;
      border: none;
      border-top: 1px solid var(--color-amber-500);
      opacity: 0.6;
      margin: 0.25rem 0;
    }
    ```

    **Critical constraints:**
    - Do NOT modify the badge SVG (`<svg class="badge-shape" ...>`) — it is already complete.
    - Do NOT remove the badge scoped styles — they are correct.
    - Do NOT change the RouteMap, ElevationProfile, or PhotoGallery sections.
    - The `download` attribute value "HiawathasRevenge.gpx" gives the downloaded file a clean filename (no underscores or spaces).
    - Use `space-y-4` on the narrative `<div>` for consistent paragraph spacing.
    - The DonateCallout appears TWICE: once above-fold (between hero and narrative) and once in the "Support the Trail" section near the bottom.

    **Anti-patterns to avoid:**
    - Do NOT remove or replace the existing shield SVG badge.
    - Do NOT add client-side JavaScript to any of the new sections.
    - Do NOT use `fetch()` anywhere — all data comes through components.
    - Do NOT hardcode route stats in index.astro — they come from RouteStats component.
  </action>
  <verify>
    Run: `grep 'import RouteStats' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found.

    Run: `grep 'import DonateCallout' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found.

    Run: `grep '<RouteStats' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found — component rendered.

    Run: `grep '<DonateCallout' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: At least 2 matches — above-fold and in support section.

    Run: `grep 'download.*gpx' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found — GPX download link present.

    Run: `grep 'topo-divider' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: At least 2 matches — dividers between sections.

    Run: `grep 'Longfellow\|Hiawatha National Forest\|Ojibwe' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Matches found — narrative content present.

    Run: `grep 'badge-rule' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found — badge refinement applied.

    Run: `grep 'badge-shape' /Users/Sheppardjm/Repos/hiawathasRevenge/src/pages/index.astro`
    Expect: Match found — original SVG preserved.

    Run: `PATH="/usr/local/opt/node/bin:$PATH" npx astro build 2>&1 | tail -10`
    Expect: Build completes without errors.
  </verify>
  <done>
    index.astro contains the complete page: badge hero with rule refinement, above-fold donate CTA, Hiawatha narrative (4 paragraphs covering Longfellow/Ojibwe/National Forest/MBTN), route stats via RouteStats component, GPX download link with download attribute, topographic dividers between major sections, all existing interactive components preserved, and a second donate CTA in the Support section.
  </done>
</task>

</tasks>

<verification>
1. `grep 'import RouteStats\|import DonateCallout' src/pages/index.astro` — both components imported
2. `grep '<RouteStats' src/pages/index.astro` — stats rendered
3. `grep -c '<DonateCallout' src/pages/index.astro` — appears at least twice
4. `grep 'download.*gpx\|Munising_Hiawatha' src/pages/index.astro` — GPX download present
5. `grep 'topo-divider' src/pages/index.astro` — topographic dividers present
6. `grep 'Longfellow\|Ojibwe\|National Forest' src/pages/index.astro` — narrative content
7. `grep 'badge-shape' src/pages/index.astro` — original SVG badge preserved
8. `grep 'badge-rule' src/pages/index.astro` — badge refinement
9. `grep 'RouteMap\|ElevationProfile\|PhotoGallery' src/pages/index.astro` — all interactive sections preserved
10. `PATH="/usr/local/opt/node/bin:$PATH" npx astro build 2>&1 | tail -10` — clean build
</verification>

<success_criteria>
- index.astro imports and renders RouteStats and DonateCallout components
- Hiawatha narrative replaces the old "The Route" placeholder with 4 paragraphs covering Longfellow, Ojibwe traditions, National Forest naming, and MBTN
- DonateCallout appears above-fold (between hero and narrative) and in Support section
- GPX download link with `download` attribute points to /Munising_Hiawatha_s_Revenge.gpx
- Two topographic dividers separate content from interactive sections
- Badge SVG is preserved with minor hr rule refinement
- All existing interactive sections (RouteMap, ElevationProfile, PhotoGallery) are unchanged
- astro build completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-content-narrative-and-visual-identity/10-03-SUMMARY.md`
</output>
