---
phase: 22-animation-polish
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - src/components/ScrollReveal.astro
  - src/pages/index.astro
  - src/components/RouteExplainer.astro
autonomous: false

must_haves:
  truths:
    - "With macOS Reduce Motion enabled, all scroll-driven animations are disabled and content appears statically"
    - "No animations fire above the fold — hero and first-visible content render without any entrance animation"
    - "Page initial transfer size is under 3MB"
    - "Build completes successfully and the dev server renders all sections correctly"
  artifacts:
    - path: "dist/index.html"
      provides: "Built page with all reveal attributes and animation CSS compiled"
      contains: "data-reveal"
  key_links:
    - from: "ScrollReveal.astro CSS"
      to: "prefers-reduced-motion media query"
      via: "CSS @media rule overriding opacity/transform to static values"
      pattern: "prefers-reduced-motion.*reduce"
---

<objective>
Validate that the scroll-driven animations from Plan 22-01 meet all accessibility and performance requirements.

Purpose: Ensure reduced-motion users see all content statically, no above-fold animations delay LCP, and the page stays within performance budgets. This is the final quality gate for v1.2.
Output: Verified compliance with ANI-03 (reduced motion) and ANI-04 (performance budget), plus any targeted fixes needed.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-animation-polish/22-RESEARCH.md
@.planning/phases/22-animation-polish/22-01-SUMMARY.md
@src/components/ScrollReveal.astro
@src/pages/index.astro
@src/components/AnimatedDivider.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify reduced-motion and above-fold correctness</name>
  <files>src/components/ScrollReveal.astro, src/pages/index.astro, src/components/RouteExplainer.astro</files>
  <action>
Run `npx astro build` and then inspect the built output to verify all Phase 22 requirements.

**Reduced-motion audit (ANI-03):**

1. Check that `dist/index.html` contains the `@media (prefers-reduced-motion: reduce)` rule that sets `[data-reveal] { opacity: 1; transform: none; transition: none; }`.
2. Check that `dist/index.html` contains the reduced-motion rule for `.segment-card` that sets `opacity: 1; transform: none; transition: none; transition-delay: 0ms;`.
3. Check that the ScrollReveal JS includes the `window.matchMedia('(prefers-reduced-motion: reduce)')` guard that skips observer setup entirely.
4. Check that AnimatedDivider's existing reduced-motion CSS (`@media (prefers-reduced-motion: reduce) { .vine-path { stroke-dashoffset: 0; transition: none; } .blossom-cycle, .berry-cycle { animation: none; } }`) is still present and unmodified.

**Above-fold audit (ANI-04 partial):**

5. Verify HeroSection does NOT have `data-reveal` attribute — search `dist/index.html` for the hero section markup and confirm no `data-reveal`.
6. Verify the first DonateCallout section does NOT have `data-reveal`.
7. Verify FloralDivider does NOT have `data-reveal`.
8. Verify the ScrollReveal JS contains the above-fold guard: `el.getBoundingClientRect().top < window.innerHeight` check that adds `is-visible` immediately without animation.

**Fix any issues found.** If all checks pass, no fixes needed — proceed to Task 2.

If any `data-reveal` attributes were accidentally placed on above-fold elements, remove them. If reduced-motion CSS is missing for any reveal target, add it. If the JS guard is missing, add it.
  </action>
  <verify>
Run `grep -c "prefers-reduced-motion" dist/index.html` — should return at least 4 (ScrollReveal global, segment-card, AnimatedDivider, gpx-download).
Run `npx astro build` passes with no errors.
Manually inspect the first ~50 lines of `<body>` in dist/index.html to confirm HeroSection has no data-reveal.
Run `du -sh dist/` and confirm total build output is well under 3MB (ANI-04 transfer size budget).
  </verify>
  <done>Reduced-motion media queries cover all animated elements (data-reveal sections, segment-card stagger, AnimatedDivider vine paths, blossom/berry cycles). Above-fold elements (hero, first donate, floral divider) have no data-reveal. JS guard prevents observer setup when motion is reduced. Build output transfer size confirmed under 3MB.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Scroll-driven section reveals, staggered segment card animations, and full reduced-motion accessibility for Phase 22 (Animation & Polish)</what-built>
  <how-to-verify>
1. Run `npx astro dev` and open http://localhost:4321 in a browser
2. Scroll down the page slowly:
   - Hero and first "Donate to MBTN" should be visible immediately (no animation)
   - HiawathaExplainer section should fade in and slide up as you scroll to it
   - RouteExplainer section should fade in, then its 7 segment cards should stagger in sequentially with visible delay
   - All remaining sections (Route Stats, GPX Download, Route Map, Elevation Profile, Photos, Support the Trail, Credits) should each fade in on scroll
   - AnimatedDividers should still draw their vine paths independently (not affected)
3. Test reduced motion:
   - macOS: System Settings > Accessibility > Display > Reduce Motion (enable)
   - OR Chrome DevTools: Rendering panel > Emulate CSS media feature > prefers-reduced-motion: reduce
   - Reload the page — ALL content should be visible immediately, no fading, no sliding, no staggering
4. Verify performance (ANI-04):
   - Page should feel fast on initial load
   - Hero image should appear quickly (not delayed by opacity: 0)
   - Check LCP: Open Chrome DevTools > Performance tab > reload with profiling > verify LCP is under 2.5s (look for the LCP marker in the timeline). Alternatively run `npx lighthouse http://localhost:4321 --only-categories=performance --output=json | grep -A2 largest-contentful-paint` to get the LCP value programmatically
   - Transfer size was verified in Task 1 via `du -sh dist/` (should be under 3MB)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx astro build` succeeds
2. Built dist/index.html contains prefers-reduced-motion rules for all animated elements
3. No above-fold elements have data-reveal
4. Visual inspection confirms animations work correctly
5. Visual inspection confirms reduced-motion disables all animations
6. Initial page load transfer stays under 3MB (Phase 22 adds <1KB CSS/JS)
</verification>

<success_criteria>
- ANI-03 verified: prefers-reduced-motion: reduce disables ALL scroll-driven animations (section reveals, card stagger, divider draw-on)
- ANI-04 verified: No above-fold animations, page transfer under 3MB, LCP not degraded by animation CSS
- Human verification approved
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/22-animation-polish/22-02-SUMMARY.md`
</output>
