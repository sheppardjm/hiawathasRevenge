---
phase: 54-route-start-relocation-data-regeneration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Munising_Hiawatha_s_Revenge.gpx
  - Hiawatha_s_Revenge_100k.gpx
  - Hiawatha_s_Revenge_50K_.gpx
  - scripts/match-photos.js
  - scripts/migrate-photos-mileage.js
  - public/data/photos-manifest.json
autonomous: true
requirements: [ROUTE-01, ROUTE-05]
must_haves:
  truths:
    - "The three repo-root GPX files start at 46.34770, -86.72515 (new staging point)"
    - "match-photos.js reads the per-route 100mi route-data.json path, not the stale legacy root path"
    - "All 56 photos-manifest.json mileage tags are shifted +1.59 mi (modulo 101.91) ahead of the pipeline run"
  artifacts:
    - path: "scripts/migrate-photos-mileage.js"
      provides: "One-shot photo mileage rotation migration"
      contains: "OFFSET_MILES"
      min_lines: 30
    - path: "Munising_Hiawatha_s_Revenge.gpx"
      provides: "100mi alt-start track (2676 pts)"
    - path: "public/data/photos-manifest.json"
      provides: "56 rotated photo mileage tags"
  key_links:
    - from: "scripts/match-photos.js"
      to: "public/data/100mi/route-data.json"
      via: "ROUTE_PATH constant"
      pattern: "'100mi', 'route-data.json'"
    - from: "scripts/migrate-photos-mileage.js"
      to: "public/data/photos-manifest.json"
      via: "readFileSync + writeFileSync"
      pattern: "photos-manifest.json"
---

<objective>
Prepare all pipeline inputs for the rotated course BEFORE the pipeline runs: swap in the three alt-start GPX tracks (ROUTE-01), fix the match-photos.js stale-path bug so post-pipeline photo snapping reads the correct per-route geometry, and run a one-shot migration that shifts every photo mileage tag in photos-manifest.json by the +1.59 mi rotation offset (ROUTE-05).

This plan deliberately does ALL pre-pipeline mutations. Per RESEARCH.md "Option A" sequencing, photos-manifest.json must be updated and the match-photos.js path bug fixed before `npm run pipeline` runs (plan 02), so the pipeline produces correct photos.json in a single pass with no double-run.

Purpose: A wrong input here (old GPX, stale match-photos path, or un-shifted manifest) silently corrupts every downstream derived file. Get the inputs right once.
Output: 3 replaced GPX files, corrected match-photos.js, new migrate-photos-mileage.js, rotated photos-manifest.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md
@.planning/phases/54-route-start-relocation-data-regeneration/54-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap in the three alt-start GPX tracks (in place, keep filenames)</name>
  <files>Munising_Hiawatha_s_Revenge.gpx, Hiawatha_s_Revenge_100k.gpx, Hiawatha_s_Revenge_50K_.gpx</files>
  <read_first>
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Source Files table; Pitfall 6 — do NOT use Hiawatha_100.gpx)
  </read_first>
  <action>
    Copy each alt-start source from ~/Downloads onto its existing repo-root target, keeping the EXACT existing filename so route-config.js gpxFile refs, index.astro download hrefs, and routes.json gpxFile fields need no change:
    - `~/Downloads/Hiawatha's Revenge 100 (alt start).gpx` (247017 bytes, 2676 pts) → repo-root `Munising_Hiawatha_s_Revenge.gpx`
    - `~/Downloads/Hiawatha's Revenge 100k (alt start).gpx` (148734 bytes, 1616 pts) → repo-root `Hiawatha_s_Revenge_100k.gpx`
    - `~/Downloads/Hiawatha's Revenge 50k (alt start).gpx` (72446 bytes, 787 pts) → repo-root `Hiawatha_s_Revenge_50K_.gpx`
    Do NOT touch `Hiawatha_100.gpx` at repo root — it is an unrelated OLD-start 22,992-pt Garmin recording and is out of scope. After copying, confirm the first `<trkpt>` of each new file is at lat 46.3477, lon -86.7251 (NOT the old 46.364xx). The source filenames in Downloads contain spaces and parentheses — quote the paths.
  </action>
  <verify>
    <automated>for f in Munising_Hiawatha_s_Revenge.gpx Hiawatha_s_Revenge_100k.gpx Hiawatha_s_Revenge_50K_.gpx; do grep -m1 -o 'lat="46\.3477[0-9]*"' "/Users/Sheppardjm/Repos/hiawathasRevenge/$f" || { echo "FAIL: $f first trkpt not at new start"; exit 1; }; done; echo "ALL THREE START AT NEW STAGING POINT"</automated>
  </verify>
  <acceptance_criteria>
    - The first `<trkpt>` lat in each of the three repo-root GPX files matches `46.3477` (verify command exits 0 and prints "ALL THREE START AT NEW STAGING POINT")
    - `Munising_Hiawatha_s_Revenge.gpx` byte size is ~247017 (alt-start), NOT ~164470 (old) and NOT ~7.2MB (Hiawatha_100.gpx)
    - `Hiawatha_100.gpx` is unchanged (still present, not used)
  </acceptance_criteria>
  <done>All three repo-root route GPX files contain the alt-start tracks starting at 46.3477, -86.7251, with original filenames preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Fix match-photos.js stale route-data path (line 23)</name>
  <files>scripts/match-photos.js</files>
  <read_first>
    - scripts/match-photos.js (lines 19-24 — the ROUTE_PATH constant block)
    - scripts/resolve-annotations.js (lines 45-46 — correct per-route path pattern)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Pitfall 1)
  </read_first>
  <action>
    On line 23 of scripts/match-photos.js, change the ROUTE_PATH constant so it resolves the per-route 100mi file instead of the stale legacy root file. Replace the path segments `'public', 'data', 'route-data.json'` with `'public', 'data', '100mi', 'route-data.json'` — adding the `'100mi'` directory segment. This is the only edit; do not change MANIFEST_PATH or OUTPUT_PATH on the surrounding lines. The corrected form matches the per-route construction used in resolve-annotations.js lines 45-46. This MUST land before the pipeline run (plan 02) or photo coordinates will snap to old-course geometry.
  </action>
  <verify>
    <automated>grep -q "'public', 'data', '100mi', 'route-data.json'" /Users/Sheppardjm/Repos/hiawathasRevenge/scripts/match-photos.js && ! grep -q "'public', 'data', 'route-data.json'" /Users/Sheppardjm/Repos/hiawathasRevenge/scripts/match-photos.js && echo "PATH FIXED"</automated>
  </verify>
  <acceptance_criteria>
    - `grep` finds `'public', 'data', '100mi', 'route-data.json'` in scripts/match-photos.js
    - The old broken form `'public', 'data', 'route-data.json'` (no 100mi segment) no longer appears
    - MANIFEST_PATH and OUTPUT_PATH lines are unchanged
  </acceptance_criteria>
  <done>match-photos.js ROUTE_PATH points at public/data/100mi/route-data.json (the pipeline-regenerated per-route file).</done>
</task>

<task type="auto">
  <name>Task 3: Create and run scripts/migrate-photos-mileage.js to rotate the 56 manifest mileages</name>
  <files>scripts/migrate-photos-mileage.js, public/data/photos-manifest.json</files>
  <read_first>
    - scripts/match-photos.js (lines 1-24, 102, 108-110 — JSDoc, imports, ESM __dirname, JSON read/write, summary-log house style)
    - scripts/generate-routes-manifest.js (lines 33-38 — try/catch JSON read with process.exit(1))
    - public/data/photos-manifest.json (entry schema: flat array of {filename, mile, ...})
    - .planning/phases/54-route-start-relocation-data-regeneration/54-PATTERNS.md (migrate-photos-mileage.js section — full synthesized structure)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Photo Mileage Migration; Pitfall 2)
  </read_first>
  <action>
    Create scripts/migrate-photos-mileage.js as a one-shot ESM migration following match-photos.js house style (JSDoc header, `fileURLToPath`/`dirname`/`resolve` ESM __dirname boilerplate, try/catch JSON read with process.exit(1) on failure, `JSON.stringify(updated, null, 2)` write, console summary log). Define `const OFFSET_MILES = 1.59` and `const NEW_TOTAL_MILES = 101.91`. The shift function adds OFFSET_MILES and applies modulo wrap ONLY when the result exceeds NEW_TOTAL_MILES (subtract NEW_TOTAL_MILES), then rounds to one decimal (`Math.round(newMile * 10) / 10`). Map over every entry preserving all other fields (spread `...entry`) and replace only `mile`. Write back to public/data/photos-manifest.json. Per RESEARCH.md/PATTERNS.md the 5 near-end photos (old miles 99.0/99.1/99.9/100.1/100.3) shift to 100.6/100.7/101.5/101.7/101.9 — all below 101.91, so the wrap branch fires for ZERO entries in this dataset. The migration MUST run before the pipeline (plan 02). Run it with the Volta node binary: `/Users/Sheppardjm/.volta/bin/node scripts/migrate-photos-mileage.js`.
  </action>
  <verify>
    <automated>cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/node scripts/migrate-photos-mileage.js && /Users/Sheppardjm/.volta/bin/node -e "const m=require('./public/data/photos-manifest.json'); if(m.length!==56){console.error('FAIL count '+m.length);process.exit(1);} const bad=m.filter(e=>e.mile>101.91||e.mile<0); if(bad.length){console.error('FAIL out-of-range',bad);process.exit(1);} console.log('OK '+m.length+' entries, max mile '+Math.max(...m.map(e=>e.mile)));"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/migrate-photos-mileage.js exists and contains `OFFSET_MILES` (= 1.59) and `NEW_TOTAL_MILES` (= 101.91)
    - Running the script exits 0 and logs entries migrated
    - photos-manifest.json still has exactly 56 entries after migration
    - No entry has `mile` > 101.91 or `mile` < 0 (verify command prints "OK 56 entries ...")
    - Max mile after migration is ~101.9 (the bhrt78... near-end photo), confirming no spurious modulo wrap to a low value
  </acceptance_criteria>
  <done>photos-manifest.json mileages are rotated +1.59 mi (no wrap fired); the migration script is committed for auditability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/Downloads → repo | GPX files copied from an outside-repo location into committed source; integrity of the copied track is the only correctness boundary |
| migration script → photos-manifest.json | In-place overwrite of committed data; risk is data corruption / loss, not external attack |

This is a static-site, build-time data-regeneration phase. There is no user input, no auth, no network surface, no secrets. Most STRIDE categories are N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-01 | Tampering | GPX swap (Task 1) | mitigate | First-trkpt coordinate assertion (46.3477) + byte-size sanity vs Hiawatha_100.gpx prevents using the wrong file |
| T-54-02 | Tampering | photos-manifest.json overwrite (Task 3) | mitigate | Entry-count (56) + range (0..101.91) assertions catch corruption; file is git-tracked so a bad write is recoverable via `git checkout` |
| T-54-03 | Denial of Service | Spoofing/Repudiation/Info-disclosure/Elevation | accept | N/A — no network, no auth, no PII, no privileged operations in a build-time data refresh |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — all pipeline deps committed since v1.0 (RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Task 1 verify command exits 0 (three GPX files start at 46.3477)
- Task 2 verify command exits 0 (ROUTE_PATH fixed, old form gone)
- Task 3 verify command exits 0 (56 entries, all in range, max ~101.9)
- No pipeline run in this plan — that happens in plan 02 after these inputs are correct
</verification>

<success_criteria>
- ROUTE-01: three repo-root route GPX files are the alt-start tracks starting at 46.34770, -86.72515
- ROUTE-05 (input half): match-photos.js reads the correct per-route path AND photos-manifest.json mileages are rotated, so the plan-02 pipeline run produces correct photos.json
</success_criteria>

<output>
Create `.planning/phases/54-route-start-relocation-data-regeneration/54-01-SUMMARY.md` when done. Record: confirmed first-trkpt coordinates per file, the migrate script's logged entry count, and the post-migration max mile value.
</output>
