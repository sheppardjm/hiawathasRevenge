---
phase: 54-route-start-relocation-data-regeneration
plan: 02
type: execute
wave: 2
depends_on: ["54-01"]
files_modified:
  - public/data/100mi/route-data.json
  - public/data/100k/route-data.json
  - public/data/50k/route-data.json
  - public/data/100mi/annotations.json
  - public/data/100k/annotations.json
  - public/data/50k/annotations.json
  - public/data/100mi/sector-elevations.json
  - public/data/100k/sector-elevations.json
  - public/data/50k/sector-elevations.json
  - public/data/routes.json
  - public/data/sector-details.json
  - public/data/photos.json
  - public/Munising_Hiawatha_s_Revenge.gpx
  - public/Hiawatha_s_Revenge_100k.gpx
  - public/Hiawatha_s_Revenge_50K_.gpx
  - scripts/route-config.js
autonomous: true
requirements: [ROUTE-02, ROUTE-03, ROUTE-04, ROUTE-06, ROUTE-07]
must_haves:
  truths:
    - "Pipeline runs end-to-end with no errors on all three rotated tracks"
    - "Gravel sectors re-snap: 8 on 100mi, 5 on 100k, 4 on 50k, all with startIdx < endIdx"
    - "Restock mileages (Camp 7, Midway) are re-derived along the new course in annotations.json"
    - "Per-route distance and elevation gain fall in expected ranges; 100mi elevation scan hits IN RANGE (or route-config elevationTargetRange is corrected)"
    - "Served public/*.gpx downloads are the new alt-start tracks starting at 46.3477"
    - "Astro build completes with no content-schema or TypeScript errors"
  artifacts:
    - path: "public/data/routes.json"
      provides: "Aggregated per-route stats (distance, elevation gain) for selector/sidebar"
    - path: "public/data/100mi/annotations.json"
      provides: "Re-snapped 100mi sectors + restock mileages"
      contains: "restock-camp7"
    - path: "public/data/photos.json"
      provides: "Photo map markers snapped to new course geometry"
    - path: "public/Munising_Hiawatha_s_Revenge.gpx"
      provides: "Served 100mi download (alt-start)"
  key_links:
    - from: "public/data/100mi/annotations.json"
      to: "new 100mi course geometry"
      via: "coordinate haversine snap in resolve-annotations.js"
      pattern: "restock-(camp7|midway)"
    - from: "public/Munising_Hiawatha_s_Revenge.gpx"
      to: "repo-root Munising_Hiawatha_s_Revenge.gpx"
      via: "copy-gpx.js"
      pattern: "lat=\"46\\.3477"
---

<objective>
Regenerate every piece of derived data for the rotated course by running the build pipeline once on the inputs prepared in plan 01, then verify the pipeline's built-in validation log gates and do post-pipeline housekeeping (restock comment values + a conditional elevation-range correction). Finishes with a full `npm run build` smoke test.

Covers ROUTE-02 (pipeline end-to-end), ROUTE-03 (sector re-snap 8/5/4), ROUTE-04 (restock re-derivation), ROUTE-06 (per-route stats + elevationTargetRange), and ROUTE-07 (served GPX downloads). Sector snapping and restock mileages are fully automatic once the new GPX is in place — this plan VERIFIES them via the pipeline's own log gates rather than re-implementing any math.

Depends on 54-01: requires the alt-start GPX in repo root, the match-photos.js path fix, and the rotated photos-manifest.json all in place before the pipeline runs.

Purpose: Produce accurate, build-validated derived data ahead of the June 6 ride.
Output: 12 regenerated JSON files, 3 served GPX copies, optional route-config.js edits, green build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md
@.planning/phases/54-route-start-relocation-data-regeneration/54-PATTERNS.md
@.planning/phases/54-route-start-relocation-data-regeneration/54-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the pipeline and assert all validation log gates pass</name>
  <files>public/data/100mi/route-data.json, public/data/100k/route-data.json, public/data/50k/route-data.json, public/data/100mi/annotations.json, public/data/100k/annotations.json, public/data/50k/annotations.json, public/data/100mi/sector-elevations.json, public/data/100k/sector-elevations.json, public/data/50k/sector-elevations.json, public/data/routes.json, public/data/sector-details.json, public/data/photos.json, public/Munising_Hiawatha_s_Revenge.gpx, public/Hiawatha_s_Revenge_100k.gpx, public/Hiawatha_s_Revenge_50K_.gpx</files>
  <read_first>
    - scripts/pipeline.js (11-step order)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Build Smoke Test success indicators; Pitfalls 3, 5, 6)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-01-SUMMARY.md (confirm plan-01 inputs landed)
  </read_first>
  <action>
    Run the pipeline once, capturing the full log, using the Volta node binary required by this project (Node >=22.12.0): from the repo root run `/Users/Sheppardjm/.volta/bin/node scripts/pipeline.js` and tee the output to a temp log. Then assert the RESEARCH.md "Build Smoke Test" gates appear in the log:
    - 100mi parse shows `2676 raw track points` (confirms the alt-start file, NOT the 22,992-pt Hiawatha_100.gpx — Pitfall 6)
    - 100mi total distance ~101.9 miles
    - exactly one 100mi elevation threshold scan line annotated `IN RANGE` (Pitfall 3 — capture this result for Task 2's conditional edit)
    - sector counts: 100mi = 8, 100k = 5, 50k = 4 (ROUTE-03)
    - `Index check: all startIdx < endIdx - OK` appears once per route (Pitfall 5)
    - pipeline reports completion (`all data files generated`)
    Do NOT edit any pipeline script. If a gate is missing, stop and report which gate failed rather than patching around it. Also confirm copy-gpx.js wrote the three public/*.gpx (ROUTE-07) by checking each served file's first trkpt is at 46.3477.
  </action>
  <verify>
    <automated>cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/node scripts/pipeline.js 2>&1 | tee /tmp/54-pipeline.log; PIPE=${PIPESTATUS[0]}; test $PIPE -eq 0 || { echo "FAIL: pipeline exit $PIPE"; exit 1; }; grep -q "2676 raw track points" /tmp/54-pipeline.log || { echo "FAIL: wrong 100mi point count"; exit 1; }; test $(grep -c "Index check: all startIdx < endIdx - OK" /tmp/54-pipeline.log) -eq 3 || { echo "FAIL: index check not 3x"; exit 1; }; for f in Munising_Hiawatha_s_Revenge.gpx Hiawatha_s_Revenge_100k.gpx Hiawatha_s_Revenge_50K_.gpx; do grep -m1 -q 'lat="46\.3477' "public/$f" || { echo "FAIL: served $f not alt-start"; exit 1; }; done; echo "PIPELINE GATES PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Pipeline exits 0 (verify command prints "PIPELINE GATES PASS")
    - Log contains `2676 raw track points` for 100mi (not 22992, not 1927)
    - Log shows `Index check: all startIdx < endIdx - OK` exactly 3 times
    - Log shows sector counts 8 (100mi), 5 (100k), 4 (50k) — record exact lines in SUMMARY
    - Record in SUMMARY whether `IN RANGE` appeared for the 100mi elevation scan, and the chosen threshold/gain values (drives Task 2)
    - All three public/*.gpx served files have first trkpt at 46.3477 (ROUTE-07)
  </acceptance_criteria>
  <done>All pipeline-derived JSON regenerated, sector/index/point-count gates pass, and served GPX downloads are the alt-start tracks.</done>
</task>

<task type="auto">
  <name>Task 2: Post-pipeline housekeeping — restock comments + conditional elevation range</name>
  <files>scripts/route-config.js</files>
  <read_first>
    - scripts/route-config.js (RESTOCK_DEFS mile comment values for camp7/midway; line 30 elevationTargetRange [2123, 2411])
    - public/data/100mi/annotations.json (post-pipeline restock-camp7 / restock-midway snapped mile values)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Restock Mileage Re-derivation; elevationTargetRange Validation Strategy)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-PATTERNS.md (route-config.js conditional edit section)
  </read_first>
  <action>
    Two edits to scripts/route-config.js, both informed by the live pipeline output (NOT guessed):
    1. RESTOCK comment re-derivation (ROUTE-04 housekeeping): read the snapped `mile` values for restock-camp7 and restock-midway from public/data/100mi/annotations.json, and update the cosmetic `mile` comment values in RESTOCK_DEFS to match (research expects roughly camp7 ~46.2, midway ~77.5; use the actual annotations.json values). These fields are not read by the pipeline — they are documentation, so this is a comment/value sync only.
    2. CONDITIONAL elevationTargetRange (ROUTE-06): only if Task 1's log showed NO `IN RANGE` for the 100mi elevation scan, update the `elevationTargetRange: [2123, 2411]` array on line 30 to bracket the threshold gain closest to the expected ~2267 ft midpoint (preserve the trailing comment format). If `IN RANGE` DID appear, make NO change to elevationTargetRange and note "no change needed" in the SUMMARY.
    After any route-config.js edit, re-run the pipeline once more ONLY if elevationTargetRange changed (so 100mi elevation re-derives against the new bounds); if only restock comments changed, no re-run is needed since those values are not consumed.
  </action>
  <verify>
    <automated>cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/node -e "const a=require('./public/data/100mi/annotations.json'); const s=JSON.stringify(a); if(!/restock-camp7/.test(s)||!/restock-midway/.test(s)){console.error('FAIL: restocks missing from 100mi annotations');process.exit(1);} const r=require('./public/data/routes.json'); console.log('routes.json present, restocks snapped OK');"</automated>
  </verify>
  <acceptance_criteria>
    - public/data/100mi/annotations.json contains both `restock-camp7` and `restock-midway` with snapped mile values along the new course (ROUTE-04)
    - RESTOCK_DEFS comment mile values in route-config.js match the annotations.json snapped values
    - elevationTargetRange is either unchanged (because IN RANGE appeared) OR updated to bracket the closest threshold gain — SUMMARY states which branch was taken
    - If elevationTargetRange changed, the pipeline was re-run and IN RANGE now appears
  </acceptance_criteria>
  <done>Restock comments synced to live snapped mileages and elevationTargetRange validated/corrected against the new 100mi track.</done>
</task>

<task type="auto">
  <name>Task 3: Full build smoke test</name>
  <files>public/data/routes.json</files>
  <read_first>
    - package.json (build script; prebuild runs the pipeline)
    - .planning/phases/54-route-start-relocation-data-regeneration/54-RESEARCH.md (Build Smoke Test — astro build must complete cleanly)
  </read_first>
  <action>
    Run a full `npm run build` from the repo root. The prebuild hook re-runs the pipeline, so the build exercises the entire chain end-to-end against the new data. Confirm the build completes with no TypeScript errors and no Astro content-collection schema errors (routes.json / per-route route-data.json / annotations.json / sector-elevations.json / photos.json all parse against their content schemas). This is the final ROUTE-02 gate. Use the project's normal npm build (Volta provides the node version automatically via the repo's toolchain).
  </action>
  <verify>
    <automated>cd /Users/Sheppardjm/Repos/hiawathasRevenge && npm run build 2>&1 | tail -40; test ${PIPESTATUS[0]} -eq 0 && echo "BUILD GREEN" || { echo "FAIL: build error"; exit 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (verify command prints "BUILD GREEN")
    - No Astro content-schema validation errors in the build output
    - No TypeScript errors in the build output
  </acceptance_criteria>
  <done>The full static build succeeds against the regenerated data — phase data is shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pipeline scripts → public/data/*.json | Build-time overwrite of committed derived data; correctness (not external attack) is the risk |
| copy-gpx.js → public/*.gpx | Served download artifacts overwritten; wrong source = users download wrong track |

Build-time data-regeneration phase. No user input, auth, network surface, or secrets. Most STRIDE categories are N/A.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04 | Tampering | Wrong GPX consumed by pipeline (Pitfall 6) | mitigate | Task 1 asserts `2676 raw track points` so the 22,992-pt Hiawatha_100.gpx can never silently feed the pipeline |
| T-54-05 | Tampering | Silent elevation/sector miscalibration | mitigate | Task 1 asserts sector counts (8/5/4) and 3x index-check; Task 2 verifies/ corrects elevationTargetRange against logged IN RANGE |
| T-54-06 | Tampering | Served download serves stale/old-start GPX (ROUTE-07) | mitigate | Task 1 asserts each public/*.gpx first trkpt is at 46.3477 |
| T-54-07 | Spoofing/Repudiation/Info-disclosure/DoS/Elevation | — | accept | N/A — no network, auth, PII, or privileged operations |
| T-54-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all pipeline deps committed since v1.0 (RESEARCH.md Package Legitimacy Audit) |
</threat_model>

<verification>
- Task 1: pipeline exits 0; point count, 3x index check, sector counts, served-GPX coords all assert (ROUTE-02/03/07)
- Task 2: 100mi annotations.json has both restocks; route-config restock comments + elevation range reconciled (ROUTE-04/06)
- Task 3: `npm run build` exits 0 with no schema/TS errors (ROUTE-02 final gate)
</verification>

<success_criteria>
- ROUTE-02: pipeline runs end-to-end and `npm run build` is green
- ROUTE-03: 8/5/4 sectors snapped with valid index ordering on every route
- ROUTE-04: Camp 7 + Midway restock mileages re-derived in annotations.json; route-config comments synced
- ROUTE-06: per-route stats in routes.json; 100mi elevation IN RANGE or elevationTargetRange corrected
- ROUTE-07: served public/*.gpx are the alt-start tracks starting at 46.3477
</success_criteria>

<output>
Create `.planning/phases/54-route-start-relocation-data-regeneration/54-02-SUMMARY.md` when done. Record: 100mi point count + total miles, the three sector-count lines, IN RANGE branch (yes / no + chosen threshold), snapped Camp 7 / Midway mileages, whether elevationTargetRange changed, and final build status.
</output>
