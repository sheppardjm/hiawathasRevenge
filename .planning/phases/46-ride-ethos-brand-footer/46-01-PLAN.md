---
phase: 46-ride-ethos-brand-footer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/images/neucadia-logo.png
  - src/components/RideEthos.astro
  - src/components/NeucadiaFooter.astro
  - src/pages/index.astro
  - src/layouts/BaseLayout.astro
autonomous: true

must_haves:
  truths:
    - "An ethos section with four kicker statements (founding date, always free, fellowship, all levels) appears between the StickyNav and the gold DonateCallout section"
    - "The ethos statement uses display font at large size with amber-500 color — visually distinct from body prose (mono font at base size)"
    - "A 'Powered by Neucadia' footer line with an inline logo appears at the very bottom of the page, below all main content"
    - "The Neucadia logo is served from a local /images/neucadia-logo.png asset (not an external URL) and has explicit width/height attributes preventing layout shift"
    - "The Neucadia logo links to neucadia.com in a new tab with rel='noopener noreferrer'"
  artifacts:
    - path: "src/components/RideEthos.astro"
      provides: "Compact declarative ethos statement with four kicker items"
      min_lines: 40
    - path: "src/components/NeucadiaFooter.astro"
      provides: "Footer line with Powered by text and local Neucadia logo"
      min_lines: 30
    - path: "public/images/neucadia-logo.png"
      provides: "Local copy of Neucadia wordmark logo (283x42 RGBA PNG)"
    - path: "src/pages/index.astro"
      provides: "RideEthos wired between StickyNav and gold-section"
      contains: "RideEthos"
    - path: "src/layouts/BaseLayout.astro"
      provides: "NeucadiaFooter placed after </main> as body-level footer"
      contains: "NeucadiaFooter"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/RideEthos.astro"
      via: "Astro component import and placement after StickyNav, before gold-section"
      pattern: "import RideEthos"
    - from: "src/layouts/BaseLayout.astro"
      to: "src/components/NeucadiaFooter.astro"
      via: "Astro component import and placement after </main>"
      pattern: "import NeucadiaFooter"
    - from: "src/components/NeucadiaFooter.astro"
      to: "public/images/neucadia-logo.png"
      via: "img src attribute referencing local asset"
      pattern: 'src="/images/neucadia-logo.png"'
---

<objective>
Create two new Astro components — RideEthos (compact declarative ethos statement) and NeucadiaFooter ("Powered by Neucadia" footer with local logo) — and wire them into the page. Download the Neucadia logo as a local asset.

Purpose: Communicate the ride's identity (free, fellowship, all levels, since 2014) at the top of the page and add brand attribution at the bottom.
Output: RideEthos.astro, NeucadiaFooter.astro, neucadia-logo.png local asset, wiring in index.astro and BaseLayout.astro.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-ride-ethos-brand-footer/46-RESEARCH.md
@src/pages/index.astro
@src/layouts/BaseLayout.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Download Neucadia logo and create RideEthos + NeucadiaFooter components</name>
  <files>
    public/images/neucadia-logo.png
    src/components/RideEthos.astro
    src/components/NeucadiaFooter.astro
  </files>
  <action>
    **Step 1: Download Neucadia logo**

    ```bash
    curl -o public/images/neucadia-logo.png https://neucadia.com/assets/neucadia_logo.png
    ```

    Verify the file is a valid PNG (should be ~5KB, 283x42 RGBA):
    ```bash
    file public/images/neucadia-logo.png
    ```

    **Step 2: Create `src/components/RideEthos.astro`**

    Build a compact declarative statement block with four kicker items. Use the EXACT component code from the research file (Pattern 1 / Code Examples section), which includes:

    - HTML: `<section class="ride-ethos" aria-label="About this ride">` with `<ul class="ethos-list">` containing 4 `<li class="ethos-item">` elements
    - Four items:
      1. Value: "Since 2014" / Label: "Founded June 7, 2014"
      2. Value: "Always Free" / Label: "No entry fee, ever"
      3. Value: "Fellowship" / Label: "Not a race"
      4. Value: "All Levels" / Label: "50K · 100K · 100 Miles"
    - Scoped CSS with:
      - `.ride-ethos`: `background-color: var(--color-forest-900); border-top/bottom: 1px solid var(--color-forest-700); padding: 2rem 1rem;`
      - `.ethos-value`: `font-family: var(--font-display); font-size: var(--font-size-2xl); font-weight: 700; color: var(--color-amber-500); text-transform: uppercase; letter-spacing: 0.08em; line-height: 1.2;`
      - `.ethos-label`: `font-family: var(--font-mono); font-size: var(--font-size-xs); color: var(--color-cream-200);`
      - `.ethos-list`: `display: flex; flex-wrap: wrap; justify-content: center; gap: 1.5rem 3rem;`
      - Mobile breakpoint at 480px reducing gap and font-size

    IMPORTANT: Use `<span>` elements for values and labels, NOT heading elements (h2/h3), to avoid inheriting global heading text-shadow styles.
    IMPORTANT: Do NOT add `data-reveal` — the ethos block is near the top and should be always visible.
    IMPORTANT: Do NOT modify `@theme static` in global.css — use only existing tokens.

    **Step 3: Create `src/components/NeucadiaFooter.astro`**

    Build a footer with "Powered by" text and inline Neucadia logo. Use the EXACT component code from the research file (Pattern 2 / Code Examples section), which includes:

    - HTML: `<footer class="neucadia-footer">` with `<a>` linking to neucadia.com
    - `target="_blank" rel="noopener noreferrer"` on the link (project convention from DonateCallout.astro)
    - `aria-label="Powered by Neucadia (opens in new tab)"` on the link
    - `<img src="/images/neucadia-logo.png" alt="Neucadia" width="142" height="21" loading="lazy" decoding="async">`
    - IMPORTANT: Explicit `width="142" height="21"` attributes are REQUIRED for CLS prevention (50% scale of 283x42 native)
    - Scoped CSS with:
      - `.neucadia-footer`: `width: 100%; padding: 0.875rem 1rem; background-color: var(--color-forest-950); border-top: 1px solid var(--color-forest-700); display: flex; justify-content: center;`
      - `.neucadia-link`: `opacity: 0.7; transition: opacity 0.2s;` with `:hover { opacity: 1; }`
      - `.neucadia-text`: `font-family: var(--font-mono); font-size: var(--font-size-xs); color: var(--color-cream-200); text-transform: uppercase; letter-spacing: 0.1em;`
      - `@media (prefers-reduced-motion: reduce)` disabling transition
  </action>
  <verify>
    - Logo file exists and is a PNG: `file public/images/neucadia-logo.png`
    - RideEthos exists: `ls src/components/RideEthos.astro`
    - RideEthos has 4 items: `grep -c "ethos-item" src/components/RideEthos.astro` returns 4 or more
    - RideEthos uses existing tokens only: `grep "font-display\|font-size-2xl\|color-amber-500" src/components/RideEthos.astro`
    - RideEthos does NOT use heading elements: `grep -c "<h[1-6]" src/components/RideEthos.astro` returns 0
    - NeucadiaFooter exists: `ls src/components/NeucadiaFooter.astro`
    - NeucadiaFooter has explicit dimensions: `grep 'width="142" height="21"' src/components/NeucadiaFooter.astro`
    - NeucadiaFooter uses local asset: `grep 'src="/images/neucadia-logo.png"' src/components/NeucadiaFooter.astro`
    - NeucadiaFooter has rel noopener: `grep 'rel="noopener noreferrer"' src/components/NeucadiaFooter.astro`
    - NeucadiaFooter has prefers-reduced-motion: `grep "prefers-reduced-motion" src/components/NeucadiaFooter.astro`
  </verify>
  <done>
    Neucadia logo PNG downloaded to public/images/neucadia-logo.png. RideEthos.astro exists with 4 kicker items using display font + amber color (no heading elements, no data-reveal). NeucadiaFooter.astro exists with local logo image, explicit width/height for CLS prevention, external link with noopener, and reduced-motion guard.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire RideEthos into index.astro and NeucadiaFooter into BaseLayout.astro</name>
  <files>
    src/pages/index.astro
    src/layouts/BaseLayout.astro
  </files>
  <action>
    **Step 1: Wire RideEthos into index.astro**

    1. Add import in the frontmatter block (after the StickyNav import on line 18):
       ```
       import RideEthos from '../components/RideEthos.astro';
       ```

    2. Place `<RideEthos />` immediately after `<StickyNav />` and before the gold-section. The template should read:
       ```astro
       <StickyNav />
       <RideEthos />

       <section class="gold-section w-full py-16">
       ```

    IMPORTANT: Do NOT add `data-reveal` to RideEthos placement — it must be always visible.
    IMPORTANT: Do NOT modify any existing sections, styles, or scripts.

    **Step 2: Wire NeucadiaFooter into BaseLayout.astro**

    1. Add import in the frontmatter block (after the existing imports):
       ```
       import NeucadiaFooter from '../components/NeucadiaFooter.astro';
       ```

    2. Place `<NeucadiaFooter />` AFTER the closing `</main>` tag and BEFORE `</body>`. The semantic structure should be:
       ```astro
       <main>
         <slot />
       </main>
       <NeucadiaFooter />
       </body>
       ```

    This places the footer as a body-level sibling to `<main>`, which is semantically correct — brand attribution is not main content. The NeucadiaFooter.astro component uses a `<footer>` root element, providing proper landmark navigation for screen readers.

    **Step 3: Build verification**

    Run `npm run build` to confirm the site builds without errors.
  </action>
  <verify>
    - RideEthos imported: `grep "import RideEthos" src/pages/index.astro`
    - RideEthos placed after StickyNav: `grep -A1 "StickyNav" src/pages/index.astro | grep "RideEthos"`
    - NeucadiaFooter imported: `grep "import NeucadiaFooter" src/layouts/BaseLayout.astro`
    - NeucadiaFooter placed after main: `grep -A1 "</main>" src/layouts/BaseLayout.astro | grep "NeucadiaFooter"`
    - Build succeeds: `npm run build`
  </verify>
  <done>
    RideEthos imported and placed between StickyNav and gold-section in index.astro. NeucadiaFooter imported and placed after </main> in BaseLayout.astro as a body-level footer. Astro build succeeds.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase success criteria:

1. `file public/images/neucadia-logo.png` confirms valid PNG file
2. `grep -c "ethos-item" src/components/RideEthos.astro` returns >= 4 (four kicker items)
3. `grep "font-size-2xl" src/components/RideEthos.astro` confirms large display text
4. `grep "color-amber-500" src/components/RideEthos.astro` confirms amber color
5. `grep -c "<h[1-6]" src/components/RideEthos.astro` returns 0 (no heading elements)
6. `grep 'width="142" height="21"' src/components/NeucadiaFooter.astro` confirms CLS prevention
7. `grep 'src="/images/neucadia-logo.png"' src/components/NeucadiaFooter.astro` confirms local asset
8. `grep 'rel="noopener noreferrer"' src/components/NeucadiaFooter.astro` confirms safe external link
9. `grep "import RideEthos" src/pages/index.astro` confirms wiring
10. `grep "import NeucadiaFooter" src/layouts/BaseLayout.astro` confirms wiring
11. `npm run build` succeeds with no errors
</verification>

<success_criteria>
- RideEthos.astro exists with 4 declarative kicker items (Since 2014, Always Free, Fellowship, All Levels)
- Ethos values use --font-display at --font-size-2xl with --color-amber-500 — visually distinct from body prose
- Ethos labels use --font-mono at --font-size-xs with --color-cream-200
- RideEthos placed between StickyNav and gold-section in index.astro (ETHOS-02)
- No heading elements used in RideEthos (avoids global text-shadow inheritance)
- No data-reveal on RideEthos (always visible near top of page)
- NeucadiaFooter.astro exists with "Powered by" text and local logo image
- Logo served from /images/neucadia-logo.png (local asset, not external URL) (FOOT-02)
- Logo has explicit width="142" height="21" for CLS prevention
- Logo linked to neucadia.com with target="_blank" rel="noopener noreferrer"
- NeucadiaFooter placed after </main> in BaseLayout.astro as body-level <footer>
- prefers-reduced-motion guard on NeucadiaFooter transitions
- No modifications to @theme static in global.css
- Astro build completes successfully
</success_criteria>

<output>
After completion, create `.planning/phases/46-ride-ethos-brand-footer/46-01-SUMMARY.md`
</output>
