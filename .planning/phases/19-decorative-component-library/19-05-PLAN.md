---
phase: 19-decorative-component-library
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pages/index.astro
  - src/components/RouteExplainer.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "AnimatedDivider is imported and rendered at least once on the page"
    - "ShieldMotif is imported and rendered at least once on the page"
    - "ElevationSparkline is imported and rendered at least once on the page, showing a real sector's elevation profile"
    - "Build passes cleanly with zero errors"
  artifacts:
    - path: "src/pages/index.astro"
      provides: "Imports and renders AnimatedDivider and ShieldMotif"
      contains: "AnimatedDivider"
    - path: "src/components/RouteExplainer.astro"
      provides: "Imports and renders ElevationSparkline for each segment"
      contains: "ElevationSparkline"
  key_links:
    - from: "src/pages/index.astro"
      to: "src/components/AnimatedDivider.astro"
      via: "import AnimatedDivider"
      pattern: "import AnimatedDivider"
    - from: "src/pages/index.astro"
      to: "src/components/ShieldMotif.astro"
      via: "import ShieldMotif"
      pattern: "import ShieldMotif"
    - from: "src/components/RouteExplainer.astro"
      to: "src/components/ElevationSparkline.astro"
      via: "import ElevationSparkline"
      pattern: "import ElevationSparkline"
---

<objective>
Wire all three decorative components (AnimatedDivider, ShieldMotif, ElevationSparkline) into the actual page so they are no longer orphaned, validating that the component library pattern works in a real rendering context.

Purpose: Closes Gap 2 from 19-VERIFICATION.md — all three components are substantive and correct in isolation but have zero usage anywhere in src/, meaning the "can be dropped into any section" claim is unvalidated
Output: Updated src/pages/index.astro (AnimatedDivider + ShieldMotif usage) and src/components/RouteExplainer.astro (ElevationSparkline usage)
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/19-decorative-component-library/19-VERIFICATION.md
@src/pages/index.astro
@src/components/RouteExplainer.astro
@src/components/AnimatedDivider.astro
@src/components/ShieldMotif.astro
@src/components/ElevationSparkline.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AnimatedDivider and ShieldMotif to index.astro</name>
  <files>src/pages/index.astro</files>
  <action>
  Make three changes to `src/pages/index.astro`:

  **Change 1: Add imports**

  Add these two imports to the existing import block in the frontmatter (after the FloralDivider import):

  ```astro
  import AnimatedDivider from '../components/AnimatedDivider.astro';
  import ShieldMotif from '../components/ShieldMotif.astro';
  ```

  **Change 2: Replace one FloralDivider with AnimatedDivider**

  Replace the SECOND `<FloralDivider />` instance (the one between the GPX download section and the "Explore the Route" section, around line 45) with:

  ```html
  <AnimatedDivider variant="berry" />
  ```

  Keep the first `<FloralDivider />` (between the DonateCallout section and HiawathaExplainer) as-is — this demonstrates both static and animated dividers coexisting.

  **Change 3: Add ShieldMotif to the footer**

  In the final footer section (the one with "A project supporting MBTN"), add a ShieldMotif as a decorative mark. Insert it immediately before the `<p class="text-sm text-cream-200">` element:

  ```html
  <ShieldMotif size={16} class="text-amber-500 mb-3 opacity-60" />
  ```

  This places a small, subtle shield icon above the footer text as a decorative brand mark.

  **Critical details:**
  - Only replace the SECOND FloralDivider, not the first — this preserves the existing static divider as contrast
  - The AnimatedDivider uses variant="berry" for visual variety (the floral variant is very similar to FloralDivider)
  - ShieldMotif size={16} produces a 16x32px icon (1:2 aspect ratio) — small and subtle for footer use
  - ShieldMotif with no `label` prop renders as decorative (aria-hidden="true", role="presentation") — correct for this ornamental usage
  - Do NOT remove the FloralDivider import since the first instance still uses it
  - Do NOT modify any other sections, components, or styles
  </action>
  <verify>
  - `grep "import AnimatedDivider" src/pages/index.astro` returns a match
  - `grep "import ShieldMotif" src/pages/index.astro` returns a match
  - `grep "AnimatedDivider" src/pages/index.astro | grep -v import` returns a match (usage in template)
  - `grep "ShieldMotif" src/pages/index.astro | grep -v import` returns a match (usage in template)
  - `grep -c "FloralDivider" src/pages/index.astro` returns at least 2 (1 import + 1 remaining usage)
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - AnimatedDivider imported and rendered once (variant="berry") replacing the second FloralDivider
  - ShieldMotif imported and rendered once as a decorative footer mark
  - First FloralDivider preserved — both static and animated dividers coexist
  - Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Add ElevationSparkline to RouteExplainer segment cards</name>
  <files>src/components/RouteExplainer.astro</files>
  <action>
  Make three changes to `src/components/RouteExplainer.astro`:

  **Change 1: Add import and sector ID mapping**

  Add the ElevationSparkline import to the frontmatter, after the existing `photosData` import:

  ```astro
  import ElevationSparkline from './ElevationSparkline.astro';
  ```

  Add a sector ID lookup map after the `SEGMENTS` array definition (before the `segmentsWithPhotos` computation). This maps segment names to sector-elevation IDs:

  ```typescript
  const SECTOR_IDS: Record<string, string> = {
    '520': 'sector-520',
    'NF2266': 'sector-nf2266',
    'Bass Lake Rd': 'sector-bass-lake',
    'NF2217-2218': 'sector-nf2217',
    'ND2225': 'sector-nd2225',
    'Doe Lake': 'sector-doe-lake',
    'Rapid River Truck Trail': 'sector-rapid-river',
  };
  ```

  **Change 2: Add ElevationSparkline to segment card template**

  Inside the `.segment-content` div in the map callback, add an ElevationSparkline after the description paragraph. Insert this immediately after the `<p class="text-cream-100 leading-relaxed">{seg.description}</p>` element:

  ```html
  {SECTOR_IDS[seg.name] && (
    <div class="mt-3">
      <ElevationSparkline sectorId={SECTOR_IDS[seg.name]} class="opacity-60" />
    </div>
  )}
  ```

  **Change 3: Add sparkline container styling**

  No additional CSS needed — the sparkline renders as a 100%-width SVG that naturally fills the `.segment-content` column, and the `opacity-60` class via Tailwind plus `mt-3` for spacing is sufficient.

  **Critical details:**
  - The SECTOR_IDS map uses the exact segment `name` values from the SEGMENTS array as keys, and the exact `id` values from sector-elevations.json as values
  - `NF2217-2218` maps to `sector-nf2217` (the sector ID uses the shorter form)
  - The `{SECTOR_IDS[seg.name] && ...}` conditional ensures graceful degradation if a segment has no matching sector data
  - `opacity-60` keeps the sparkline subtle — it's a visual accent, not a primary data display
  - ElevationSparkline is server-rendered (zero client JS) so it adds no runtime cost
  - The sparkline appears INSIDE the segment-content area, not as a separate grid area — this keeps the existing grid layout intact
  </action>
  <verify>
  - `grep "import ElevationSparkline" src/components/RouteExplainer.astro` returns a match
  - `grep "ElevationSparkline" src/components/RouteExplainer.astro | grep -v import` returns a match (usage in template)
  - `grep "SECTOR_IDS" src/components/RouteExplainer.astro` returns a match
  - `grep "sector-520" src/components/RouteExplainer.astro` returns a match
  - `grep "sector-nf2266" src/components/RouteExplainer.astro` returns a match
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - ElevationSparkline imported and rendered inside each segment card that has a matching sector ID
  - SECTOR_IDS map correctly maps all 7 segment names to their sector-elevation IDs
  - Sparklines render as subtle elevation profiles below each segment description
  - Graceful fallback for any segment without sector data
  - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `grep "import AnimatedDivider" src/pages/index.astro` returns a match
2. `grep "import ShieldMotif" src/pages/index.astro` returns a match
3. `grep "import ElevationSparkline" src/components/RouteExplainer.astro` returns a match
4. `grep -c "AnimatedDivider\|ShieldMotif" src/pages/index.astro` returns >= 4 (2 imports + 2 usages)
5. `grep -c "ElevationSparkline" src/components/RouteExplainer.astro` returns >= 2 (1 import + 1 usage)
6. `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
</verification>

<success_criteria>
- AnimatedDivider is imported and rendered on the index page (no longer orphaned)
- ShieldMotif is imported and rendered on the index page (no longer orphaned)
- ElevationSparkline is imported and rendered in RouteExplainer for each segment (no longer orphaned)
- All three components render in a real page context and build succeeds
- Existing page layout and functionality are preserved
</success_criteria>

<output>
After completion, create `.planning/phases/19-decorative-component-library/19-05-SUMMARY.md`
</output>
