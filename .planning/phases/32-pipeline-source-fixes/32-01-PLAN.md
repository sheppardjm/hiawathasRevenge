---
phase: 32-pipeline-source-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/resolve-annotations.js
  - scripts/pipeline.js
  - public/data/annotations.json
  - public/data/sector-details.json
  - public/data/sector-elevations.json
  - public/og-image.jpg
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "resolve-annotations.js source code defines NF2217-2218 (not NF2217) for the sector-nf2217 entry"
    - "npm run pipeline completes successfully with no errors"
    - "All three data files contain NF2217-2218 with zero occurrences of bare NF2217"
    - "pipeline.js includes generate-og-image step and produces public/og-image.jpg"
  artifacts:
    - path: "scripts/resolve-annotations.js"
      provides: "Corrected NF2217-2218 sector name in GRAVEL_SECTORS source"
      contains: "name: 'NF2217-2218'"
    - path: "scripts/pipeline.js"
      provides: "generate-og-image step in pipeline"
      contains: "generate-og-image"
    - path: "public/data/annotations.json"
      provides: "Regenerated annotations with NF2217-2218"
      contains: "NF2217-2218"
    - path: "public/data/sector-details.json"
      provides: "Regenerated sector details with NF2217-2218"
      contains: "NF2217-2218"
    - path: "public/data/sector-elevations.json"
      provides: "Regenerated sector elevations with NF2217-2218"
      contains: "NF2217-2218"
    - path: "public/og-image.jpg"
      provides: "Pipeline-generated OG image"
  key_links:
    - from: "scripts/resolve-annotations.js"
      to: "public/data/annotations.json"
      via: "GRAVEL_SECTORS name field flows to annotation output"
      pattern: "name: 'NF2217-2218'"
    - from: "scripts/pipeline.js"
      to: "scripts/generate-og-image.js"
      via: "steps array entry triggers OG image generation"
      pattern: "generate-og-image.*generate-og-image\\.js"
---

<objective>
Fix two pipeline source gaps so that `npm run pipeline` produces correct output without manual intervention.

Purpose: Close the DEBT-02 regression (NF2217 hardcoded in resolve-annotations.js reverts the NF2217-2218 fix on every pipeline run) and wire generate-og-image.js into the pipeline (script exists but is not called).

Output: Two source file edits and a verified clean pipeline run proving all data files are correct.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@scripts/resolve-annotations.js
@scripts/pipeline.js
@scripts/generate-og-image.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix NF2217 source name and wire OG image into pipeline</name>
  <files>scripts/resolve-annotations.js, scripts/pipeline.js</files>
  <action>
Two targeted edits:

1. In `scripts/resolve-annotations.js` line 26, change the GRAVEL_SECTORS entry for sector-nf2217 from:
   `{ id: 'sector-nf2217',      name: 'NF2217',                  startMile: 36.8, lengthMiles: 6.6, difficulty: 'moderate', stars: 2 },`
   to:
   `{ id: 'sector-nf2217',      name: 'NF2217-2218',             startMile: 36.8, lengthMiles: 6.6, difficulty: 'moderate', stars: 2 },`

   The sector ID (`sector-nf2217`) stays unchanged -- only the display name changes. This ensures downstream scripts (generate-sector-details.js, compute-sector-elevations.js) propagate the correct name.

2. In `scripts/pipeline.js`, add the generate-og-image step to the steps array. Insert it after the `copy-gpx` step (last current entry) as it has no data dependencies on other steps and logically belongs at the end:
   `{ name: 'generate-og-image', script: 'scripts/generate-og-image.js' },`

   The steps array should end with:
   ```
   { name: 'match-photos', script: 'scripts/match-photos.js' },
   { name: 'copy-gpx', script: 'scripts/copy-gpx.js' },
   { name: 'generate-og-image', script: 'scripts/generate-og-image.js' },
   ```
  </action>
  <verify>
Verify source edits:
- `grep "NF2217-2218" scripts/resolve-annotations.js` returns exactly 1 match
- `grep "NF2217" scripts/resolve-annotations.js | grep -v "NF2217-2218"` returns only the `id: 'sector-nf2217'` line (no bare NF2217 in the name field)
- `grep "generate-og-image" scripts/pipeline.js` returns the new step entry
  </verify>
  <done>
resolve-annotations.js source defines NF2217-2218 for the sector-nf2217 entry. pipeline.js includes generate-og-image as its final step.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run pipeline and verify all outputs</name>
  <files>public/data/annotations.json, public/data/sector-details.json, public/data/sector-elevations.json, public/og-image.jpg</files>
  <action>
Run the full pipeline using Volta-managed Node to regenerate all data files:

```
/Users/Sheppardjm/.volta/bin/node scripts/pipeline.js
```

After the pipeline completes, verify all four success criteria:

1. Check annotations.json contains NF2217-2218 and no bare NF2217 in name fields:
   `grep "NF2217" public/data/annotations.json`

2. Check sector-details.json contains NF2217-2218 and no bare NF2217 in name fields:
   `grep "NF2217" public/data/sector-details.json`

3. Check sector-elevations.json contains NF2217-2218 and no bare NF2217 in name fields:
   `grep "NF2217" public/data/sector-elevations.json`

4. Verify og-image.jpg exists and is a valid JPEG:
   `file public/og-image.jpg` should report JPEG image data
   `ls -la public/og-image.jpg` should show reasonable file size (50-200KB)

If any check fails, diagnose and fix before marking complete.
  </action>
  <verify>
All five commands must pass:
- `grep -c "NF2217-2218" public/data/annotations.json` returns 1
- `grep -c "NF2217-2218" public/data/sector-details.json` returns 1
- `grep -c "NF2217-2218" public/data/sector-elevations.json` returns 1
- `grep "NF2217" public/data/annotations.json public/data/sector-details.json public/data/sector-elevations.json | grep -v "NF2217-2218" | grep -v "sector-nf2217"` returns empty (no bare NF2217 except in the id field)
- `file public/og-image.jpg | grep -q JPEG && echo OK` returns OK
  </verify>
  <done>
Pipeline runs clean. All three data files contain NF2217-2218 with no bare NF2217 name references. public/og-image.jpg is a valid JPEG generated by the pipeline. All four phase success criteria are satisfied.
  </done>
</task>

</tasks>

<verification>
Phase 32 success criteria (all must be TRUE):
1. `scripts/resolve-annotations.js` outputs `"name": "NF2217-2218"` for the NF2217 sector
2. After `npm run pipeline`, all three data files (annotations.json, sector-details.json, sector-elevations.json) contain `"NF2217-2218"` with no bare `"NF2217"`
3. `scripts/pipeline.js` includes a `generate-og-image` step that produces `public/og-image.jpg`
4. A clean pipeline run from scratch produces all expected outputs including the OG image
</verification>

<success_criteria>
- resolve-annotations.js GRAVEL_SECTORS array has `name: 'NF2217-2218'` for sector-nf2217
- pipeline.js steps array includes `{ name: 'generate-og-image', script: 'scripts/generate-og-image.js' }`
- Full pipeline run completes with exit code 0
- NF2217-2218 appears in all three JSON data files; bare NF2217 does not appear in any name field
- public/og-image.jpg exists as a valid JPEG after pipeline run
</success_criteria>

<output>
After completion, create `.planning/phases/32-pipeline-source-fixes/32-01-SUMMARY.md`
</output>
