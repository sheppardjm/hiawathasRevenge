---
phase: 47-history-light-dark-mode
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/components/HiawathaExplainer.astro]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "History section body text is dark (forest-900) on cream background in light mode"
    - "Pull-quote text is dark (forest-900) in light mode"
    - "Pull-quote attribution and cite link are dark (forest-800) in light mode"
    - "Dark mode text remains cream-100 on forest-950 — no regressions"
  artifacts:
    - path: "src/components/HiawathaExplainer.astro"
      provides: "Light-mode @media block at END of <style> tag, after all default rules"
      contains: "@media (prefers-color-scheme: light)"
  key_links:
    - from: "@media (prefers-color-scheme: light) .editorial-grid p"
      to: ".editorial-grid p { color: cream-100 } default rule"
      via: "CSS source order — light-mode AFTER default means light-mode wins at equal specificity"
      pattern: "prefers-color-scheme.*light.*editorial-grid p.*forest-900"
---

<objective>
Fix light-mode text readability by moving the `@media (prefers-color-scheme: light)` CSS block to the END of the `<style>` tag in HiawathaExplainer.astro.

Purpose: The light-mode overrides at lines 265-337 are placed BEFORE the default dark-mode rules they need to override (lines 353+). Both selectors have equal specificity, so the later (default) rules always win — making light-mode text unreadable (cream on cream). Moving the light-mode block after all defaults fixes source order.

Output: Readable dark text on cream background in History section when OS is set to light mode.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-history-light-dark-mode/47-01-SUMMARY.md
@.planning/phases/v1.8-UAT.md
@.planning/debug/history-light-mode-text.md
@src/components/HiawathaExplainer.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move light-mode @media block to end of style tag</name>
  <files>src/components/HiawathaExplainer.astro</files>
  <action>
    In `src/components/HiawathaExplainer.astro`, restructure the CSS source order inside the `<style>` tag:

    1. **CUT** the entire `@media (prefers-color-scheme: light) { ... }` block (currently at approximately lines 265-337, starting with the comment `/* Light-mode overrides */` and ending with the closing `}` of the media query).

    2. **PASTE** this block at the very END of the `<style>` tag — after all other rules including the responsive `@media (max-width: 899px)` and `@media (max-width: 640px)` blocks.

    3. The final source order inside `<style>` should be:
       - `.hiawatha-section` (section background)
       - `.editorial-grid` (grid layout)
       - `.subsection-bg` (sub-section grid + position: relative)
       - `.subsection-bg::before` (background image system)
       - `.subsection-bg.bg-visible::before` (scroll-triggered opacity)
       - Per-section `::before` image URLs (poem, forest, ride)
       - `@media (prefers-reduced-motion: reduce)` (static backgrounds)
       - `.section-heading` rules
       - `.editorial-grid p` (default: color cream-100) -- THIS MUST COME BEFORE the light-mode block
       - `.museum-plate` rules
       - `.two-col-moment` rules
       - `.pull-quote` rules (default: color cream-100/cream-200)
       - `.drop-cap` rules
       - `@media (max-width: 899px)` responsive
       - `@media (max-width: 640px)` responsive
       - `@media (prefers-color-scheme: light)` -- LAST, so it overrides all defaults above

    4. Do NOT change any selectors or property values inside the light-mode block. The fix is purely source order.

    5. Do NOT touch the `<script>` block or any HTML.

    **Why this works:** When two CSS rules have identical specificity, the one that appears LATER in source order wins. By placing the light-mode media query after all default rules, the light-mode colors (forest-900 for body text, forest-800 for attribution) correctly override the dark-mode defaults (cream-100, cream-200) when the media query matches.
  </action>
  <verify>
    Run: `cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/npx astro check 2>&1 | tail -5`
    Run: `grep -n "prefers-color-scheme" src/components/HiawathaExplainer.astro` — the light-mode block should appear on a HIGHER line number than `editorial-grid p { color: var(--color-cream-100)` default.
    Run: `grep -n "color.*cream-100" src/components/HiawathaExplainer.astro` and `grep -n "color.*forest-900" src/components/HiawathaExplainer.astro` — verify forest-900 rules appear AFTER cream-100 rules in source order.
  </verify>
  <done>
    The `@media (prefers-color-scheme: light)` block is the last major block in the `<style>` tag. All four affected selectors (`.editorial-grid p`, `.pull-quote p`, `.pull-quote-attribution`, `.pull-quote-attribution cite a`) now appear in light-mode AFTER their default-color counterparts, ensuring light-mode wins at equal specificity. Build passes with no errors.
  </done>
</task>

</tasks>

<verification>
1. `npx astro check` passes (no TypeScript/template errors)
2. `npx astro build` succeeds (no build failures)
3. In the built CSS output, the `prefers-color-scheme: light` block appears after the default `.editorial-grid p { color: cream-100 }` rule
4. Dark mode: History section text remains cream-100 on forest-950 (no regression)
5. Light mode: History section body text is forest-900, pull-quote text is forest-900, attribution is forest-800
</verification>

<success_criteria>
- Light-mode `@media` block is last major block in `<style>` tag (after all responsive breakpoints)
- No CSS selectors or values were modified — only source order changed
- `astro build` passes cleanly
- User sees dark readable text on cream background when OS is set to light mode
</success_criteria>

<output>
After completion, create `.planning/phases/47-history-light-dark-mode/47-02-SUMMARY.md`
</output>
