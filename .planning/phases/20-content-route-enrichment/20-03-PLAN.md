---
phase: 20-content-route-enrichment
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteExplainer.astro
autonomous: true

must_haves:
  truths:
    - "Each segment subheading has a ShieldMotif icon prefix and difficulty-coded color (sun-400 easy, amber-500 moderate, scarlet-400 hard)"
    - "Each segment has a conditional Strava link that renders only when stravaId is present"
    - "Each segment description includes surface type, key landmarks, and seasonal notes"
    - "Build passes cleanly with zero errors"
  artifacts:
    - path: "src/components/RouteExplainer.astro"
      provides: "Enriched segment cards with shield icons, difficulty colors, Strava links, and expanded descriptions"
      contains: "DIFFICULTY_COLORS"
  key_links:
    - from: "src/components/RouteExplainer.astro"
      to: "src/components/ShieldMotif.astro"
      via: "import ShieldMotif"
      pattern: "import ShieldMotif"
    - from: "src/components/RouteExplainer.astro"
      to: "strava.com/segments"
      via: "conditional Strava outbound link"
      pattern: "strava.com/segments"
---

<objective>
Enrich RouteExplainer.astro segment cards with ShieldMotif icon prefixes on subheadings, difficulty-coded heading colors, conditional Strava segment links, and expanded terrain descriptions including surface type, landmarks, and seasonal notes.

Purpose: Transforms the route segment section from a simple data listing into a rich, informative editorial guide that helps riders plan their effort — each segment becomes a mini-narrative with visual difficulty coding, terrain intel, and direct Strava integration.
Output: Updated RouteExplainer.astro with enriched segment data, ShieldMotif import, DIFFICULTY_COLORS record, Strava link pattern, and expanded descriptions.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-content-route-enrichment/20-RESEARCH.md
@src/components/RouteExplainer.astro
@src/components/ShieldMotif.astro
@src/components/ElevationSparkline.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ShieldMotif, difficulty colors, and Strava links to segment cards</name>
  <files>src/components/RouteExplainer.astro</files>
  <action>
  Make five changes to `src/components/RouteExplainer.astro`:

  **Change 1: Add ShieldMotif import**

  Add this import to the frontmatter, after the existing `ElevationSparkline` import:

  ```typescript
  import ShieldMotif from './ShieldMotif.astro';
  ```

  **Change 2: Add stravaId to Segment interface and DIFFICULTY_COLORS record**

  Update the `Segment` interface to add an optional `stravaId` field:

  ```typescript
  interface Segment {
    name: string;
    startMi: number;
    endMi: number;
    distFromStart: string;
    length: string;
    difficulty: number;
    description: string;
    stravaId?: string;
  }
  ```

  Add a `DIFFICULTY_COLORS` record after the `SEGMENTS` array (before `SECTOR_IDS`):

  ```typescript
  const DIFFICULTY_COLORS: Record<number, string> = {
    1: 'text-sun-400',      // easy — sun-yellow 9.46:1 contrast, text-safe
    2: 'text-sun-400',      // easy
    3: 'text-amber-500',    // moderate — amber primary accent
    4: 'text-scarlet-400',  // hard — scarlet-400 5.24:1 contrast, text-safe
    5: 'text-scarlet-400',  // hard
  };
  ```

  **Change 3: Replace segment heading with ShieldMotif + difficulty-coded h3**

  Replace the existing `<h3 class="text-xl font-display text-amber-400">{seg.name}</h3>` in the template with:

  ```html
  <div class="flex items-center gap-2">
    <ShieldMotif size={10} class={DIFFICULTY_COLORS[seg.difficulty] ?? 'text-amber-500'} />
    <h3 class={`text-xl font-display ${DIFFICULTY_COLORS[seg.difficulty] ?? 'text-amber-500'}`}>
      {seg.name}
    </h3>
  </div>
  ```

  This renders a small shield icon (10x20px) alongside each segment name, both colored by difficulty level:
  - Difficulty 1-2 (easy): sun-400 (gold-yellow)
  - Difficulty 3 (moderate): amber-500 (warm amber)
  - Difficulty 4-5 (hard): scarlet-400 (red)

  **Change 4: Add conditional Strava link after the metadata line**

  After the existing metadata `<div>` (the one with `distFromStart`, `·`, `length`, `·`, star rating), add a conditional Strava link:

  ```html
  {seg.stravaId && (
    <a
      href={`https://www.strava.com/segments/${seg.stravaId}`}
      class="strava-link"
      target="_blank"
      rel="noopener noreferrer"
    >
      View on Strava
      <svg class="strava-icon" viewBox="0 0 16 16" width="12" height="12" aria-hidden="true" focusable="false">
        <path fill="currentColor" d="M3 1.5L1.5 3 11.09 3l-8.59 8.59L3.91 13 12.5 4.41 12.5 14 14.5 14 14.5 1.5z" />
      </svg>
    </a>
  )}
  ```

  The SVG is a simple external-link arrow icon (not the Strava logo — avoids brand asset questions). The link text "View on Strava" follows Strava branding guidelines.

  **Change 5: Add Strava link CSS to the `<style>` block**

  Add these styles to the existing `<style>` block:

  ```css
  .strava-link {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: var(--font-size-sm);
    color: #FC5200;
    text-decoration: none;
    margin-top: 0.5rem;
    transition: opacity 0.2s;
  }

  .strava-link:hover {
    opacity: 0.8;
    text-decoration: underline;
  }

  .strava-icon {
    flex-shrink: 0;
  }

  @media (prefers-reduced-motion: reduce) {
    .strava-link {
      transition: none;
    }
  }
  ```

  The Strava link uses `#FC5200` (Strava's brand orange) as the text color. This is hardcoded rather than using a design token because it is a third-party brand color.

  **Critical details:**
  - Do NOT use `text-amber-400` on the h3 anymore — it is replaced by the difficulty-coded color from `DIFFICULTY_COLORS`
  - The `?? 'text-amber-500'` fallback handles any unexpected difficulty values gracefully
  - ShieldMotif at `size={10}` renders as 10x20px — small enough to sit inline with the h3 text
  - All `stravaId` fields in SEGMENTS are currently `undefined` (not set) — the user will provide IDs during implementation. The conditional `{seg.stravaId && ...}` prevents rendering broken links.
  - The `target="_blank"` MUST be paired with `rel="noopener noreferrer"` for security
  - Do NOT use moss-500 for any heading text — it fails AA on dark backgrounds
  - Do NOT use scarlet-600 — use scarlet-400 (5.24:1 contrast, text-safe)
  </action>
  <verify>
  - `grep "import ShieldMotif" src/components/RouteExplainer.astro` returns a match
  - `grep "DIFFICULTY_COLORS" src/components/RouteExplainer.astro` returns a match
  - `grep "text-sun-400" src/components/RouteExplainer.astro` returns a match
  - `grep "text-scarlet-400" src/components/RouteExplainer.astro` returns a match
  - `grep "strava.com/segments" src/components/RouteExplainer.astro` returns a match
  - `grep "noopener noreferrer" src/components/RouteExplainer.astro` returns a match
  - `grep "stravaId" src/components/RouteExplainer.astro` returns a match
  - `grep "FC5200" src/components/RouteExplainer.astro` returns a match
  - `grep -v "text-amber-400" src/components/RouteExplainer.astro | wc -l` confirms text-amber-400 is no longer used on h3 elements
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - Segment subheadings have ShieldMotif icon prefix and difficulty-coded color
  - DIFFICULTY_COLORS record maps 1-5 to sun-400/amber-500/scarlet-400
  - Strava link pattern in place with conditional rendering, noopener noreferrer, and Strava orange
  - All stravaId fields are undefined (user provides IDs later)
  - Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Expand segment descriptions with surface type, landmarks, and seasonal notes</name>
  <files>src/components/RouteExplainer.astro</files>
  <action>
  Update the `description` field for all 7 segments in the `SEGMENTS` array. Each description must include: (1) the original editorial prose (preserved or enhanced), (2) surface type, (3) key landmarks, and (4) seasonal notes. Maintain the witty New Yorker editorial voice established in v1.1.

  Replace each segment's `description` value with the expanded version below:

  **520 (difficulty 2):**
  ```
  A brief paved warm-up along County Road 520 east out of Munising — flat, fast, and deceptively civilized for what lies ahead. Surface: smooth asphalt for the full stretch. You'll pass the Munising Falls trailhead and the Pictured Rocks visitor center before the pavement runs out. In spring, snowmelt can leave sand drifts at the road shoulders; by midsummer, this is a pleasant cruise.
  ```

  **NF2266 (difficulty 5):**
  ```
  The route's crucible. Deep sand, washboard ruts, and relentless climbs through old-growth hardwoods on a barely maintained Forest Service road. This is where the ride earns its name. Surface: deteriorating sand and gravel two-track, often rutted from logging traffic. Landmarks include the junction with Forest Road 2262 and a seasonal creek crossing that can run shin-deep in May. In spring, this road is a mudpit; in fall, the hardwood canopy explodes with color but the sand never relents.
  ```

  **Bass Lake Rd (difficulty 2):**
  ```
  Welcome relief after NF2266. A well-graded gravel road rolling past Bass Lake with broad sight lines and room to spin your legs back to life. Surface: packed gravel, county-maintained. Bass Lake itself appears through the birch trees around mile 28 — a good photo stop. Watch for seasonal soft spots after rain, but this stretch is generally the most forgiving gravel on the route.
  ```

  **NF2217-2218 (difficulty 2):**
  ```
  A long, meditative stretch through the forest interior. Smooth gravel threading between Camp 7 Lake and the headwaters of the Indian River — good surface for making up time. Surface: compact gravel and hard-packed dirt, dual forest road. Camp 7 Lake Campground at mile 44.7 has a water pump and vault toilet — your first restock point. In July and August, this corridor buzzes with horseflies; early morning or late season is kinder.
  ```

  **ND2225 (difficulty 3):**
  ```
  The route bends south and the surface degrades — loose gravel and intermittent sand demand attention. Quiet and remote, with no services for miles in either direction. Surface: loose gravel transitioning to sandy patches, unmaintained in stretches. You'll cross the Indian River headwaters and pass through stands of jack pine and red pine. Fall color peaks here in late September; spring thaw turns the low-lying sections to pudding.
  ```

  **Doe Lake (difficulty 4):**
  ```
  Technical and punishing. Sandy two-track weaves through dense forest near Doe Lake with short, sharp climbs that hit different at mile 85. The Midway General Store at mile 75.7 is your last chance to restock — food, water, bathrooms, and precious cell service. Surface: deep sand and rugged two-track, roots and embedded rock in spots. Doe Lake appears briefly through the trees to the east. By October the sand firms up somewhat, but summer heat on this stretch is relentless.
  ```

  **Rapid River Truck Trail (difficulty 2):**
  ```
  The home stretch. A well-maintained truck trail carries you north back toward Munising with long descents and the growing certainty that you're going to finish this thing. Surface: firm packed gravel, USFS-maintained truck trail. You'll cross the Rapid River and catch glimpses of Lake Superior as you descend toward town. In late September, the aspens along this corridor turn gold — a fitting reward for surviving the previous 94 miles.
  ```

  **Critical details:**
  - Maintain the exact same editorial voice — witty, vivid, slightly wry New Yorker tone
  - Each expanded description is 3-4 sentences (up from 1-2 in the original)
  - Surface type is always introduced with "Surface:" for scannable consistency
  - Landmarks use specific names (Bass Lake, Camp 7 Lake, Indian River, Doe Lake, Rapid River, Lake Superior)
  - Seasonal notes reference specific months or seasons (May, July-August, late September, etc.)
  - Do NOT change any other fields (name, startMi, endMi, distFromStart, length, difficulty)
  - Do NOT add stravaId values — leave them undefined or omit the field entirely
  </action>
  <verify>
  - `grep "Surface:" src/components/RouteExplainer.astro | wc -l` returns 7 (one per segment)
  - `grep "Bass Lake" src/components/RouteExplainer.astro` returns a match (landmark)
  - `grep "Camp 7 Lake" src/components/RouteExplainer.astro` returns a match (landmark)
  - `grep "Indian River" src/components/RouteExplainer.astro` returns a match (landmark)
  - `grep "Rapid River" src/components/RouteExplainer.astro` returns a match (landmark)
  - `grep "September\|October\|spring\|summer\|fall\|May\|July\|August" src/components/RouteExplainer.astro | wc -l` returns >= 7 (seasonal notes)
  - `grep "asphalt\|gravel\|sand\|two-track\|truck trail" src/components/RouteExplainer.astro | wc -l` returns >= 7 (surface types)
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - All 7 segment descriptions expanded with surface type, landmarks, and seasonal notes
  - Consistent "Surface:" label for scannable reading
  - Specific landmark names and seasonal timing throughout
  - Editorial voice maintained — witty, vivid, New Yorker tone
  - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `grep "import ShieldMotif" src/components/RouteExplainer.astro` returns a match
2. `grep "DIFFICULTY_COLORS" src/components/RouteExplainer.astro` returns a match
3. `grep "strava.com/segments" src/components/RouteExplainer.astro` returns a match
4. `grep "Surface:" src/components/RouteExplainer.astro | wc -l` returns 7
5. `grep "text-sun-400" src/components/RouteExplainer.astro` returns a match
6. `grep "text-scarlet-400" src/components/RouteExplainer.astro` returns a match
7. `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
</verification>

<success_criteria>
- Every segment subheading has a ShieldMotif icon and difficulty-coded color (sun-400, amber-500, or scarlet-400)
- Strava link mechanism is in place with conditional rendering — no broken links when IDs are absent
- All 7 segment descriptions include surface type, key landmarks, and seasonal notes
- WCAG color constraints respected: no scarlet-600 or moss-500 on heading text
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/20-content-route-enrichment/20-03-SUMMARY.md`
</output>
