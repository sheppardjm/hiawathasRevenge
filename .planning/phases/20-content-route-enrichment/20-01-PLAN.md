---
phase: 20-content-route-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - images/historical/remington-hiawatha-departure-1891.jpg
  - images/historical/remington-hiawatha-fasting-1891.jpg
  - public/data/historical-manifest.json
  - src/components/HiawathaExplainer.astro
autonomous: true

must_haves:
  truths:
    - "HiawathaExplainer has 4 subheadings (The Poem, The Confusion, The Forest, The Ride) in National Park typeface with 3+ distinct colors"
    - "At least 2 historical Hiawatha illustrations appear between narrative paragraphs with figcaption attribution and sepia treatment"
    - "Historical illustrations are visually distinct from route photography via CSS sepia/desaturated filter"
    - "6-8rem whitespace separates major editorial sections"
  artifacts:
    - path: "images/historical/remington-hiawatha-departure-1891.jpg"
      provides: "First historical source image for pipeline processing"
    - path: "images/historical/remington-hiawatha-fasting-1891.jpg"
      provides: "Second historical source image for pipeline processing"
    - path: "public/data/historical-manifest.json"
      provides: "Manifest entries for 2 historical images with title, artist, year, source, license"
      contains: "Frederic Remington"
    - path: "src/components/HiawathaExplainer.astro"
      provides: "Editorial restructure with subheadings, historical image breaks, and sepia CSS"
      contains: "font-display"
  key_links:
    - from: "public/data/historical-manifest.json"
      to: "images/historical/"
      via: "filename field references source images"
      pattern: "remington-hiawatha"
    - from: "src/components/HiawathaExplainer.astro"
      to: "public/data/historical-photos.json"
      via: "import for historical image data"
      pattern: "historical-photos"
---

<objective>
Source 2 public domain Frederic Remington Hiawatha illustrations, populate the historical image manifest, run the pipeline, and restructure HiawathaExplainer.astro with editorial subheadings in National Park typeface (4 sections, 3+ colors) and historical image breaks with sepia treatment and figcaption attribution.

Purpose: Transforms the HiawathaExplainer from a single unbroken prose block into an award-winning editorial feature with visual rhythm, distinct narrative beats, and historical imagery that grounds the content in 19th-century Americana.
Output: 2 historical images in pipeline, populated manifest, restructured HiawathaExplainer.astro with subheadings, image breaks, and sepia CSS.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/20-content-route-enrichment/20-RESEARCH.md
@src/components/HiawathaExplainer.astro
@scripts/process-historical.js
@public/data/historical-manifest.json
@public/data/historical-photos.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Source historical images and populate manifest</name>
  <files>images/historical/remington-hiawatha-departure-1891.jpg, images/historical/remington-hiawatha-fasting-1891.jpg, public/data/historical-manifest.json</files>
  <action>
  Download 2 public domain Frederic Remington illustrations from the 1891 Houghton Mifflin photogravure edition of *The Song of Hiawatha*. These are grisaille oil paintings, firmly public domain (pre-1928, artist died 1909).

  **Image 1 — "Hiawatha's Departure"**

  Use WebFetch to download from Wikimedia Commons. Search for Frederic Remington Hiawatha illustrations on `commons.wikimedia.org`. Look for the illustration titled "Hiawatha's Departure" or "Hiawatha's Friends" or similar from the 1891 edition. The file should be a JPEG or PNG.

  If the exact Wikimedia image cannot be located via URL, use WebSearch to find `Frederic Remington "Song of Hiawatha" 1891 illustration wikimedia` and follow the most promising result.

  Save to: `images/historical/remington-hiawatha-departure-1891.jpg`

  **Image 2 — "Hiawatha's Fasting"**

  Download a second distinct Remington illustration from the same 1891 edition. Look for "Hiawatha's Fasting" or "Hiawatha and Mudjekeewis" or any other illustration from the set.

  Save to: `images/historical/remington-hiawatha-fasting-1891.jpg`

  **Fallback if specific Wikimedia URLs are not found:** Search Internet Archive for the 1891 Houghton Mifflin edition at `archive.org`. The digitized pages contain the illustrations as scanned JPEGs. Download 2 distinct illustration pages.

  **If neither Wikimedia nor Internet Archive yields downloadable images:** As a last resort, download ANY 2 public domain Hiawatha illustrations (pre-1928). The filenames and manifest can be adjusted — the important thing is having 2 real image files in `images/historical/`.

  **Populate the manifest:**

  Write `public/data/historical-manifest.json` with entries for both images:

  ```json
  [
    {
      "filename": "remington-hiawatha-departure-1891.jpg",
      "category": "historical",
      "title": "Hiawatha's Departure",
      "artist": "Frederic Remington",
      "year": "1891",
      "source": "Wikimedia Commons / Public Domain",
      "license": "Public Domain"
    },
    {
      "filename": "remington-hiawatha-fasting-1891.jpg",
      "category": "historical",
      "title": "Hiawatha's Fasting",
      "artist": "Frederic Remington",
      "year": "1891",
      "source": "Wikimedia Commons / Public Domain",
      "license": "Public Domain"
    }
  ]
  ```

  Adjust `title` and `source` fields to match actual images sourced. The `filename` must match the actual file saved to `images/historical/`.

  **Run the pipeline to process images:**

  ```bash
  PATH="/Users/Sheppardjm/.volta/bin:$PATH" node scripts/process-historical.js
  ```

  This generates WebP thumbnails in `public/thumbs/historical/` and writes `public/data/historical-photos.json` with `thumb` paths.

  **Critical details:**
  - Files MUST be valid image files (JPEG, PNG, TIFF, or WebP) — the sharp pipeline will error on corrupted files
  - The `category` field MUST be `"historical"` — the pipeline validates this and skips non-historical entries
  - Filenames in the manifest MUST exactly match the filenames on disk in `images/historical/`
  - After running the pipeline, `public/data/historical-photos.json` should contain 2 entries with `thumb` paths
  </action>
  <verify>
  - `ls images/historical/ | wc -l` returns 2 (two image files exist)
  - `cat public/data/historical-manifest.json | grep -c '"category": "historical"'` returns 2
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" node scripts/process-historical.js` exits 0
  - `cat public/data/historical-photos.json | grep -c '"thumb"'` returns 2
  - `ls public/thumbs/historical/ | wc -l` returns 2 (WebP thumbnails generated)
  </verify>
  <done>
  - 2 public domain historical Hiawatha illustrations exist in images/historical/
  - historical-manifest.json has 2 valid entries with title, artist, year, source, license
  - Pipeline processes both images into WebP thumbnails
  - historical-photos.json has 2 entries with thumb paths
  </done>
</task>

<task type="auto">
  <name>Task 2: Restructure HiawathaExplainer with subheadings and historical image breaks</name>
  <files>src/components/HiawathaExplainer.astro</files>
  <action>
  Rewrite `src/components/HiawathaExplainer.astro` to add editorial subheadings, historical image breaks, and sepia CSS.

  **Change 1: Add frontmatter import**

  Add this import at the top of the frontmatter (after the opening `---`):

  ```typescript
  import historicalPhotos from '../../public/data/historical-photos.json';
  ```

  **Change 2: Restructure prose into 4 narrative sections with subheadings**

  The existing 5 paragraphs + 1 blockquote map to 4 narrative beats. Insert `<h3>` subheadings to break the prose into sections. Each subheading uses `font-display` (National Park typeface) with a distinct color:

  - **"The Poem"** before paragraph 1 (Longfellow published in 1855...)
    - Class: `text-2xl font-display text-amber-500 mt-0 mb-4 tracking-wide`
    - Note: `mt-0` because it's the first subheading after the main h2

  - **"The Confusion"** before paragraph 2 (Hiawatha was a real historical figure...)
    - Class: `text-2xl font-display text-turquoise-400 mt-[6rem] mb-4 tracking-wide`

  - **"The Forest"** before paragraph 4 (None of this diminished the poem's popularity...)
    - Class: `text-2xl font-display text-sun-400 mt-[6rem] mb-4 tracking-wide`

  - **"The Ride"** before paragraph 5 (Into this magnificent, improbably-named landscape...)
    - Class: `text-2xl font-display text-scarlet-400 mt-[6rem] mb-4 tracking-wide`

  This gives 4 distinct colors: amber-500, turquoise-400, sun-400, scarlet-400 — all WCAG AA text-safe on forest-900/950 backgrounds.

  **Change 3: Insert historical image breaks between sections**

  Insert 2 `<figure>` elements between narrative sections, using data from `historicalPhotos`:

  - **Image 1:** Between "The Confusion" section and "The Forest" section (after the blockquote, before paragraph 4). This corresponds to `historicalPhotos[0]`.

  - **Image 2:** Between "The Forest" section and "The Ride" section (after paragraph 4, before paragraph 5). This corresponds to `historicalPhotos[1]`.

  Each image break uses conditional rendering:

  ```html
  {historicalPhotos.length > 0 && (
    <figure class="historical-break">
      <img
        src={historicalPhotos[0].thumb}
        alt={`${historicalPhotos[0].title} by ${historicalPhotos[0].artist}, ${historicalPhotos[0].year}`}
        class="historical-img"
        loading="lazy"
        decoding="async"
      />
      <figcaption class="historical-caption">
        <em>{historicalPhotos[0].title}</em> — {historicalPhotos[0].artist}, {historicalPhotos[0].year}. {historicalPhotos[0].source}
      </figcaption>
    </figure>
  )}
  ```

  For the second image, use `historicalPhotos.length > 1` and index `[1]`.

  **Change 4: Add component `<style>` block**

  Add a `<style>` block at the end of the component with these styles:

  ```css
  .historical-break {
    margin: 6rem auto;
    text-align: center;
    max-width: 640px;
  }

  .historical-img {
    filter: sepia(80%) saturate(30%) brightness(0.9);
    width: 100%;
    max-width: 640px;
    margin: 0 auto;
    display: block;
    border-radius: 0.25rem;
  }

  .historical-caption {
    font-size: var(--font-size-sm);
    color: var(--color-cream-200);
    margin-top: 0.75rem;
    font-style: italic;
  }
  ```

  **The final structure of the template should be:**

  ```
  <section>
    <h2>The Poem, the Forest, the Ride</h2>
    <div class="max-w-prose space-y-4 ...">

      <h3>The Poem</h3>               <!-- amber-500 -->
      <p>In 1855, Henry Wadsworth...</p>

      <h3>The Confusion</h3>           <!-- turquoise-400, mt-[6rem] -->
      <p>Hiawatha was a real...</p>
      <p>Longfellow apparently found...</p>
      <blockquote>...</blockquote>

      <!-- Historical image 1 -->

      <h3>The Forest</h3>              <!-- sun-400, mt-[6rem] -->
      <p>None of this diminished...</p>

      <!-- Historical image 2 -->

      <h3>The Ride</h3>                <!-- scarlet-400, mt-[6rem] -->
      <p>Into this magnificent...</p>

    </div>
  </section>
  ```

  **Critical details:**
  - The h3 subheadings go INSIDE the `<div class="max-w-prose space-y-4 ...">` container, not outside it
  - Use `mt-[6rem]` on all h3 except the first one (which uses `mt-0`) for generous section spacing
  - All 4 heading colors are WCAG AA text-safe: amber-500 (existing), turquoise-400 (7.78:1), sun-400 (9.46:1), scarlet-400 (5.24:1)
  - Do NOT use scarlet-600 for any heading text — it fails AA normal text
  - Historical images use `loading="lazy" decoding="async"` — they are below the fold
  - The `historical-break` figures sit OUTSIDE the paragraph flow — they are visual breaks between sections, with 6rem margin above and below
  - The sepia filter `sepia(80%) saturate(30%) brightness(0.9)` creates the "historical artifact" effect that visually distinguishes these from route photos
  - If `historicalPhotos` is empty (no images sourced yet), the conditionals prevent any rendering — build does not fail
  </action>
  <verify>
  - `grep -c "font-display" src/components/HiawathaExplainer.astro` returns at least 4 (4 h3 subheadings)
  - `grep "The Poem" src/components/HiawathaExplainer.astro` returns a match
  - `grep "The Confusion" src/components/HiawathaExplainer.astro` returns a match
  - `grep "The Forest" src/components/HiawathaExplainer.astro` returns a match (as h3 text, not just in h2)
  - `grep "The Ride" src/components/HiawathaExplainer.astro` returns a match
  - `grep "text-turquoise-400" src/components/HiawathaExplainer.astro` returns a match
  - `grep "text-sun-400" src/components/HiawathaExplainer.astro` returns a match
  - `grep "text-scarlet-400" src/components/HiawathaExplainer.astro` returns a match
  - `grep "historical-photos.json" src/components/HiawathaExplainer.astro` returns a match
  - `grep "historical-img" src/components/HiawathaExplainer.astro` returns a match
  - `grep "sepia" src/components/HiawathaExplainer.astro` returns a match
  - `grep "figcaption" src/components/HiawathaExplainer.astro` returns a match
  - `grep "mt-\[6rem\]" src/components/HiawathaExplainer.astro` returns at least 3 matches
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - HiawathaExplainer has 4 h3 subheadings in National Park typeface with 4 distinct colors (amber-500, turquoise-400, sun-400, scarlet-400)
  - 2 historical image breaks render between narrative sections with figcaption attribution
  - Images have sepia/desaturated CSS filter treatment distinct from route photos
  - 6rem whitespace between major editorial sections
  - Build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `ls images/historical/ | wc -l` returns 2
2. `cat public/data/historical-photos.json | grep -c '"thumb"'` returns 2
3. `grep -c "font-display" src/components/HiawathaExplainer.astro` returns >= 4
4. `grep "sepia" src/components/HiawathaExplainer.astro` returns a match
5. `grep "figcaption" src/components/HiawathaExplainer.astro` returns a match
6. `grep "text-turquoise-400\|text-sun-400\|text-scarlet-400\|text-amber-500" src/components/HiawathaExplainer.astro | wc -l` returns >= 4
7. `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
</verification>

<success_criteria>
- 2 historical Hiawatha illustrations sourced, processed through pipeline, and rendered between narrative sections
- HiawathaExplainer has 4 narrative beat subheadings in National Park typeface with 3+ distinct WCAG-safe colors
- Historical images have sepia filter treatment visually distinct from full-color route photography
- Generous 6-8rem whitespace between major editorial sections
- Build passes cleanly with populated historical-photos.json
</success_criteria>

<output>
After completion, create `.planning/phases/20-content-route-enrichment/20-01-SUMMARY.md`
</output>
