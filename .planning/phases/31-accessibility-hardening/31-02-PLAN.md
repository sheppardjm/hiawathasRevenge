---
phase: 31-accessibility-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteExplainer.astro
autonomous: true

must_haves:
  truths:
    - "Empty stars in difficulty ratings have a contrast ratio of at least 4.5:1 against the forest-800 card background"
    - "Sector panel open/close transitions are instant when prefers-reduced-motion is enabled"
  artifacts:
    - path: "src/components/RouteExplainer.astro"
      provides: "Star rating gradient with accessible empty-star contrast"
      contains: "color-amber-300"
    - path: "src/components/RouteMap.astro"
      provides: "A11Y-04 reduced-motion compliance (already implemented)"
      contains: "prefers-reduced-motion"
  key_links:
    - from: "src/components/RouteExplainer.astro"
      to: "star rating visual"
      via: "CSS gradient second color stop"
      pattern: "color-amber-300.*var\\(--percent\\)"
---

<objective>
Fix star rating empty-star contrast to meet WCAG AA minimum and verify sector panel reduced-motion compliance.

Purpose: Satisfy A11Y-03 (star rating contrast) and verify A11Y-04 (reduced motion) for WCAG AA compliance.
Output: Updated star rating gradient with accessible contrast, verified reduced-motion implementation.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-accessibility-hardening/31-RESEARCH.md
@src/components/RouteExplainer.astro
@src/components/RouteMap.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix star rating empty-star contrast</name>
  <files>src/components/RouteExplainer.astro</files>
  <action>
In `src/components/RouteExplainer.astro`, inside the `@supports (background-clip: text)` block (around line 231-243), change the gradient's second color stop from `var(--color-forest-700)` to `var(--color-amber-300)`.

Current code (line 233-236):
```css
background: linear-gradient(
  to right,
  var(--color-amber-500) var(--percent),
  var(--color-forest-700) var(--percent)
);
```

Change to:
```css
background: linear-gradient(
  to right,
  var(--color-amber-500) var(--percent),
  var(--color-amber-300) var(--percent)  /* A11Y-03: was forest-700 (1.58:1) — amber-300 gives 5.30:1 on forest-800 */
);
```

Contrast math:
- OLD: forest-700 (#3d6b3d) on forest-800 (#2d4a2d) = 1.58:1 — FAILS WCAG AA (4.5:1 minimum)
- NEW: amber-300 (#e0b95f) on forest-800 (#2d4a2d) = 5.30:1 — PASSES WCAG AA

The `--color-amber-300` token is already defined in global.css `@theme static` (line 27: `--color-amber-300: #e0b95f`). No new tokens needed.

Important:
- Do NOT change the first color stop (`var(--color-amber-500)`) — that's the filled-star color and already passes contrast.
- Do NOT change the solid amber fallback outside the `@supports` block — it uses `color: var(--color-amber-500)` which is already accessible.
- Do NOT change the panel-stars in RouteMap.astro — they use solid `color: var(--color-amber-500)` on forest-900 = 5.49:1, already passing.
  </action>
  <verify>
Verify the gradient change:
- `grep "color-amber-300" src/components/RouteExplainer.astro` should match the gradient line
- `grep "color-forest-700" src/components/RouteExplainer.astro` should NOT match inside the `@supports` block (it will still match in the topo background SVG, which is fine)

Run the Astro build: `cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/node node_modules/.bin/astro build 2>&1 | tail -5`
  </verify>
  <done>
Star rating gradient uses amber-300 for empty stars, giving 5.30:1 contrast ratio against forest-800 card background. WCAG AA minimum of 4.5:1 is met. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify sector panel reduced-motion compliance</name>
  <files>src/components/RouteMap.astro</files>
  <action>
Verify that `src/components/RouteMap.astro` already contains the `prefers-reduced-motion` media query that disables the sector panel transition. The research confirmed this exists at lines 92-96:

```css
@media (prefers-reduced-motion: reduce) {
  .sector-panel {
    transition: none;
  }
}
```

This is A11Y-04 — already implemented. No code change needed.

Add a comment marking the A11Y-04 requirement above the existing rule to make the connection explicit. Change:

```css
/* Respect reduced motion */
@media (prefers-reduced-motion: reduce) {
```

To:

```css
/* A11Y-04: Respect reduced motion — panel transitions instant */
@media (prefers-reduced-motion: reduce) {
```

Also verify that RouteMap.astro's `initMap()` already uses `prefersReducedMotion` for map animations (line 269: `const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;` and line 675: `animate: !prefersReducedMotion`). These are already correct. No changes needed.
  </action>
  <verify>
Verify reduced-motion rule exists:
- `grep "prefers-reduced-motion" src/components/RouteMap.astro` should show the media query
- `grep "A11Y-04" src/components/RouteMap.astro` should show the updated comment
- `grep "transition: none" src/components/RouteMap.astro` should match inside the reduced-motion block

Run the Astro build to confirm no regressions: `cd /Users/Sheppardjm/Repos/hiawathasRevenge && /Users/Sheppardjm/.volta/bin/node node_modules/.bin/astro build 2>&1 | tail -5`
  </verify>
  <done>
RouteMap.astro has `@media (prefers-reduced-motion: reduce) { .sector-panel { transition: none; } }` with A11Y-04 comment. Map fitBounds also respects reduced motion via `animate: !prefersReducedMotion`. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. Build completes without errors: `astro build` exits 0
2. `grep "color-amber-300" src/components/RouteExplainer.astro` matches the gradient
3. `grep "prefers-reduced-motion" src/components/RouteMap.astro` matches
4. `grep "A11Y-04" src/components/RouteMap.astro` matches
5. Star rating empty stars use amber-300 (#e0b95f) — 5.30:1 contrast on forest-800 (#2d4a2d) — passes WCAG AA 4.5:1 minimum
</verification>

<success_criteria>
- A11Y-03: Empty stars in difficulty ratings have contrast ratio of 5.30:1 (above 4.5:1 WCAG AA minimum) against forest-800 card background
- A11Y-04: Sector panel transitions are instant with prefers-reduced-motion enabled (verified + commented)
- Build succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/31-accessibility-hardening/31-02-SUMMARY.md`
</output>
