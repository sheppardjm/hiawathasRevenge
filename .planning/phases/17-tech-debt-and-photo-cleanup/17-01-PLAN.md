---
phase: 17-tech-debt-and-photo-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/pages/index.astro
  - public/data/photos-manifest.json
  - public/data/photos.json
autonomous: true
gap_closure: true

must_haves:
  truths:
    - ".topo-divider CSS rule does not exist anywhere in the codebase"
    - "prefers-reduced-motion: reduce disables .gpx-download transition at ALL viewport widths, not just >=640px"
    - "gold-600 inline comment accurately states AA large text only, not AA normal text"
    - "photos-manifest.json contains exactly 51 entries (3 duplicates removed)"
    - "photos.json contains exactly 51 entries after pipeline regeneration"
    - "Build passes cleanly with zero errors"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Clean CSS with no dead .topo-divider rule, accurate WCAG comment"
    - path: "src/pages/index.astro"
      provides: "Fixed style block with no brace imbalance"
    - path: "public/data/photos-manifest.json"
      provides: "51-entry manifest (3 duplicates removed)"
    - path: "public/data/photos.json"
      provides: "Regenerated 51-entry photo data"
  key_links:
    - from: "photos-manifest.json"
      to: "photos.json"
      via: "match-photos.js pipeline step"
      pattern: "51 entries after regeneration"
---

<objective>
Close all tech debt from v1.1 milestone audit: remove dead `.topo-divider` CSS, fix brace imbalance in index.astro style block, correct gold-600 WCAG comment, and remove 3 duplicate photo pairs from the data pipeline.

Purpose: Clean closure of v1.1 before archiving the milestone
Output: Three CSS fixes in two files, 9 duplicate files deleted, manifest updated, photos.json regenerated with 51 entries
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-tech-debt-and-photo-cleanup/17-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix three CSS issues (dead rule, brace imbalance, WCAG comment)</name>
  <files>src/styles/global.css, src/pages/index.astro</files>
  <action>
  **In `src/styles/global.css`:**

  1. Fix the gold-600 WCAG comment on line 47. Replace:
     ```
     /* Gold family — PASSES AA normal text on forest-900 and forest-950 */
     ```
     With:
     ```
     /* Gold family — gold-600 passes AA large text only (4.45:1); gold-500/400 pass AA normal text */
     ```

  2. Remove the dead `.topo-divider` rule and its comment (lines 180-187). Delete:
     ```css
     /* Topographic contour divider — decorative wilderness pattern */
     .topo-divider {
       height: 60px;
       background-image: url('data:image/svg+xml;utf8,...');
       background-repeat: repeat-x;
       background-size: 400px 60px;
       opacity: 0.7;
     }
     ```
     Keep the closing `}` on line 188 — it closes `@layer base`.

  **In `src/pages/index.astro`:**

  3. Fix the brace imbalance in the `<style>` block. The `@media (min-width: 640px) {` on line 105 opens but never closes, incorrectly wrapping the `prefers-reduced-motion` rule inside a viewport gate. Remove ONLY line 105 (`@media (min-width: 640px) {`). Do NOT add a closing brace. The result should be:
     ```css
     @media (prefers-reduced-motion: reduce) {
       .gpx-download {
         transition: none;
       }
     }
     ```
     This ensures mobile users with reduced-motion preference get `transition: none`.
  </action>
  <verify>
  - `grep -r "topo-divider" src/` returns zero results
  - `grep "Gold family" src/styles/global.css` shows "large text only"
  - In `src/pages/index.astro`, count opening and closing braces in the style block — they must match
  - The `prefers-reduced-motion` media query is NOT nested inside any other media query
  </verify>
  <done>
  - `.topo-divider` rule and comment are deleted from global.css
  - gold-600 comment reads "passes AA large text only (4.45:1)"
  - `prefers-reduced-motion` rule applies at all viewport widths (no wrapping `@media (min-width: 640px)`)
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove 3 duplicate photos and regenerate pipeline</name>
  <files>public/data/photos-manifest.json, public/data/photos.json</files>
  <action>
  **Step 1: Remove 3 duplicate entries from `public/data/photos-manifest.json`.**

  Remove these three entries (they have ` (1)` in the filename):
  - `{ "filename": "Ng6XqfSk8fbQ2lv6nfeaD9f48E4cWUpJtiNAwxR-NU8-1536x2048 (1).jpg", "mile": 39 }`
  - `{ "filename": "SKRm_qAOXfwauqbvmMD4ax-s0yHIwctXiGtAeI-LVGY-1536x2048 (1).jpg", "mile": 41.2 }`
  - `{ "filename": "bhrt78MHvrVBto-qBpwHU11DLfAMJrU2VT8M2CLyRq0-1536x2048 (1).jpg", "mile": 100.3 }`

  After removal, the manifest should have exactly 51 entries.

  **Step 2: Delete 3 source image files from `images/`.**

  ```bash
  rm "images/Ng6XqfSk8fbQ2lv6nfeaD9f48E4cWUpJtiNAwxR-NU8-1536x2048 (1).jpg"
  rm "images/SKRm_qAOXfwauqbvmMD4ax-s0yHIwctXiGtAeI-LVGY-1536x2048 (1).jpg"
  rm "images/bhrt78MHvrVBto-qBpwHU11DLfAMJrU2VT8M2CLyRq0-1536x2048 (1).jpg"
  ```

  **Step 3: Delete 3 public image files from `public/images/`.**

  ```bash
  rm "public/images/Ng6XqfSk8fbQ2lv6nfeaD9f48E4cWUpJtiNAwxR-NU8-1536x2048 (1).jpg"
  rm "public/images/SKRm_qAOXfwauqbvmMD4ax-s0yHIwctXiGtAeI-LVGY-1536x2048 (1).jpg"
  rm "public/images/bhrt78MHvrVBto-qBpwHU11DLfAMJrU2VT8M2CLyRq0-1536x2048 (1).jpg"
  ```

  **Step 4: Delete 3 thumbnail files from `public/thumbs/`.** Note: thumbnails use underscore `_(1)` not space ` (1)`, and `.webp` extension.

  ```bash
  rm "public/thumbs/Ng6XqfSk8fbQ2lv6nfeaD9f48E4cWUpJtiNAwxR-NU8-1536x2048_(1).webp"
  rm "public/thumbs/SKRm_qAOXfwauqbvmMD4ax-s0yHIwctXiGtAeI-LVGY-1536x2048_(1).webp"
  rm "public/thumbs/bhrt78MHvrVBto-qBpwHU11DLfAMJrU2VT8M2CLyRq0-1536x2048_(1).webp"
  ```

  **Step 5: Regenerate photos.json by running the pipeline.**

  ```bash
  PATH="/Users/Sheppardjm/.volta/bin:$PATH" node scripts/match-photos.js
  ```

  This rewrites `public/data/photos.json` from scratch using the updated manifest. The output should have 51 entries.

  **Step 6: Run full build to verify everything passes.**

  ```bash
  PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build
  ```
  </action>
  <verify>
  - `grep -c '"filename"' public/data/photos-manifest.json` returns 51
  - `grep -c '"filename"' public/data/photos.json` returns 51
  - `ls images/*"(1)"* 2>/dev/null | wc -l` returns 0
  - `ls public/images/*"(1)"* 2>/dev/null | wc -l` returns 0
  - `ls public/thumbs/*"(1)"* 2>/dev/null | wc -l` returns 0
  - `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` exits 0
  </verify>
  <done>
  - 3 duplicate `(1)` entries removed from photos-manifest.json (51 entries remain)
  - 9 duplicate files deleted (3 source + 3 public + 3 thumbnails)
  - photos.json regenerated with 51 entries
  - Full build passes cleanly
  </done>
</task>

</tasks>

<verification>
1. `grep -r "topo-divider" src/` — zero results
2. In `src/pages/index.astro` style block, `@media (prefers-reduced-motion: reduce)` is NOT nested inside `@media (min-width: 640px)`
3. `grep "Gold family" src/styles/global.css` — shows "large text only"
4. `grep -c '"filename"' public/data/photos-manifest.json` — returns 51
5. `grep -c '"filename"' public/data/photos.json` — returns 51
6. No `(1)` files remain in images/, public/images/, or public/thumbs/
7. `PATH="/Users/Sheppardjm/.volta/bin:$PATH" npm run build` — exits 0
</verification>

<success_criteria>
- Dead .topo-divider CSS rule removed — zero references in codebase
- Brace imbalance fixed — prefers-reduced-motion applies at all viewport widths
- gold-600 WCAG comment accurately states "AA large text only (4.45:1)"
- 3 duplicate photo pairs removed from manifest, source, public, and thumbnails (54 -> 51 entries)
- photos.json regenerated with 51 entries
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/17-tech-debt-and-photo-cleanup/17-01-SUMMARY.md`
</output>
