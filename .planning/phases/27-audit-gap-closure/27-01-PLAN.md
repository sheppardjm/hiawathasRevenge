---
phase: 27-audit-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/BaseLayout.astro
  - public/data/sector-details.json
  - src/components/RouteMap.astro
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "EB Garamond font loads and ::first-letter drop-caps render in EB Garamond (not system serif)"
    - "Panel click-outside behavior is documented as intentional trade-off (map stays interactive)"
    - "NF2217-2218 name is consistent between sector-details.json and RouteExplainer.astro SEGMENTS"
    - "No stale comments or dead CSS remain in RouteMap.astro"
  artifacts:
    - path: "src/layouts/BaseLayout.astro"
      provides: "EB Garamond Font component tag"
      contains: '<Font cssVariable="--font-garamond"'
    - path: "public/data/sector-details.json"
      provides: "Consistent NF2217-2218 sector name"
      contains: '"NF2217-2218"'
    - path: "src/components/RouteMap.astro"
      provides: "Clean tech debt — no stale comments, no dead ::backdrop CSS, documented click-outside trade-off"
  key_links:
    - from: "src/layouts/BaseLayout.astro"
      to: "astro.config.ts fonts[] array"
      via: "Font cssVariable='--font-garamond' matches config entry"
      pattern: '--font-garamond'
    - from: "public/data/sector-details.json"
      to: "src/components/RouteExplainer.astro SEGMENTS const"
      via: "NF2217-2218 name matches in both locations"
      pattern: 'NF2217-2218'
---

<objective>
Close all audit gaps from v1.3-MILESTONE-AUDIT.md: load EB Garamond font (VIS-02), document the MAP-08 click-outside trade-off, fix NF2217 display name inconsistency, and remove stale comment / dead CSS in RouteMap.astro.

Purpose: Final gap closure before v1.3 milestone can be archived as complete.
Output: Three files updated — BaseLayout.astro, sector-details.json, RouteMap.astro — with all audit gaps resolved.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-audit-gap-closure/27-RESEARCH.md
@src/layouts/BaseLayout.astro
@src/components/RouteMap.astro
@public/data/sector-details.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: VIS-02 font fix and NF2217 name consistency</name>
  <files>src/layouts/BaseLayout.astro, public/data/sector-details.json</files>
  <action>
**BaseLayout.astro (VIS-02):**
Add the missing EB Garamond Font tag in `<head>`, after the existing two Font tags (lines 24-25):

```astro
<Font cssVariable="--font-space-mono" preload />
<Font cssVariable="--font-national-park" preload />
<Font cssVariable="--font-garamond" />
```

Do NOT add `preload` to the Garamond tag. EB Garamond is only used for `::first-letter` drop-caps below the fold — preloading would add unnecessary critical-path weight. The font is already declared in `astro.config.ts` `fonts[]` with Google Fonts provider; the `<Font>` tag is what makes it actually load.

**sector-details.json (NF2217 name fix):**
Update the sector with `"id": "sector-nf2217"` — change `"name": "NF2217"` to `"name": "NF2217-2218"`. This matches the canonical name in `RouteExplainer.astro` SEGMENTS const (line 21) and the jump-link mapping key `'NF2217-2218': 'sector-nf2217'` (line 39). No other files need updating.
  </action>
  <verify>
1. `grep 'font-garamond' src/layouts/BaseLayout.astro` returns the new Font tag
2. `grep 'NF2217-2218' public/data/sector-details.json` returns the updated name
3. `grep -c 'preload' src/layouts/BaseLayout.astro` returns 2 (only Space Mono and National Park have preload)
4. Run `npx astro check` or `npx astro build` to confirm no build errors
  </verify>
  <done>EB Garamond Font tag present in BaseLayout.astro without preload; sector-details.json shows "NF2217-2218" matching RouteExplainer.astro; build passes cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: RouteMap.astro tech debt cleanup and MAP-08 documentation</name>
  <files>src/components/RouteMap.astro</files>
  <action>
Three targeted edits in RouteMap.astro:

**1. Remove dead `::backdrop` CSS (lines 133-136):**
Delete the entire block:
```css
/* Panel backdrop — native dialog ::backdrop (only renders with showModal(), dead code with show()) */
.sector-panel::backdrop {
  background: rgba(0, 0, 0, 0.3);
}
```
This CSS rule never executes because the project uses `dialog.show()` (non-modal), not `showModal()`. The comment already documents it as dead code.

**2. Update stale comment (line 482):**
Replace:
```javascript
// Build panel body — minimal content; Plan 25-02 adds full buildPanelBody() with sparkline
```
With:
```javascript
// Build panel body — stars, meta, sparkline, description, strava link, jump link
```

**3. Document MAP-08 click-outside trade-off (line 544):**
Replace the existing comment at the click-outside handler:
```javascript
// Click outside panel to close — dialog.show() has no backdrop, so listen on document
```
With a more complete explanation:
```javascript
// Click outside panel to close — dialog.show() (non-modal) has no native backdrop.
// Excludes .leaflet-interactive clicks intentionally: map stays fully interactive
// while panel is open. This is a deliberate trade-off for choosing show() over
// showModal(). Non-map clicks outside the panel (page header, sections) close it.
// Panel also closes via X button and Escape key.
```
  </action>
  <verify>
1. `grep '::backdrop' src/components/RouteMap.astro` returns no results (dead CSS removed)
2. `grep 'Plan 25-02' src/components/RouteMap.astro` returns no results (stale comment removed)
3. `grep 'deliberate trade-off' src/components/RouteMap.astro` returns the new MAP-08 documentation comment
4. `grep 'stars, meta, sparkline' src/components/RouteMap.astro` returns the updated comment
5. Run `npx astro build` to confirm no build errors
  </verify>
  <done>Dead `::backdrop` CSS removed; stale "Plan 25-02" comment replaced with accurate description; click-outside handler has full documentation of the MAP-08 trade-off decision; build passes cleanly.</done>
</task>

</tasks>

<verification>
1. `npx astro build` completes without errors — the Font tag resolves correctly and all component changes are valid
2. `grep 'font-garamond' src/layouts/BaseLayout.astro` confirms EB Garamond Font tag is present
3. `grep '::backdrop' src/components/RouteMap.astro` returns empty — dead code is gone
4. `grep 'Plan 25-02' src/components/RouteMap.astro` returns empty — stale comment is gone
5. `grep 'NF2217-2218' public/data/sector-details.json` returns the corrected name
6. `grep 'deliberate trade-off' src/components/RouteMap.astro` confirms MAP-08 decision is documented
</verification>

<success_criteria>
- EB Garamond font tag is in BaseLayout.astro head (without preload) — VIS-02 closed
- MAP-08 click-outside trade-off is documented in code comment — MAP-08 closed as "satisfied with documented exception"
- NF2217-2218 display name is consistent between sector-details.json and RouteExplainer.astro
- No dead `::backdrop` CSS or stale "Plan 25-02" comment remains in RouteMap.astro
- Astro build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/27-audit-gap-closure/27-01-SUMMARY.md`
</output>
