---
phase: 11-responsive-polish-and-production-build
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/RouteStats.astro
autonomous: true

must_haves:
  truths:
    - "Stats grid displays as single column on mobile (below 640px) and 3 columns on sm+ breakpoint"
    - "No horizontal overflow or clipping occurs at 375px viewport width"
    - "Surface breakdown text does not wrap awkwardly at 375px"
  artifacts:
    - path: "src/components/RouteStats.astro"
      provides: "Responsive grid-template-columns with mobile breakpoint"
      contains: "grid-template-columns: 1fr"
  key_links:
    - from: "src/components/RouteStats.astro"
      to: "CSS media query"
      via: "@media (min-width: 640px) controls 3-column layout"
      pattern: "min-width.*640"
---

<objective>
Fix the RouteStats 3-column grid that overflows at 375px mobile viewport. The current `grid-template-columns: repeat(3, 1fr)` creates three cramped ~109px columns with `font-size: 1.875rem` stat values that wrap or clip on small screens.

Purpose: DSGN-05 responsive layout requirement — the page must adapt correctly at 375px without overflow or clipping.
Output: Updated scoped styles in RouteStats.astro with responsive grid breakpoint.
</objective>

<execution_context>
@/Users/Sheppardjm/.claude/get-shit-done/workflows/execute-plan.md
@/Users/Sheppardjm/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-responsive-polish-and-production-build/11-RESEARCH.md
@src/components/RouteStats.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add responsive grid breakpoint to RouteStats stats grid</name>
  <files>src/components/RouteStats.astro</files>
  <action>
    In the scoped `<style>` block of `RouteStats.astro`, change the `.stats-grid` rule from a fixed 3-column layout to a mobile-first responsive layout:

    Replace the existing `.stats-grid` rule:

    ```css
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 1rem;
      margin-bottom: 1.5rem;
    }
    ```

    With:

    ```css
    .stats-grid {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1rem;
      margin-bottom: 1.5rem;
    }

    @media (min-width: 640px) {
      .stats-grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }
    ```

    This makes the stats stack vertically on mobile (below 640px) where three columns would create ~109px cells that can't comfortably fit the `1.875rem` stat values plus labels. At `sm` (640px+), the 3-column grid provides enough room per cell (~200px each).

    **No other changes needed:**
    - The badge (300px on mobile) fits within the 375px viewport (80% width) — no change needed.
    - The map (min-height: 400px) is intentionally tall on mobile — no change needed.
    - The elevation chart (h-[140px] sm:h-[180px]) already has responsive heights — no change needed.
    - The photo gallery (grid-cols-2 sm:grid-cols-3 lg:grid-cols-4) already handles breakpoints — no change needed.
    - The page container (max-w-4xl mx-auto px-4) provides proper 16px side padding on mobile — no change needed.
  </action>
  <verify>
    Run: `grep 'grid-template-columns: 1fr' /Users/Sheppardjm/Repos/hiawathasRevenge/src/components/RouteStats.astro`
    Expect: Match found — mobile-first single-column default.

    Run: `grep 'min-width.*640' /Users/Sheppardjm/Repos/hiawathasRevenge/src/components/RouteStats.astro`
    Expect: Match found — breakpoint for 3-column layout.

    Run: `grep 'repeat(3' /Users/Sheppardjm/Repos/hiawathasRevenge/src/components/RouteStats.astro`
    Expect: Match found — 3-column grid still used at sm+ breakpoint.

    Run: `PATH="/usr/local/opt/node/bin:$PATH" npx astro check 2>&1 | tail -5`
    Expect: No type errors.
  </verify>
  <done>Stats grid displays as 1 column on mobile (below 640px) and 3 columns on sm+ screens. No overflow or clipping at 375px viewport width.</done>
</task>

</tasks>

<verification>
1. `grep 'grid-template-columns: 1fr' src/components/RouteStats.astro` — single-column mobile default present
2. `grep -A1 'min-width.*640' src/components/RouteStats.astro` — 3-column breakpoint at 640px
3. The file should contain BOTH `grid-template-columns: 1fr` (mobile default) AND `repeat(3, 1fr)` (sm+ breakpoint)
4. Verify key components at 768px and 1280px viewports via static analysis:
   - `grep 'grid-cols' src/components/PhotoGallery.astro` — gallery uses responsive grid-cols (grid-cols-2 sm:grid-cols-3 lg:grid-cols-4) that adapts at 768px and 1280px
   - `grep 'sm:h-\|lg:h-' src/components/ElevationProfile.astro` — chart has responsive height breakpoints
   - `grep 'max-w-' src/pages/index.astro` — page container max-width constrains content properly at 1280px
   - `grep 'width.*300\|max-width' src/components/RouteStats.astro` — badge width stays reasonable at wider viewports
</verification>

<success_criteria>
- RouteStats.astro stats grid uses 1-column layout by default (mobile-first)
- 3-column grid kicks in at 640px+ (sm breakpoint)
- No horizontal overflow at 375px viewport
- Badge, map, chart, gallery, and page container layouts confirmed correct at 768px and 1280px (no changes needed)
- No type or build errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-responsive-polish-and-production-build/11-03-SUMMARY.md`
</output>
