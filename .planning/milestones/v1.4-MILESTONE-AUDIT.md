---
milestone: v1.4
audited: 2026-04-06T17:00:00Z
updated: 2026-04-06T18:30:00Z
status: passed
scores:
  requirements: 15/15
  phases: 5/5
  integration: 18/18
  flows: 6/6
gaps: []
tech_debt:
  - phase: 29-seo-social-sharing
    items:
      - "astro.config.ts TODO: update site URL to actual deployed domain before going live"
---

# v1.4 Performance & Polish — Milestone Audit

**Audited:** 2026-04-06 (initial)
**Updated:** 2026-04-06 (post-Phase-32 integration check)
**Status:** PASSED
**Milestone Goal:** Ship-ready polish — tech debt cleanup, SEO/social sharing, image optimization, and accessibility hardening

## Scores

| Category | Score | Status |
|----------|-------|--------|
| Requirements | 15/15 | All satisfied |
| Phases | 5/5 verified | All passed |
| Integration | 18/18 connections | All wired |
| E2E Flows | 6/6 complete | All verified |

## Requirements Coverage

| Requirement | Phase | Status | Notes |
|-------------|-------|--------|-------|
| IMG-01 | 30 | SATISFIED | Hero WebP srcset (640w/1280w/1600w) with `<picture>` element |
| IMG-02 | 30 | SATISFIED | `<link rel="preload">` with imagesrcset for LCP |
| IMG-03 | 30 | SATISFIED | Gallery serves .webp thumbnails |
| IMG-04 | 30 | SATISFIED | CSS image-set() on 3 parallax backgrounds |
| A11Y-01 | 31 | SATISFIED | Global :focus-visible baseline + component overrides |
| A11Y-02 | 31 | SATISFIED | Gallery alt text "Route photo at mile X.X" |
| A11Y-03 | 31 | SATISFIED | amber-300 empty stars (5.30:1 contrast) |
| A11Y-04 | 31 | SATISFIED | Sector panel transition: none under prefers-reduced-motion |
| SEO-01 | 29 | SATISFIED | OpenGraph tags with absolute URLs |
| SEO-02 | 29 | SATISFIED | Twitter Card summary_large_image |
| SEO-03 | 29 | SATISFIED | Canonical link from Astro.site |
| SEO-04 | 29 | SATISFIED | Event JSON-LD for June 6, 2026 |
| DEBT-01 | 28 | SATISFIED | --font-body eliminated, --font-serif in use |
| DEBT-02 | 28+32 | SATISFIED | Source fixed in resolve-annotations.js (Phase 32 closed the regression) |
| DEBT-03 | 28 | SATISFIED | @supports guard with Firefox fallback |

## Phase Verification Summary

| Phase | Status | Score | Critical Gaps | Tech Debt |
|-------|--------|-------|---------------|-----------|
| 28: Tech Debt Cleanup | Passed | 3/3 | None | None |
| 29: SEO & Social Sharing | Passed | 4/4 | None | TODO: update site URL |
| 30: Image Optimization | Passed | 4/4 | None | None |
| 31: Accessibility Hardening | Passed | 4/4 | None | None |
| 32: Pipeline Source Fixes | Passed | 2/2 | None | None |

---

## Integration Check (post-Phase-32)

*Performed 2026-04-06 by integration checker. All code read from actual source files.*

### Wiring Summary

**Connected:** 18 exports/connections properly wired
**Orphaned:** 0 exports created but unused
**Missing:** 0 expected connections not found

### API Coverage

No HTTP API routes. All data flows are:
- Pipeline scripts → JSON files in `public/data/`
- Astro components fetch those JSON files at runtime via `fetch('/data/*.json')` in client `<script>` blocks
- Build-time data via Astro content collections (`getEntry`, `getCollection`)

**All fetch targets verified present:**
- `public/data/route-data.json` — exists
- `public/data/annotations.json` — exists, NF2217-2218 correct
- `public/data/sector-details.json` — exists, NF2217-2218 correct
- `public/data/sector-elevations.json` — exists, NF2217-2218 correct
- `public/data/surface-points.json` — exists
- `public/data/photos.json` — exists

### Cross-Phase Dependency Verification

#### Dependency 1: Phase 28 (DEBT-03) @supports guard + Phase 31 (A11Y-03) amber-300 fix

**Status: WIRED CORRECTLY**

`/Users/Sheppardjm/Repos/hiawathasRevenge/src/components/RouteExplainer.astro` lines 221–247 confirm:

- Base `.star-rating` rule sets `color: var(--color-amber-500)` — solid amber fallback for all browsers (Firefox included)
- `@supports (background-clip: text) or (-webkit-background-clip: text)` block adds the gradient only where the feature is supported
- Inside the @supports block, gradient second stop is `var(--color-amber-300)` with inline comment `/* A11Y-03: was forest-700 (1.58:1) — amber-300 gives 5.30:1 on forest-800 */`

The Phase 31 amber-300 change lives entirely **inside** the @supports block, not in the fallback. This is correct: Firefox gets solid amber-500 (passing contrast); Chrome/Safari get the gradient where the second stop is amber-300 (also passing contrast). The @supports guard from Phase 28 and the color fix from Phase 31 compose without conflict.

#### Dependency 2: Phase 28 (DEBT-02) data fix + Phase 32 source fix

**Status: WIRED CORRECTLY**

Phase 28 fixed `public/data/annotations.json`, `sector-details.json`, `sector-elevations.json` (the output files).
Phase 32 fixed the regression source at `/Users/Sheppardjm/Repos/hiawathasRevenge/scripts/resolve-annotations.js` line 27: `name: 'NF2217-2218'`.

The data files currently contain `"NF2217-2218"` (verified via grep). The pipeline source now also generates `"NF2217-2218"`. A pipeline re-run will not revert the name. Both layers are consistent.

The `SECTOR_IDS` map in `RouteExplainer.astro` line 39 maps `'NF2217-2218'` → `'sector-nf2217'`, matching the `id` field in all data files. The DOM anchor `id="sector-nf2217"` and the panel jump link `href="#sector-nf2217"` round-trip correctly.

#### Dependency 3: Phase 29 (generate-og-image.js) + Phase 32 (pipeline wiring)

**Status: WIRED CORRECTLY**

`/Users/Sheppardjm/Repos/hiawathasRevenge/scripts/pipeline.js` line 29 contains:
`{ name: 'generate-og-image', script: 'scripts/generate-og-image.js' }` as the final entry in the `steps` array.

`/Users/Sheppardjm/Repos/hiawathasRevenge/scripts/generate-og-image.js` exists and reads `public/images/irrVhAXHnnFzslJGVemLiPEy5iQFbqZF6VzqxYOHL1o-2048x1536.jpg` → writes `public/og-image.jpg` (1200×630 JPEG).

`/Users/Sheppardjm/Repos/hiawathasRevenge/public/og-image.jpg` exists at 233KB on disk.

`BaseLayout.astro` line 16 builds the OG image URL as `new URL('/og-image.jpg', Astro.site)` and uses it in `og:image` and `twitter:image` meta tags and in the Event JSON-LD `"image"` field. The chain is complete.

#### Dependency 4: Phase 30 (generate-webp.js) + Phase 32 (both in pipeline)

**Status: WIRED CORRECTLY**

`pipeline.js` step order (lines 17–30):
1–7: parse-gpx through generate-thumbnails
8: `generate-webp` (Phase 30) — position 8 of 12
9–11: copy-images, process-historical, match-photos, copy-gpx
12: `generate-og-image` (Phase 32) — final step

Both steps are present. `generate-webp.js` has no dependency on `generate-og-image.js` and `generate-og-image.js` has no dependency on `generate-webp.js`. Ordering is safe.

### E2E Flow Verification

#### Flow 1: Pipeline — `npm run pipeline`

**Status: COMPLETE**

12 steps in sequence. Both Phase 30 (`generate-webp`) and Phase 32 (`generate-og-image`) are registered. `pipeline.js` uses `execFileSync` with `process.execPath` (respects Volta/nvm). On failure, `currentStep` name is logged and process exits with code 1. All output files for downstream flows exist on disk.

#### Flow 2: Font rendering chain

**Status: COMPLETE**

`astro.config.ts` → Spectral font, `cssVariable: '--font-spectral'`, weights [400,700]
→ `BaseLayout.astro` line 91: `<Font cssVariable="--font-spectral" />` (no preload — below-fold correct)
→ `global.css` line 113: `--font-serif: var(--font-spectral, 'Spectral', Georgia, serif)`
→ `RouteMap.astro` line 56: `.sector-panel { font-family: var(--font-serif); }`

Note: `.sector-panel__title` overrides to `var(--font-display)` (line 108) — this is intentional. The heading uses the display font; body text in `.sector-panel__body` inherits `--font-serif` from the panel container. No break in the chain.

#### Flow 3: Image optimization chain

**Status: COMPLETE**

`generate-webp.js` produces three hero WebP variants at 640w/1280w/1600w and three parallax WebPs. All six files verified on disk in `public/images/`.

`HeroSection.astro` uses `<picture>` with `<source type="image/webp" srcset="…640w…1280w…1600w">` pointing to the exact filenames generate-webp.js produces.

`BaseLayout.astro` preload tag references the same filenames with `imagesrcset` for LCP optimization. Filenames match exactly across: generate-webp.js output, HeroSection srcset, and BaseLayout preload.

`HiawathaExplainer.astro` parallax sections use `image-set()` with `.webp` as first type and `.jpg` as JPEG fallback. All three WebP files exist.

#### Flow 4: SEO metadata chain

**Status: COMPLETE**

`generate-og-image.js` → `public/og-image.jpg` (1200×630, 233KB)
→ `BaseLayout.astro`: `ogImageURL = new URL('/og-image.jpg', Astro.site)` → `Astro.site = 'https://hiawathasrevenge.com'` from `astro.config.ts`
→ `og:image` meta, `twitter:image` meta, Event JSON-LD `"image"` field — all use `ogImageURL.href`
→ `og:image:width="1200"`, `og:image:height="630"` match the actual generated dimensions

One open item: `astro.config.ts` has a `TODO` comment on the `site` value. The domain `hiawathasrevenge.com` is hardcoded. This is a deployment concern, not a wiring failure — the chain is correctly assembled.

#### Flow 5: Accessibility layer composition

**Status: COMPLETE**

Layer 1 — global baseline (`global.css` lines 162–167):
`a:focus-visible, button:focus-visible { outline: 2px solid var(--color-amber-500); ... }`

Layer 2 — component override (`RouteMap.astro` lines 129–134):
`.sector-panel__close:focus-visible { outline: 2px solid var(--color-amber-500); outline-offset: 2px; }`
(Slightly tighter offset than global 3px — intentional for the close button's position context)

Layer 3 — section color override (`index.astro` lines 148–151):
`.gold-section :global(a:focus-visible), .gold-section :global(button:focus-visible) { outline-color: var(--color-forest-950); }`
(Overrides amber ring to dark on the yellow/sun-500 background — correct for contrast)

Layer 4 — reduced-motion for panel transitions (`RouteMap.astro` lines 91–96):
`@media (prefers-reduced-motion: reduce) { .sector-panel { transition: none; } }`

Layer 5 — reduced-motion for map fitBounds (`RouteMap.astro` line 682):
`map.fitBounds(initialBounds, { padding: [20, 20], animate: !prefersReducedMotion })`
`prefersReducedMotion` is evaluated once at map init from `window.matchMedia`.

Layer 6 — reduced-motion for Leaflet CSS (`global.css` lines 206–219):
`@media (prefers-reduced-motion: reduce)` suppresses Leaflet zoom/fade/pan animations.

Phase 28's @supports guard and Phase 31's amber-300 fix compose correctly (see Dependency 1 above). No conflicts between layers.

#### Flow 6: Sector click — full interaction chain

**Status: COMPLETE**

1. User clicks sector polyline → `ghostPoly.on('click')` fires
2. `sectorDetails` looked up from `sector-details.json` (fetched at init via Promise.all)
3. `sectorElevations` looked up from `sector-elevations.json`
4. `openPanel({ sector, details, elevData })` called
5. Panel title set from `details.name` ("NF2217-2218") or `sector.name` fallback
6. Stars built as Unicode `★★☆☆☆` with `aria-label="Difficulty: N of 5 stars"`
7. Sparkline SVG generated client-side via `generateSparklineSvg(elevData)`
8. `panel.show()` opens the dialog (non-modal)
9. Close button receives focus (keyboard accessibility)
10. Close button has `:focus-visible` ring (RouteMap component rule)
11. On close: `panel.addEventListener('close', ...)` resets active sector style and returns focus to `previousFocus`
12. `transition: none` fires if `prefers-reduced-motion: reduce`

All 12 steps present in code. Chain is complete.

### Orphaned Code

None identified. All scripts in `scripts/` are registered in `pipeline.js`. All components in `src/components/` are imported in `src/pages/index.astro`.

### Missing Connections

None. All cross-phase dependencies verified wired.

### Remaining Tech Debt (non-blocking)

1. **`astro.config.ts` site URL** — `'https://hiawathasrevenge.com'` with TODO comment. Canonical URLs and OG image URLs resolve correctly for this domain. If the deployed domain differs, all absolute URLs in BaseLayout will need updating. Not a wiring failure — a deployment checklist item.

2. **`--font-spectral` loaded without `preload`** — Intentional. Spectral is used only in the sector panel (below-fold). No performance concern.

3. **`generate-webp.js` uses skip-if-exists logic** — Idempotent by design. New images added to the pipeline that need WebP conversion must have their basenames added to `PARALLAX_BASENAMES` array. Not a current gap.

---

## Human Verification Items (requiring browser)

These require browser-based confirmation — code is correct but cannot be auto-verified:

1. Spectral font rendering in sector panel (Phase 28)
2. Star rating cross-browser rendering: Chrome gradient vs Firefox solid amber (Phase 28)
3. Google Rich Results Test on deployed URL (Phase 29)
4. Social share preview on Facebook/LinkedIn/Twitter (Phase 29)
5. Lighthouse LCP audit — preload link correctly hints WebP hero (Phase 30)
6. Parallax WebP visual regression check (Phase 30)
7. Keyboard focus ring visibility across all interactive elements (Phase 31)
8. Reduced-motion panel behavior (Phase 31)
9. Star rating amber-300 contrast visual check (Phase 31)

---

_Initial audit: 2026-04-06T17:00:00Z_
_Integration check updated: 2026-04-06T18:30:00Z_
_Auditor: Claude (milestone-auditor + integration-checker)_
